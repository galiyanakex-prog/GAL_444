# Сценарий ручной проверки — День 11 «Модель памяти агента (memory layers)»

> Чек-лист для живой проверки проекта `Nedela_3/den_11` руками, без автотестов.
> Время прохождения: ~30–35 минут. Живые запросы к API — платные (≈1–3 ₽ на весь сценарий);
> Части 5–6 полностью идут на `--mock` (бесплатно, ключ не нужен).
> Отметки: ☐ — не выполнено, ✅ — прошло, ❌ — не прошло (записать, что именно).
> Артефакт процесса: файл живёт только в `den_11_dev/` (п.19). Машиночитаемый дубль —
> `den_11_dev/tests_debug/check_acceptance.sh` (Часть 7).

## Подготовка (5 минут)

☐ **П.1** Открыть терминал в каталоге `Nedela_3/den_11`.

☐ **П.2** Убедиться, что ключ API и venv на месте:
```bash
grep -q "API_KEY" ../../.env && echo "ключ есть" || echo "ключа НЕТ"
test -x ../../.venv/bin/python && echo "venv есть" || echo "venv НЕТ"
```
Ожидание: «ключ есть», «venv есть». Если ключа нет — создать `.env` по `.env.example` в
корне `AI_9` (для Частей 5–6 он всё равно не нужен).

☐ **П.3** Запомнить короткий путь до интерпретатора (далее по тексту используется `$PY`):
```bash
PY=../../.venv/bin/python
```

☐ **П.4** Зафиксировать, что было до проверки (артефакты прошлых прогонов допустимы,
но их содержимое не должно меняться в ходе Частей 1–4 для чужих пользователей):
```bash
ls users/ && ls Den_11_log.md den_11_tokens.csv 2>/dev/null
```
Ожидание: есть `users/profiles.db`; пользователя `checker11` и `/tmp/den11_chk` — нет
(они появятся в ходе проверки — это нормально).

---

## Часть 1. Идентификация и интервью-инициализация (5 минут, живой API)

☐ **1.1** Запустить нового пользователя:
```bash
./den_11_run.sh --user checker11
```

☐ **1.2** Проверить порядок старта: сначала идентификация (`--user checker11`), и только
затем решение «памяти нет → интервью». Ожидание первой строки:
`[Инициализация] Новый пользователь «checker11» — проведу интервью.`

☐ **1.3** Пройти интервью (4 вопроса — имя + три составляющие профиля):
```
Имя:Checker
краткий
Python, без воды
Учусь делать агентов, цель — сдать неделю 3
```
Ожидание: `[Инициализация] Профиль и дерево users/checker11/ созданы.`

☐ **1.4** Проверить шапку: `[Режим] Доставка слоёв: ['long_term', 'profile', 'short_term', 'working']`
(все четыре слоя по умолчанию), приглашение — `Вы [Основная_задача]: ` (задача видна в промпте).

☐ **1.5** Отправить 3 сообщения (живой API):
```
Меня зовут Алексей, мой стек Python
Давай сделаем интернет-магазин косметики
Срок запуска — 1 октября
```
Ожидание: на каждое — ответ `Агент: …`, без `[Ошибка]`.

☐ **1.6** Ввести `/exit`.
Ожидание: `[Выход] Состояние сохранено. До встречи!`

---

## Часть 2. Явная маршрутизация: какие данные в какой слой (6 минут, без API)

☐ **2.1** Открыть журнал маршрутизации:
```bash
tail -20 Den_11_log.md
```
Ожидание: строки вида `[Память] profile ← ['id', 'name', 'style', 'constraints', 'context'] → …`,
`[Память] working ← ['description'] → …`, `[Память] long_term ← ['tasks'] → …`,
`[Память] remember(short_term) → session.json +M1`, плюс строка
`[Хранилище] profile = JSON в SQLite (таблица profiles, user_id=checker11)`.
По логгу видно **какой слой ← что → куда** — автосохранения «всего в один файл» нет.

☐ **2.2** Проверить неизменяемость и цепочку `parent_id`:
```bash
$PY -c "import json,glob;p=sorted(glob.glob('users/checker11/tasks/*/sessions/*/session.json'))[-1];m=json.load(open(p))['messages'];print([(x['id'],x['parent_id'],x['role']) for x in m])"
```
Ожидание: 6 сообщений `M1…M6`, у `M1` parent — `None`, у каждого следующего parent = id
предыдущего; роли чередуются `user` / `assistant`.

☐ **2.3** Проверить иерархию по канону куратора:
```bash
find users/checker11 -type f | sort
```
Ожидание ровно эти файлы:
```
users/checker11/profile.json
users/checker11/long_term_memory.json
users/checker11/tasks/Основная_задача/working_memory.json
users/checker11/tasks/Основная_задача/sessions/<ГГГГММДД_ЧЧММСС>/session.json
```
(`sessions_resume.md` появится после `/exit` — см. 4.8.)

☐ **2.4** Проверить профиль как отдельную сущность в SQLite:
```bash
$PY -c "import sqlite3;print(sqlite3.connect('users/profiles.db').execute('select user_id,updated_at from profiles').fetchall())"
```
Ожидание: строка `('checker11', '<дата>')` — профиль живёт в БД, `profile.json` лишь зеркало.

☐ **2.5** Проверить, что в долговременной памяти профиль — **ссылка**, а не содержимое:
```bash
cat users/checker11/long_term_memory.json
```
Ожидание: ключи `profile_ref` (= `checker11`), `tasks`, `decisions`, `knowledge`; тела
профиля (style/constraints/context) внутри **нет**.

☐ **2.6** Проверить, что рабочая память — состояние, а не лог:
```bash
$PY -c "import json;d=json.load(open('users/checker11/tasks/Основная_задача/working_memory.json'));print(sorted(d));print(d['current_state'])"
```
Ожидание: ключи `description, refs, lifecycle_summary, decisions, constraints, facts,
open_questions, current_state`; стадия `planning`. **Сообщений диалога здесь нет** — они только в `session.json`.

☐ **2.7** В живом чате (перезапустить `./den_11_run.sh --user checker11`) ввести `/memory`.
Ожидание: четыре блока в каноническом порядке `[profile] → [long_term] → [working] → [short_term]`,
в каждом — своё содержимое. Ввести `/profile` — видны `Стиль / Ограничения / Контекст`. `/exit`.

---

## Часть 3. Дозированная доставка и влияние памяти на ответы (6 минут, живой API)

☐ **3.1** Наполнить долговременную память проверяемым фактом (иначе сравнивать нечего):
```bash
$PY -c "import json;p='users/checker11/long_term_memory.json';d=json.load(open(p));d['decisions'].append({'text':'Стек проекта: Python + PostgreSQL','status':'выполнена'});json.dump(d,open(p,'w'),ensure_ascii=False,indent=2)"
```

☐ **3.2** Запустить чат и выполнить `/compare`:
```bash
./den_11_run.sh --user checker11
/compare
```
Ожидание: два ответа на один вопрос («Какой стек я предпочитаю и почему?»):
```
С long_term  (~N вх. токенов): …Python…PostgreSQL…
БЕЗ long_term (~M вх. токенов): …про стек не знает…
```
`M < N`, и во втором ответе факта из долговременной памяти нет. Это доказательство
одновременно дозированной доставки и влияния памяти на ответы (п.5, п.14).

☐ **3.3** ⚠️ Зафиксировать побочный эффект: после `/compare` режим доставки остаётся
урезанным. Ввести `/memory` — в списке командного режима `long_term` отсутствует.
Вернуть вручную: `/deliver profile,long_term,working,short_term`
Ожидание: `[Доставка] Теперь: ['long_term', 'profile', 'short_term', 'working']`.
(Если не вернуть — все последующие ответы идут без долговременной памяти.)

☐ **3.4** Отключить один слой и убедиться, что он физически уходит из запроса:
```
/deliver profile,working
Привет, что ты помнишь про мой проект?
```
Ожидание: ответ опирается только на профиль и рабочую память; в `Den_11_log.md` —
строка `[Агент] M7: ответ получен (доставка: ['profile', 'working'])`.

☐ **3.5** Проверить отказные случаи:
- `/deliver` без аргументов → `[Доставка] Укажите слои: /deliver profile,working`;
- `/deliver foo,bar` → `[Доставка] Теперь: []` (мусор отброшен, чат жив);
- `/task` без имени → `[Задача] Укажите имя: /task <имя>`.

☐ **3.6** Токен-учёт: ввести `/tokens`, затем `/cost`.
Ожидание: непустые счётчики обменов и оценка в ₽; строка с указанием CSV-журнала.
Выйти `/exit` и проверить журнал:
```bash
tail -5 den_11_tokens.csv
```
Ожидание: заголовок `exchange,timestamp,prompt_tokens,cost_rubles,cost_total_rubles`
и строки с монотонно растущим `cost_total_rubles`.

---

## Часть 4. Задачи, переходы, resume, state machine (6 минут, живой API)

☐ **4.1** В чате (`./den_11_run.sh --user checker11`) ввести `/tasks`.
Ожидание: `[Задачи] Всего: 1; активная: Основная_задача` и строка `* Основная_задача`.

☐ **4.2** Создать/переключить задачу с пробелом в имени:
```
/task Подбор косметики
```
Ожидание: `[Задача] Переключился на «Подбор косметики»`, приглашение стало
`Вы [Подбор косметики]: `.

☐ **4.3** Проверить, что имя корректно легло в ФС (`safe_name`):
```bash
find users/checker11/tasks -maxdepth 1 -type d | sort
```
Ожидание: каталог `Подбор_косметики` (пробел → подчёркивание), внутри — `sessions/`.

☐ **4.4** Отправить в новой задаче одно сообщение, затем `/tasks`.
Ожидание: `Всего: 2`, звёздочка у активной; в `/memory` блок `[working]` — про новую задачу.

☐ **4.5** Проверить отметку перехода в СТАРОЙ задаче:
```bash
$PY -c "import json;print(json.load(open('users/checker11/tasks/Основная_задача/working_memory.json'))['open_questions'])"
```
Ожидание: элемент вида `Переход на задачу «Подбор косметики» (сессия <id>)`.

☐ **4.6** Проверить ссылку на сессию-источник в долговременной памяти:
```bash
$PY -c "import json;print(json.load(open('users/checker11/long_term_memory.json'))['tasks'])"
```
Ожидание: две задачи; у второй есть `source_session` с id сессии-источника.

☐ **4.7** Ввести `/state`.
Ожидание: четыре стадии и матрица переходов:
```
planning → execution
execution → planning, validation
validation → execution, planning
done → —
```

☐ **4.8** Ввести `/exit`, затем `./den_11_run.sh --user checker11` (resume).
Ожидание: интервью НЕ повторяется, сразу `[Память] Пользователь «checker11» найден,
контекст загружен. Задача: …`; создаётся **новая** сессия (новый `<sid>`), а старая цела.
Проверить резюме: `/summary` и
```bash
cat users/checker11/tasks/*/sessions_resume.md
```
Ожидание: заголовок `# Резюме сессий задачи «…»` и строка `Последняя сессия: <id>`.

☐ **4.9** ⚠️ Зафиксировать, какая задача восстановилась. `load_state()` берёт `tasks[0]`,
поэтому ожидается `Основная_задача`, а не последняя активная `Подбор косметики`.
Если так — записать как недочёт «активная задача не сохраняется» (в чек-листе — заметка,
не падение). Проверить `--fresh`: `./den_11_run.sh --user checker11 --fresh` → интервью
повторяется, профиль дополняется слиянием (старые поля не пропали: `/profile`). `/exit`.

---

## Часть 5. Mock-режим: доставка видна напрямую (4 минуты, без ключа)

☐ **5.1** Запустить без ключа (временно переименовать `.env` не нужно — `--mock` сам берёт заглушку):
```bash
./den_11_run.sh --mock --user mockuser
```
Ожидание: `[Режим] MockClient (заглушка) — живых запросов к API не будет.`

☐ **5.2** Отправить сообщение и прочитать ответ заглушки:
```
Привет
```
Ожидание: `[Mock] вижу N сообщений: system:Ты полезный ассистент… || system:[profile]… ||
system:[long_term]… || system:[working]… || user:… || assistant:… || user:Привет`.
По списку блоков **напрямую видно**, какие слои реально попали в запрос.

☐ **5.3** Урезать доставку и повторить тот же вопрос:
```
/deliver profile
Привет
```
Ожидание: в ответе заглушки остались только роль, `system:[profile]` и текущий запрос —
блоков `[long_term]`, `[working]` и сообщений краткосрочной памяти нет.

☐ **5.4** Проверить токен-эффект урезания: `/tokens`, `/cost` — у урезанного запроса
оценка ниже, чем у полного. Ввести `/exit`.

---

## Часть 6. Устойчивость (5 минут, mock + изолированное хранилище)

Все проверки Части 6 идут в `/tmp` через `--memory-dir` — рабочие `users/` не затрагиваются.

☐ **6.1** Новый пользователь + пустое хранилище (интервью по stdin):
```bash
printf 'Имя\nкраткий\nPython\nтест\nПривет\n/exit\n' | \
  ./den_11_run.sh --mock --user probe --memory-dir /tmp/den11_chk/users
```
Ожидание: интервью пройдено, ответ `[Mock] …`, `[Выход]`, БЕЗ Traceback.

☐ **6.2** Битый файл долговременной памяти:
```bash
echo "битый json" > /tmp/den11_chk/users/probe/long_term_memory.json
printf 'Привет\n/memory\n/exit\n' | ./den_11_run.sh --mock --user probe --memory-dir /tmp/den11_chk/users
```
Ожидание: `[Память] Пользователь «probe» найден, контекст загружен`, в `/memory` слой
`[long_term]` вернулся к схеме по умолчанию (`profile_ref`, пустые `tasks/decisions/knowledge`),
БЕЗ Traceback.

☐ **6.3** Битая рабочая память:
```bash
echo "мусор" > /tmp/den11_chk/users/probe/tasks/Основная_задача/working_memory.json
printf 'Привет\n/memory\n/exit\n' | ./den_11_run.sh --mock --user probe --memory-dir /tmp/den11_chk/users
```
Ожидание: `[working]` восстановлен дефолтами (`current_state: planning`), чат жив, БЕЗ Traceback.

☐ **6.4** Потерянная база профилей (зеркало JSON остаётся):
```bash
mv /tmp/den11_chk/users/profiles.db /tmp/den11_chk/profiles.db.bak
printf 'Имя\nподробный\nнет\nснова\nПривет\n/exit\n' | ./den_11_run.sh --mock --user probe --memory-dir /tmp/den11_chk/users
```
Ожидание: `exists()` → False → снова интервью, но `profile.json` не потерян (запись слиянием);
БЕЗ Traceback. Вернуть базу: `mv /tmp/den11_chk/profiles.db.bak /tmp/den11_chk/users/profiles.db`.

☐ **6.5** Ввод в «ломаной» кодировке (эмуляция CP1251):
```bash
printf '\xcf\xf0\xe8\xe2\xe5\xf2\n/exit\n' | ./den_11_run.sh --mock --user probe --memory-dir /tmp/den11_chk/users
```
Ожидание: БЕЗ Traceback (кракозябры допустимы — `errors='replace'`).

☐ **6.6** Обрыв ввода: в живом чате (`--mock --user probe --memory-dir /tmp/den11_chk/users`)
нажать Ctrl-D, затем повторить с Ctrl-C.
Ожидание: `[Выход] Ввод завершён (EOF)` / `[Выход] Прервано пользователем`, состояние сохранено.

☐ **6.7** Неизвестная команда: `/net-suchit` → `[Команда] Неизвестная команда: /net-suchit. См. /help`, чат жив.

☐ **6.8** Стрелки и редактирование ввода: в живом чате нажать ↑ — предыдущая команда
подставляется (readline). Ввести `/exit`. Убрать мусор: `rm -rf /tmp/den11_chk`.

---

## Часть 7. Комплектность, автотесты и финал (5 минут)

☐ **7.1** Проверить комплект:
```bash
ls -la den_11_Kod.py README_d11.md den_11_run.sh den_11_run.desktop Задание_Д11.txt START-PROMT_d11.md
ls core memory storage
```
Ожидание: все файлы на месте; `den_11_run.sh` с правами `x`; в `core/` — 4 модуля,
в `memory/` — 6, в `storage/` — 2.

☐ **7.2** Проверить `--help`:
```bash
./den_11_run.sh --help
```
Ожидание: 10 флагов — `--user`, `--deliver`, `--mock`, `--fresh`, `--log`, `--token-log`,
`--max-tokens`, `--price-in`, `--price-out`, `--memory-dir`.

☐ **7.3** L1 — сборка:
```bash
$PY -m py_compile den_11_Kod.py core/*.py memory/*.py storage/*.py && echo "L1 OK"
```

☐ **7.4** L2 — юнит-тесты (без живого ключа):
```bash
env -u API_KEY $PY den_11_dev/tests_debug/unit_runner.py
```
Ожидание: `Итого: 31 OK, 0 FAIL`.

☐ **7.5** L3 и L4 — смоук и сценарии:
```bash
API_KEY=test-key $PY den_11_dev/tests_debug/smoke.py
API_KEY=test-key $PY den_11_dev/tests_debug/scenario.py
```
Ожидание: `SMOKE OK: exit 0`; `SCENARIO OK: exit 0` (5 сценариев: интервью, маршрутизация,
дозированная доставка, влияние памяти, resume).

☐ **7.6** Гейт приёмки (идемпотентен, пишет только в `/tmp`):
```bash
API_KEY=test-key bash den_11_dev/tests_debug/check_acceptance.sh
```
Ожидание: `ИТОГ: 9 из 9 зелёные (FAIL=0)`, exit 0.

☐ **7.7** Проверить, что задание не тронуто и чек-лист на месте (п.19):
```bash
wc -l Задание_Д11.txt README_d11.md
test ! -e Проверка_Д11.md && echo "в корне дня чек-листа нет — ОК"
```
Ожидание: `Задание_Д11.txt` — 48 строк (текст задания + аддитивная пометка 11.0);
`README_d11.md` — 284 строки, есть раздел «Модель памяти»; в корне дня файла нет.

☐ **7.8** (Опционально) Ярлык: двойной клик по `den_11_run.desktop` в файловом менеджере —
открывается терминал с чатом. ⚠️ Поле `Exec=` содержит абсолютный путь текущего хоста —
после переноса репозитория ярлык нужно пересоздать.

☐ **7.9** Убрать артефакты проверки (если не нужны):
```bash
rm -rf users/checker11 users/mockuser /tmp/den11_chk __pycache__ core/__pycache__ memory/__pycache__ storage/__pycache__
rm -f Den_11_log.md den_11_tokens.csv
```
(Из `users/profiles.db` строки `checker11`/`mockuser` остаются — это данные БД, чистятся по желанию.)

---

## Итоговый чек-лист приёмки (сверка с заданием, 19 пунктов §7)

| № | Требование | Где проверено | Статус |
|---|---|---|---|
| 1 | Комплектность и сборка (`core/`, `memory/`, `storage/`, `.sh` +x) | 7.1, 7.3 | ☐ |
| 2 | 3 типа памяти + профиль — разные классы и файлы | Ч.2 (2.3–2.6), 7.4 | ☐ |
| 3 | Иерархия хранилища по канону куратора | Ч.2 (2.3–2.5), 7.4 | ☐ |
| 4 | Явная маршрутизация `remember(layer, …)` + лог «что куда» | Ч.2 (2.1) | ☐ |
| 5 | Дозированная доставка (`deliver` — параметр, урезанный режим) | Ч.3 (3.2, 3.4), Ч.5 (5.3) | ☐ |
| 6 | Идентификация на старте (`--user` / ввод до загрузки памяти) | Ч.1 (1.1–1.2) | ☐ |
| 7 | Интервью-инициализация нового ID + первая задача | Ч.1 (1.2–1.3), 6.1 | ☐ |
| 8 | Понятие таски + переход со ссылкой на сессию-источник | Ч.4 (4.1–4.6) | ☐ |
| 9 | Промт собирается явными блоками в каноническом порядке | Ч.2 (2.7), Ч.5 (5.2), 7.4 | ☐ |
| 10 | LLM за интерфейсом (ABC + RouterAI + Mock) | Ч.5 (5.1–5.2), 7.4 | ☐ |
| 11 | Задел state machine (4 стадии + переходы) | Ч.4 (4.7), 2.6 | ☐ |
| 12 | Resume «с того же места» | Ч.4 (4.8–4.9) | ☐ |
| 13 | Неизменяемые сообщения с `parent_id`; рабочая ≠ лог | Ч.2 (2.2, 2.6) | ☐ |
| 14 | Демонстрации задания (состав слоёв, влияние на ответы, интервью, resume) | Ч.2, Ч.3 (3.2), Ч.4, 7.5 | ☐ |
| 15 | Текстовое описание модели памяти | 7.7 (README, раздел «Модель памяти») | ☐ |
| 16 | Уроки Дня 10 перенесены (readline, `errors=replace`, retry 429, CSV, `BASE_DIR`) | Ч.3 (3.6), Ч.6 (6.5–6.8), 7.1 | ☐ |
| 17 | Тесты без живого ключа, артефакты — в `/tmp` | 7.4–7.6, Ч.6 | ☐ |
| 18 | Фильтр инвариантов НЕ реализуем (только задел `PolicyEngine`/`visibility`) | 7.4 (`test_state`/`test_memory`), README «Заделы» | ☐ |
| 19 | `Проверка_Д11.md` только в `den_11_dev/` | 7.7 | ☐ |

**Результат проверки:** ______ пройдено / 19 всего. Найденные проблемы:
(записать здесь; известные кандидаты — 3.3 «`/compare` оставляет урезанную доставку»,
4.9 «resume восстанавливает `tasks[0]`, а не последнюю активную задачу»,
7.8 «`Exec=` в `.desktop` привязан к хосту»)
