# Сценарий ручной проверки — День 12 «Персонализация ассистента (мультипрофиль + роутер)»

> Чек-лист для живой проверки проекта `Nedela_3/den_12` руками, без автотестов.
> Время прохождения: ~40–45 минут. **Живой ключ API не нужен** — весь сценарий идёт на
> `--mock` (заглушка `MockClient`); живые запросы к API платные (≈1–3 ₽) и запускаются
> только по отдельному решению.
> Отметки: ☐ — не выполнено, ✅ — прошло, ❌ — не прошло (записать, что именно).
> Артефакт процесса: файл живёт только в `dev/` (п.19). Машиночитаемый дубль —
> `dev/tests_debug/check_acceptance.sh` (Часть 7).
> ⚠️ Не запускать чекеры через `bash -x` — реальный `API_KEY` попадает в stdout.

## Подготовка (5 минут)

☐ **П.1** Открыть терминал в каталоге `Nedela_3/den_12`.

☐ **П.2** Убедиться, что venv на месте (ключ API — нет, он не нужен):
```bash
test -x ../../.venv/bin/python && echo "venv есть" || echo "venv НЕТ"
../../.venv/bin/python -V
```
Ожидание: «venv есть», Python 3.13 (или 3.10+).

☐ **П.3** Запомнить короткие пути (далее по тексту используются `$PY`, `$KOD`, `$TMP`):
```bash
PY=../../.venv/bin/python
KOD="$PWD"
TMP="$KOD/dev/tests_debug/.tmp/manual_12_$$"
mkdir -p "$TMP"
```
Все ручные прогоны пишут **только** в `$TMP` (через `--memory-dir`, `--log`, `--token-log`).
Рабочие `users/`, `Den_log.md`, `tokens.csv` в ходе проверки меняться не должны.

☐ **П.4** Зафиксировать состояние до проверки:
```bash
ls -A users/ ; wc -l < tokens.csv ; tail -3 Den_log.md
```
Ожидание: `users/` пуст; в `tokens.csv` одна строка (заголовок); в `Den_log.md` — строки
фаз разработки (не пользовательского рантайма). Это «нулевая точка» — сравним в 7.9.

☐ **П.5** Проверить, что задание не тронуто, а чек-листов в корне дня нет (п.19):
```bash
wc -l Задание_Д12.txt
ls Проверка*.md ./Проверка.md 2>&1 | tail -2
```
Ожидание: `Задание_Д12.txt` — 117 строк; во второй команде — `ls: не удалось получить
доступ` (в корне дня нет ни `Проверка.md`, ни `Проверка_Д12.md`). Если двойник
`Проверка_Д12.md` всё-таки вернулся из истории — он удаляется в 7.8 (нарушение п.19).

---

## Часть 1. Идентификация и интервью-инициализация (6 минут, без API)

☐ **1.1** Запустить нового пользователя в изолированное хранилище:
```bash
./run.sh --mock --user demo --memory-dir "$TMP/users" \
         --log "$TMP/log.md" --token-log "$TMP/tokens.csv"
```
Ожидание первой строки: `[Инициализация] Новый пользователь «demo» — проведу интервью.`
Порядок старта: сначала идентификация (`--user demo`), и только затем решение
«памяти нет → интервью».

☐ **1.2** Пройти интервью (4 вопроса — имя + три составляющие профиля):
```
Алексей
краткий, по делу
Python, без воды
Учусь делать агентов, цель — сдать неделю 3
```
Ожидание: `[Инициализация] Профиль и дерево users/demo/ созданы.`

☐ **1.3** Проверить шапку режима:
```
[Режим] Доставка слоёв: ['long_term', 'profile', 'short_term', 'working']
[Режим] MockClient (заглушка) — живых запросов к API не будет.
```
Приглашение ввода — `Вы [Основная_задача]: ` (задача видна в промпте).

☐ **1.4** Отправить два сообщения:
```
Меня зовут Алексей, мой стек Python
Привет
```
Ожидание: на первое — `Агент: [Mock] вижу 6 сообщений: system:Ты полезный ассистент…`,
на второе — `…вижу 8 сообщений…` (история краткосрочной памяти растёт). Без `[Ошибка]`
и без Traceback.

☐ **1.5** Ввести `/exit`.
Ожидание: `[Выход] Состояние сохранено. До встречи!`

☐ **1.6** Проверить, что создалось на диске (иерархия по канону + профили-зеркала):
```bash
find "$TMP/users" -type f | sort
```
Ожидание ровно эти файлы:
```
demo/long_term_memory.json
demo/profile.json
demo/profiles/default.json
demo/tasks/Основная_задача/sessions/<ГГГГММДД_ЧЧММСС>/session.json
demo/tasks/Основная_задача/sessions_resume.md
demo/tasks/Основная_задача/working_memory.json
profiles.db
```
`profile.json` — старое зеркало default (обратная совместимость с Днём 11),
`profiles/default.json` — новое зеркало по `profile_id`.

☐ **1.7** Проверить журнал маршрутизации:
```bash
cat "$TMP/log.md"
```
Ожидание в начале:
```
[Хранилище] profile «default» = JSON в SQLite (таблица profiles, user_id=demo)
[Память] profile «default» ← ['id', 'name', 'style', 'constraints', 'context'] → …/profiles/default.json (JSON в SQLite)
[Память] remember(profile) → profile.json ([…])
[Память] working ← ['description'] → …/tasks/Основная_задача/working_memory.json
[Память] long_term ← ['tasks'] → …/long_term_memory.json
[Агент] Пользователь demo инициализирован (интервью пройдено)
```
По логу видно **какой слой ← что → куда**; автосохранения «всего в один файл» нет.

---

## Часть 2. Явная маршрутизация 4 слоёв — без регрессии Дня 11 (6 минут, без API)

☐ **2.1** Проверить неизменяемость и цепочку `parent_id`:
```bash
$PY -c "import json,glob;p=sorted(glob.glob('$TMP/users/demo/tasks/*/sessions/*/session.json'))[-1];m=json.load(open(p))['messages'];print([(x['id'],x['parent_id'],x['role']) for x in m])"
```
Ожидание: 4 сообщения `M1…M4`, у `M1` parent — `None`, у каждого следующего parent = id
предыдущего; роли чередуются `user` / `assistant`.

☐ **2.2** Проверить, что профиль в долговременной памяти — **ссылка**, а не содержимое:
```bash
$PY -c "import json;d=json.load(open('$TMP/users/demo/long_term_memory.json'));print(sorted(d));print(d['profile_ref'], d['tasks'])"
```
Ожидание: ключи `decisions, knowledge, profile_ref, tasks`; `profile_ref = demo`;
тела профиля (style/constraints/context) внутри **нет**.

☐ **2.3** Проверить, что рабочая память — состояние, а не лог:
```bash
$PY -c "import json;d=json.load(open('$TMP/users/demo/tasks/Основная_задача/working_memory.json'));print(sorted(d));print(d['current_state'])"
```
Ожидание: ключи `description, refs, lifecycle_summary, decisions, constraints, facts,
open_questions, current_state`; стадия `planning`. **Сообщений диалога здесь нет.**

☐ **2.4** Проверить профиль как отдельную сущность в SQLite:
```bash
$PY -c "import sqlite3;print(sqlite3.connect('$TMP/users/profiles.db').execute('select user_id,profile_id,is_default from profiles').fetchall())"
```
Ожидание: `[('demo', 'default', 1)]` — первый профиль автоматически становится дефолтным.

☐ **2.5** Перезапустить чат (`./run.sh --mock --user demo --memory-dir "$TMP/users" --log "$TMP/log.md" --token-log "$TMP/tokens.csv"`)
и ввести `/memory`.
Ожидание: четыре блока в каноническом порядке `[profile] → [long_term] → [working] → [short_term]`.
Затем `/profile` — строки `[Профиль] активный: (default)` и dict профиля. `/exit`.

---

## Часть 3. Дозированная доставка и `/compare` — без регрессии (6 минут, без API)

☐ **3.1** В чате (запуск как в 2.5) выполнить `/compare`.
Ожидание: два ответа на жёстко зашитый вопрос «Какой стек я предпочитаю и почему?»:
```
[Сравнение] Влияние долговременной памяти на ответы:
  С long_term  (~N вх. токенов): [Mock] вижу K сообщений: …
  БЕЗ long_term (~M вх. токенов): [Mock] вижу K-1 сообщений: …
  (разница показывает, как слой долговременной памяти меняет ответ)
```
Во втором варианте из списка блоков **физически исчез** `system:[long_term]` — это
доказательство одновременно дозированной доставки (п.5) и влияния памяти (п.14).
Числа токенов могут отличаться — важно соотношение `М < N` и состав блоков.

☐ **3.2** ⚠️ Зафиксировать побочный эффект: после `/compare` режим доставки **не
восстанавливается**. Ввести `/memory`, затем сообщение `Что помнишь?` и проверить лог:
```bash
grep "доставка" "$TMP/log.md" | tail -2
```
Ожидание: `[Агент] M2: ответ получен (доставка: ['profile', 'short_term', 'working'])` —
`long_term` потерян. Записать как недочёт (в чек-листе — заметка, не падение).
Вернуть вручную: `/deliver profile,long_term,working,short_term`
Ожидание: `[Доставка] Теперь: ['long_term', 'profile', 'short_term', 'working']`.

☐ **3.3** Отключить слой и убедиться, что он уходит из запроса:
```
/deliver profile
Привет
```
Ожидание: в Mock-ответе остались только роль, `system:[profile]` и текущий запрос —
блоков `[long_term]`, `[working]` и истории краткосрочной памяти нет.

☐ **3.4** Проверить отказные случаи доставки:
- `/deliver` без аргументов → `[Доставка] Укажите слои: /deliver profile,working`;
- `/deliver foo,bar` → `[Доставка] Теперь: []` (мусор отброшен, чат жив).

☐ **3.5** Токен-учёт (п.16): `/tokens`, затем `/cost`.
Ожидание: `[Токены] Обменов: N | Всего вх. оценка: T | Последний запрос: см. CSV tokens.csv`
и `[Стоимость] Оценка сессии (вход): 0.000XXX ₽ (вход 11.0 ₽/1M, выход 33.0 ₽/1M)`.
Выйти `/exit` и проверить журнал:
```bash
tail -3 "$TMP/tokens.csv"
```
Ожидание: заголовок `exchange,timestamp,prompt_tokens,cost_rubles,cost_total_rubles`
и строки с монотонно растущим `cost_total_rubles`.

---

## Часть 4. Задачи, resume, state machine — без регрессии (6 минут, без API)

☐ **4.1** В чате (запуск как в 2.5) ввести `/tasks`.
Ожидание: `[Задачи] Всего: 1; активная: Основная_задача` и строка `* Основная_задача`.

☐ **4.2** Создать/переключить задачу с пробелом в имени:
```
/task Подбор косметики
```
Ожидание: `[Задача] Переключился на «Подбор косметики»`, приглашение стало
`Вы [Подбор косметики]: `.

☐ **4.3** Отправить в новой задаче одно сообщение, затем `/tasks`.
Ожидание: `Всего: 2; активная: Подбор косметики`, звёздочка у активной.
Проверить корректное имя каталога (`safe_name`) и ссылку на сессию-источник:
```bash
find "$TMP/users/demo/tasks" -maxdepth 1 -type d | sort
$PY -c "import json;print(json.load(open('$TMP/users/demo/long_term_memory.json'))['tasks'])"
```
Ожидание: каталог `Подбор_косметики` (пробел → подчёркивание); в `long_term.tasks` две
задачи, у второй есть `source_session` с id сессии-источника.

☐ **4.4** Проверить отметку перехода в СТАРОЙ задаче:
```bash
$PY -c "import json;print(json.load(open('$TMP/users/demo/tasks/Основная_задача/working_memory.json'))['open_questions'])"
```
Ожидание: элемент вида `Переход на задачу «Подбор косметики» (сессия <id>)`.

☐ **4.5** Ввести `/state`.
Ожидание — четыре стадии и матрица переходов (задел, п.11):
```
planning → execution
execution → validation, planning
validation → execution, planning
done → —
```
⚠️ Порядок целей у `execution`/`validation` не гарантирован — это `set`, а не список.

☐ **4.6** Ввести `/exit`, затем перезапустить без `--fresh`.
Ожидание: интервью НЕ повторяется, сразу
`[Память] Пользователь «demo» найден, контекст загружен. Задача: …`;
создаётся **новая** сессия (новый `<sid>`), старая цела. Проверить:
```bash
/summary
```
Ожидание: `[Саммари]` + `# Резюме сессий задачи «…»` и строка `Последняя сессия: <id>`.

☐ **4.7** ⚠️ Зафиксировать, какая задача восстановилась в 4.6. `load_state()` берёт
`tasks[0]`, поэтому ожидается `Основная_задача`, а **не** последняя активная
`Подбор косметики`. Записать как недочёт «активная задача не сохраняется» (заметка, не падение).

☐ **4.8** Отказные случаи: `/task` без имени → `[Задача] Укажите имя: /task <имя>`;
`/zzz` → `[Команда] Неизвестная команда: /zzz. См. /help`; Ctrl-D →
`[Выход] Ввод завершён (EOF)` (состояние сохранено).

---

## Часть 5. Персонализация: несколько профилей-«призм» (8 минут, без API) — кульминация дня

☐ **5.1** Запустить пользователя с двумя профилями (все ответы по stdin, чтобы
исключить ручной ввод):
```bash
printf '%s\n' \
 'Анна' 'кратко' 'нет' 'лаборатория' \
 '/profile new chemist' 'Химик' 'химия' 'химия,реактив' 'строго' 'не выдумывать' 'посты' \
 '/profile new lab' 'Лаборант' 'лаборатория' 'реактив,колба' 'по шагам' '—' 'учёт' \
 '/profile list' '/exit' | \
 ./run.sh --mock --user anna --memory-dir "$TMP/r5/users" \
          --log "$TMP/r5/log.md" --token-log "$TMP/r5/tokens.csv"
```
Ожидание на каждый `/profile new …`:
```
[Профиль] Создание профиля «chemist» (мини-интервью).
  Имя профиля:   Домен (область запросов):   Триггеры (через запятую):   Стиль ответов:   Ограничения:   Контекст: [Профиль] Профиль «chemist» создан и активирован.
```
(подсказки идут слитно, потому что stdin не терминал — это нормально.)

☐ **5.2** Проверить список профилей (сортировка по `profile_id`, пометки `*`/`>`):
Ожидание:
```
[Профили]
     chemist
  *  default
   > lab
  (* — default, > — активный)
```
Все три профиля в алфавитном порядке; `default` остаётся дефолтным (`*`), активным (`>`)
стал последний созданный профиль (`lab`). Столбцы ровно в две позиции: `*`/`>` пишутся
в своей колонке, поэтому у неактивного и недефолтного профиля обе позиции пустые.

☐ **5.3** Проверить, что профили легли и в SQLite, и в зеркала:
```bash
$PY -c "import sqlite3;print(sqlite3.connect('$TMP/r5/users/profiles.db').execute('select profile_id,is_default from profiles order by profile_id').fetchall())"
find "$TMP/r5/users/anna/profiles" -type f | sort
```
Ожидание: `[('chemist', 0), ('default', 1), ('lab', 0)]`; файлы
`chemist.json`, `default.json`, `lab.json`. `profile.json` по-прежнему зеркало **default**
(авторитет — SQLite, зеркала — fallback и читаемость).

☐ **5.4** Проверить модель профиля:
```
/profile show lab
```
Ожидание JSON с ключами `id, profile_id, name, domain, triggers, style, constraints,
context, skills`. Поле `skills` — `[]` (задел: скиллы пока только декларативно
подмешиваются в промт, исполнения нет).

☐ **5.5** Проверить, что профиль реально меняет промт (одна и та же реплика при двух
активных профилях):
```bash
printf '%s\n' 'Составь план' '/profile use chemist' 'Составь план' '/exit' | \
  ./run.sh --mock --user anna --memory-dir "$TMP/r5/users" \
           --log "$TMP/r5/log.md" --token-log "$TMP/r5/tokens.csv" | grep -o "Профиль: .*"
```
Ожидание: `[Профиль] Активный профиль: chemist`, а в Mock-блоках — **разные заголовки
активного профиля**: `Профиль: Анна (default)` в первом ответе и `Профиль: Химик (chemist)`
во втором (плюс строки `Имя:` / `Стиль:` того же профиля).
Это и есть персонализация: **один user_id, разные «призмы»**.
⚠️ Строку `Домен: …` в Mock-выводе вы, скорее всего, **не увидите**: `MockClient`
обрезает содержимое каждого сообщения до 80 символов, а `domain` добавляется в блок
последней строкой. Полностью домен/триггеры видны в `/profile show <id>` (5.4).


☐ **5.6** Отказные случаи семейства `/profile`:
- `/profile use ghost` → `[Профиль] «ghost» не существует (см. /profile list)`;
- `/profile show ghost` → `[Профиль] «ghost» не найден.`;
- `/profile xyz` → `[Профиль] Неизвестная подкоманда: xyz. См. /help`;
- `/profile` (голо), `/profile list`, `/profile show`, `/profile use`, `/profile new`
  без id → подсказка `Укажите id: …`.

☐ **5.7** Флаг `--profile` (активный профиль на старте):
```bash
./run.sh --mock --user anna --profile chemist --memory-dir "$TMP/r5/users" \
         --log "$TMP/r5/log.md" --token-log "$TMP/r5/tokens.csv" </dev/null | head -3
./run.sh --mock --user anna --profile ghost --memory-dir "$TMP/r5/users" \
         --log "$TMP/r5/log.md" --token-log "$TMP/r5/tokens.csv" </dev/null | head -3
```
Ожидание: `[Профиль] Активный профиль на старте: chemist` и
`[Профиль] ⚠ Профиль «ghost» не найден — использую default.` (падения нет).

---

## Часть 6. Профиль-роутер: авто-выбор по запросу (7 минут, без API)

☐ **6.1** Проверить разбор без переключения (`/profile route <текст>` — только объясняет):
```
/profile route Химия: какой реактив взять
```
Ожидание:
```
[Роутер] разбор запроса: «Химия: какой реактив взять»
  chemist: счёт 5 (триггеры: химия, реактив; домен: химия)
  default: счёт 0 (триггеры: —; домен: —)
  lab: счёт 2 (триггеры: реактив, колба; домен: лаборатория)
  решение → профиль «chemist»
```
Модель счёта: **+2** за каждый совпавший триггер (подстрока, без учёта регистра),
**+1** за `domain`. Здесь: 2 триггера (4) + домен (1) = 5.

☐ **6.2** Проверить, что `route` **не** переключает активный профиль:
```
/profile list
```
Ожидание: пометка `>` осталась у прежнего активного профиля, не у `chemist`.

☐ **6.3** Проверить ничью и ноль:
```
/profile route Реактив закончился
/profile route Расскажи про отпуск
```
Ожидание в обоих случаях: `решение → без переключения (ничья или ноль; остаёмся на текущем/дефолтном)`.
В журнале `$TMP/r5/log.md` при этом:
```
[Роутер] ничья ['chemist', 'lab'] (счёт 2) → остаёмся на текущем
[Роутер] без совпадений → default
```

☐ **6.4** Включить авто-роутинг и проверить переключение ДО сборки промта
(активным сейчас остаётся `default` — `route` из 6.1–6.3 переключать не должен):
```
/profile auto on
Химия: какой реактив взять
/profile list
```
Ожидание: `[Роутер] Авто-роутинг включён (профиль выбирается по каждому запросу).`,
в журнале `$TMP/r5/log.md` — две строки:
`[Роутер] запрос → профиль chemist (счёт 5)` и `[Агент] активный профиль: chemist`
(последняя пишется **только** в журнал, в терминал она не выводится);
в Mock-ответе блок начинается с `Профиль: Химик (chemist)`, а `/profile list` показывает
`> chemist`. Терминал при этом печатает обычный `Агент: [Mock] …` — переключение беззвучное.

☐ **6.5** Проверить управление флагом и отказной случай:
- `/profile auto` → `[Роутер] Авто-роутинг: on (управление: /profile auto on|off)` —
  команда **показывает текущее состояние**, а не включает его (после 6.4 — `on`);
- `/profile auto off` → `[Роутер] Авто-роутинг выключен.`; затем `/profile auto` → `off`;
- `/profile route` без текста → `[Роутер] Укажите текст: /profile route <текст>`.

☐ **6.6** ⚠️ Зафиксировать поведение при `--deliver`, урезанном `/compare` (см. 3.2): если
перед авто-роутингом доставка урезана и `profile` исключён из набора, переключение
**произойдёт**, но в промт блок `[profile]` не попадёт. Проверить:
```
/compare
/deliver working,short_term
/profile auto on
Химия: какой реактив взять
```
Ожидание: в логе `[Роутер] запрос → профиль chemist …`, но в Mock-выводе строки
`system:[profile]` нет. Вывод: роутер и доставка — независимые механизмы.
Вернуть: `/deliver profile,long_term,working,short_term`.

---

## Часть 7. Миграция схемы, обратная совместимость, автотесты и финал (8 минут)

☐ **7.1** Сымитировать БД **старой** схемы (День 11: `user_id PRIMARY KEY`, один профиль)
и проверить авто-миграцию при открытии:
```bash
LEG="$TMP/leg"; mkdir -p "$LEG/users"
$PY -c "
import sqlite3, json, os
c = sqlite3.connect(os.path.abspath('$LEG/users/profiles.db'))
c.execute('CREATE TABLE profiles (user_id TEXT PRIMARY KEY, profile_json TEXT NOT NULL, updated_at TEXT NOT NULL)')
c.execute('INSERT INTO profiles VALUES (?,?,?)', ('alice', json.dumps({'id':'alice','name':'Алиса','style':{'answers':'кратко'}}, ensure_ascii=False), '2026-09-20 10:00:00'))
c.commit(); c.close()"
printf '%s\n' '/profile list' '/profile show default' | \
  ./run.sh --mock --user alice --memory-dir "$LEG/users" \
           --log "$LEG/log.md" --token-log "$LEG/tokens.csv"
```
Ожидание: интервью НЕ запрошено (`[Память] Пользователь «alice» найден…`),
`/profile list` → `  *> default`, в `$LEG/log.md` строка
`[Хранилище] миграция старой схемы profiles → (user_id, profile_id): 1 строк`
(⚠️ «1 строк» — реальный текст лога, не опечатка чек-листа).

☐ **7.2** Проверить, что схема стала новой, а данные не потеряны:
```bash
$PY -c "import sqlite3;c=sqlite3.connect('$LEG/users/profiles.db');print([r[1] for r in c.execute('PRAGMA table_info(profiles)')]);print(c.execute('select user_id,profile_id,is_default from profiles').fetchall())"
```
Ожидание: колонки `['user_id', 'profile_id', 'profile_json', 'is_default', 'updated_at']`,
PK `(user_id, profile_id)`, строка `[('alice', 'default', 1)]`; в JSON дописан
`profile_id: default`; старые поля (`name`, `style`) целы; таблицы `profiles_legacy` больше нет.

☐ **7.3** Обратная совместимость без SQLite (зеркало JSON). Убираем авторитет — БД:
```bash
mv "$LEG/users/profiles.db" "$LEG/profiles.db.bak"
printf '%s\n' 'Алиса' 'кратко' 'нет' 'обновление' '/profile' '/profile list' '/exit' | \
  ./run.sh --mock --user alice --memory-dir "$LEG/users" \
           --log "$LEG/log.md" --token-log "$LEG/tokens.csv"
mv "$LEG/profiles.db.bak" "$LEG/users/profiles.db"
```
Ожидание: интервью запрашивается **заново** (`[Инициализация] Новый пользователь «alice»
— проведу интервью.`) — потому что `exists()` смотрит в SQLite, а её нет; после ответов —
`/profile` → `[Профиль] активный: (default)`, `/profile list` → `  *> default`, Traceback нет,
БД пересоздана (base вернётся на место командой выше). Зеркало `profile.json` обновится
новыми ответами интервью. Отдельно: если БД нет, но профиль запрашивается **без** интервью
(старый пользователь с зеркалом), слой читает его как fallback — это проверяет автотест
`test_backward_compatibility` (7.6).

☐ **7.4** ⚠️ Зафиксировать найденный при подготовке сценария недочёт: интервью
(`run_interview`) при неинтерактивном вводе (`</dev/null`) даёт Traceback `EOFError` —
`input()` не обёрнут в `try`. Срабатывает при `--fresh`, при потерянной БД нового
пользователя, и когда в stdin меньше 4 строк. В интерактивном терминале не проявляется.
Записать как известную шероховатость, не как падение проверки.


☐ **7.5** Машинный контур — L1 (сборка):
```bash
$PY -m py_compile Kod.py core/*.py memory/*.py storage/*.py && echo "L1 OK"
```

☐ **7.6** L2 (юниты, без живого ключа) — полный прогон и только персонализация:
```bash
env -u API_KEY $PY dev/tests_debug/unit_runner.py
env -u API_KEY $PY dev/tests_debug/unit_runner.py test_person
```
Ожидание: `Итого: 40 OK, 0 FAIL` и `Итого: 9 OK, 0 FAIL`.
⚠️ Фильтр `unit_runner.py` принимает **имя файла** (`test_person`), а не имя модуля
(`person` / `memory`) — с ним без `test_` префикса не найдётся ни одного теста.
В `test_person.py` закрыты: `test_multi_profile_roundtrip`, `test_migration_old_schema`,
`test_backward_compatibility`, `test_router_*`, `test_profile_block_differs`,
`test_agent_switch_profile`, `test_agent_auto_route_switches_before_prompt`,
`test_two_profiles_different_answers`.

☐ **7.7** L3, L4 и гейт приёмки (гейт идемпотентен, пишет только в `dev/tests_debug/.tmp/`):
```bash
API_KEY=test-key $PY dev/tests_debug/smoke.py
API_KEY=test-key $PY dev/tests_debug/scenario.py
API_KEY=test-key bash dev/tests_debug/check_acceptance.sh
```
Ожидание: `SMOKE OK: exit 0`; `SCENARIO OK: exit 0` и **6** строк `[scenario] … OK`
(интервью, маршрутизация, дозированная доставка, влияние памяти, resume,
**персонализация (профили + роутер)**); `ИТОГ: 9 из 9 зелёные (FAIL=0)`, exit 0.
⚠️ Не запускать гейт с `bash -x`.

☐ **7.8** Пункт 19 — чек-лист живёт **только** в `dev/`. Удалить двойник из корня дня:
```bash
rm -f Проверка_Д12.md ./Проверка.md
test ! -e ./Проверка.md && test ! -e ./Проверка_Д12.md && test -e dev/Проверка.md \
  && echo "п.19 ОК: чек-лист только в dev/"
grep -n "dev/Проверка.md" README.md | head -3
```
Ожидание: «п.19 ОК: чек-лист только в dev/»; README ссылается на `dev/Проверка.md`
(строки ~322, 354, 409) и ни на какой файл в корне дня.

☐ **7.9** Комплектность и отсутствие загрязнения рантайма:
```bash
ls -la Kod.py README.md run.sh run.desktop Задание_Д12.txt
ls core memory storage
./run.sh --help | grep -c '^  --'
ls -A users/ ; wc -l < tokens.csv
```
Ожидание: `run.sh` с правами `x`; в `core/` — 5 модулей (`agent`, `llm_client`,
`profile_router`, `prompt_builder`, `state_machine`), в `memory/` — 6, в `storage/` — 2;
**11** флагов (`--user --profile --deliver --mock --fresh --log --token-log
--max-tokens --price-in --price-out --memory-dir`); рабочий `users/` пуст,
`tokens.csv` — как в П.4 (1 строка).

☐ **7.10** (Опционально) Ярлык: двойной клик по `run.desktop` открывает терминал с чатом.
⚠️ Поля `Exec=`/`Path=` содержат абсолютный путь текущего хоста — после переноса
репозитория ярлык нужно пересоздать.

☐ **7.11** Убрать артефакты проверки:
```bash
rm -rf "$TMP"
find . -name __pycache__ -type d -prune -exec rm -rf {} +
git status --porcelain
```
Ожидание: `git status` показывает изменёнными только `dev/Проверка.md` (и удаление
`Проверка_Д12.md`, если он был отслежен). `users/`, `Den_log.md`, `tokens.csv` — не изменены.

---

## Итоговый чек-лист приёмки (сверка с заданием, 22 критерия)

Пункты 1–19 — День 11 **без регрессии**; 20–22 — новое Дня 12.

| № | Требование | Где проверено | Статус |
|---|---|---|---|
| 1 | Комплектность и сборка (`core/`, `memory/`, `storage/`, `run.sh` +x) | 7.5, 7.9 | ☐ |
| 2 | 3 типа памяти + профиль — разные классы и файлы | Ч.1 (1.6–1.7), Ч.2, 7.6 | ☐ |
| 3 | Иерархия хранилища по канону куратора | 1.6, 2.2–2.4, 7.6 (`test_storage`) | ☐ |
| 4 | Явная маршрутизация `remember(layer, …)` + лог «что куда» | 1.7, 2.5 | ☐ |
| 5 | Дозированная доставка (`deliver` — параметр, урезанный режим) | 3.1, 3.3–3.4, 6.6, 7.6 | ☐ |
| 6 | Идентификация на старте (`--user` / ввод до загрузки памяти) | 1.1, 4.6, 7.1 | ☐ |
| 7 | Интервью-инициализация нового ID + первая задача | 1.2–1.3, 7.6 (`test_agent`), 7.7 | ☐ |
| 8 | Понятие таски + переход со ссылкой на сессию-источник | Ч.4 (4.1–4.4) | ☐ |
| 9 | Промт собирается явными блоками в каноническом порядке | 2.5, 3.1, 3.3, 7.6 (`test_prompt`) | ☐ |
| 10 | LLM за интерфейсом (ABC + RouterAI + Mock) | 1.4, 5.5, 7.6 (`test_llm`) | ☐ |
| 11 | Задел state machine (4 стадии + переходы) | 4.5, 2.3, 7.6 (`test_state`) | ☐ |
| 12 | Resume «с того же места» | 4.6–4.7, 7.7 (`scenario_resume`) | ☐ |
| 13 | Неизменяемые сообщения с `parent_id`; рабочая ≠ лог | 2.1, 2.3 | ☐ |
| 14 | Демонстрации задания (состав слоёв, влияние на ответы, интервью, resume) | Ч.3 (3.1), Ч.4, 7.7 | ☐ |
| 15 | Текстовое описание модели памяти | 7.8–7.9 (README, раздел «Модель памяти») | ☐ |
| 16 | Уроки Дня 10 перенесены (readline, `errors=replace`, retry 429, CSV, `BASE_DIR`) | 3.5, 4.8, 7.9 | ☐ |
| 17 | Тесты без живого ключа, артефакты — во временном каталоге | 7.6–7.7, весь сценарий на `--memory-dir` | ☐ |
| 18 | Фильтр инвариантов НЕ реализуем (только задел `PolicyEngine`/`visibility`) | 7.6, README «Заделы» | ☐ |
| 19 | `Проверка.md` только в `dev/` (в корне дня чек-листов нет) | 7.8 | ☐ |
| 20 | Несколько профилей-«призм» на одного пользователя | Ч.5 (5.1–5.7), 7.6 (`test_multi_profile_roundtrip`, `test_profile_block_differs`) | ☐ |
| 21 | Миграция старой схемы и обратная совместимость | 7.1–7.3, 7.6 (`test_migration_old_schema`, `test_backward_compatibility`), без регрессии 1–19 | ☐ |
| 22 | Профиль-роутер и авто-выбор профиля по запросу | Ч.6 (6.1–6.6), 7.6 (`test_router_*`, `test_agent_auto_route_switches_before_prompt`), 7.7 (`scenario_personalization`) | ☐ |

**Результат проверки:** ______ пройдено / 22 всего. Найденные проблемы:
(записать здесь; известные кандидаты, уже подтверждённые чтением/прогонами —
3.2 «`/compare` оставляет урезанную доставку»,
4.7 «resume восстанавливает `tasks[0]`, а не последнюю активную задачу»,
7.2 «в логе миграции — «1 строк» без склонения»,
7.4 «`--fresh` при неинтерактивном вводе → Traceback `EOFError`»,
7.10 «`Exec=` в `.desktop` привязан к хосту»,
5.4 «`skills[]` исполняются только как текст в промте»,
6.6 «роутер не учитывает `deliver`»,
а также заделы: `budget` в `PromptBuilder.build()` из CLI не передаётся, блок `summary`
недостижим из `--deliver` (нет в `LAYER_ORDER`), `next_state()` FSM не вызывается,
`Agent.identify()` не вызывается (логика в `main()`), `dev/tests_debug/scenario/scen_1.md`
описывает ещё День 11.)
