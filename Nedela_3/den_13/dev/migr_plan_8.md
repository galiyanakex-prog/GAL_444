# migr_plan_8.md — Рабочий план. Этап 8: Финал и приёмка

> **Тип файла: РАБОЧИЙ ПЛАН** (создан из план-эталона `dev/migr_plan.md`, раздел «Этап 8»).
> Миграция выполняется **по этому файлу**, подшаг за подшагом. Это **последний этап**.
> **Автономный режим: включён по умолчанию** — действия считаются заранее одобренными;
> живые LLM-вызовы с реальным ключом — только с явного согласия пользователя.
>
> **Цель этапа:** полная приёмка миграции (регресс + новое), отчёты dev-пространства,
> **запись о завершении миграции в `dev/migr_log.md`** и **финальная корректировка
> `Nedela_3/den_13/README.md`** по итоговому состоянию проекта.
>
> **Источники истины:** `$ARCH` §4.3 (критерии 23–25), `Задание_Д13.txt`, `dev/migr_log.md`.

---

## Входные условия этапа

- [ ] Этап 7 завершён: в `dev/migr_log.md` есть запись «Этап 7 … ✅».
- [ ] Документация (README, Проверка, FINAL_REV, ПРОМТ_fsm) обновлена.
- [ ] Переменные: `$KOD`, `$DEV`, `$TST`, `$PY`, `$ARCH` — как в эталоне §0.5 п. 8.

## Правила этапа (наследуются из эталона §0.5 и §0.3)

1. Один подшаг = один логический блок; после правки — проверка → запись в `migr_log.md`.
2. Retry-цикл: при неуспехе — фиксация ❌ (+ карточка в `dev/logs_reports/errors/`),
   минимальная правка, повтор до ✅.
3. Полный прогон — без живого ключа; временные файлы — только `$TST/.tmp/`.
4. **Ничего не коммитить** без прямой команды пользователя (8.5 — только предложение
   сообщения коммита).
5. Порядок 8.3 → 8.4 обязателен: сначала запись о завершении миграции в журнале, затем
   корректировка README (п. 7–8 алгоритма цепочки).

---

## Подшаги этапа

### Подшаг 8.1 — Полный прогон (регресс + новое)
- Цель: вся пирамида без живого ключа: `py_compile` всей сборки → `unit_runner.py`
  (8 модулей: прежние 40 тестов + новые fsm) → `smoke.py` → `scenario.py` (7) →
  `check_acceptance.sh` (10/10).
- Прочитать: (ничего).
- Изменить/создать: (ничего; при падениях — карточки в `dev/logs_reports/errors/`).
- Проверка: все команды EXIT 0.
- Запись в журнал: 8.1.
- Объём: ~0 чтения, ~0–30 строк правки (только фиксы падений).
- Кандидат на батч: нет.

### Подшаг 8.2 — Отчёты: `stage_F_state.md`, `run_log.md`, `final_report.md`
- Цель: этапный отчёт «было (задел Enum+next_state) → стало (полный автомат) → проверка →
  статус»; сводный журнал прогонов (команда → результат → EXIT); итоговый отчёт «что
  сделано / что проверено / что осталось спорным» (спорное: validator-заглушка, уровень 2
  изоляции стадий — заделы следующих дней).
- Прочитать: `dev/migr_log.md` (записи Этапов 0–7).
- Изменить/создать: три файла в `dev/logs_reports/`.
- Проверка: все подшаги 0.1–8.1 отражены; незакрытых ⚠ нет (или перечислены в «спорном»).
- Запись в журнал: 8.2.
- Объём: ~100 строк чтения, ~120 строк правки.
- Кандидат на батч: да (с 8.3).

### Подшаг 8.3 — Запись о завершении миграции в `migr_log.md`
- Цель: итоговая запись по формату эталона §0.4 («МИГРАЦИЯ ЗАВЕРШЕНА»): этапы 0–8 ✅,
  приёмка (22 прежних критерия + 23–25), гейт 10/10; строка «README.md: корректируется
  следующим пунктом (8.4)».
- Прочитать: `migr_log.md` (все записи), `final_report.md`.
- Изменить/создать: `dev/migr_log.md` — финальная запись.
- Проверка: запись существует, все этапы 0–8 перечислены со статусом ✅.
- Запись в журнал: 8.3 (финальная запись миграции).
- Объём: ~80 строк чтения, ~15 строк правки.
- Кандидат на батч: да (с 8.2).

### Подшаг 8.4 — Финальная корректировка `README.md` по итоговому состоянию
- Цель: сверить README с фактом: дерево проекта (строки/секции), таблица команд (все формы),
  флаги, число тестов/сценариев/критериев приёмки, раздел «Состояние задачи», примеры
  запуска; убрать расхождения, оставшиеся после Этапов 1–7. По завершении — обновить
  строку финальной записи в `migr_log.md` на «README.md: скорректирован по финальному
  состоянию».
- Прочитать: `README.md` (весь), фактическое дерево (`find $KOD -maxdepth 2`), `final_report.md`.
- Изменить/создать: `README.md` (+ одна строка в `dev/migr_log.md`).
- Проверка: каждое утверждение README подтверждается файлом/командой; расхождений нет.
- Запись в журнал: 8.4.
- Объём: ~420 строк чтения, ~80 строк правки.
- Кандидат на батч: нет.

### Подшаг 8.5 — Финальная сводка пользователю
- Цель: дерево готового дня, результаты приёмки (22 прежних + критерии 23–25), сводка
  «что сделано / что проверено / что осталось спорным», предложение сообщения коммита
  (**не коммитить** без прямой команды).
- Прочитать: `final_report.md`, `migr_log.md`.
- Изменить/создать: (ничего — ответ в чате).
- Проверка: чек-лист `$ARCH` §4.3 закрыт по всем трём пунктам (23, 24, 25).
- Запись в журнал: 8.5.
- Объём: ~20 строк чтения, ~0 строк правки.
- Кандидат на батч: да (с 8.4 — после проверки README).

---

## Чек-лист приёмки этапа (= приёмка всей миграции)

- [ ] `py_compile` всей сборки — OK.
- [ ] L2: `unit_runner.py` — 8 модулей, все тесты зелёные.
- [ ] L3: `smoke.py` — EXIT 0.
- [ ] L4: `scenario.py` — 7 сценариев, EXIT 0.
- [ ] Гейт: `check_acceptance.sh` — 10 из 10.
- [ ] Критерий 23 (`$ARCH` §4.3): `/state` показывает этап, текущий шаг и ожидаемое действие.
- [ ] Критерий 24: пауза на planning/execution/validation; после resume — продолжение с того
      же шага без повторных объяснений (включая перезапуск процесса).
- [ ] Критерий 25: `task_state.json` на диске; завершённые шаги не повторяются; переходы
      только разрешённые.
- [ ] Регресс: прежние 22 критерия приёмки зелёные.
- [ ] `dev/migr_log.md` содержит запись «МИГРАЦИЯ ЗАВЕРШЕНА» и строку о корректировке README.
- [ ] `README.md` соответствует финальному состоянию проекта.
- [ ] Отчёты `stage_F_state.md`, `run_log.md`, `final_report.md` заполнены.

## Последний пункт алгоритма этапа (выполняется только после ✅ приёмки)

1. Дописать в `dev/migr_log.md` подтверждение закрытия этапа 8 (все пункты чек-листа ✅).
2. **Миграция завершена — следующего этапа нет.** Дальнейшие действия — только по прямой
   команде пользователя (коммит, правки, переход к Дню 14).
