# migr_plan_2.md — Рабочий план. Этап 2: Хранилище — `storage/store.py` (task_state.json)

> **Тип файла: РАБОЧИЙ ПЛАН** (создан из план-эталона `dev/migr_plan.md`, раздел «Этап 2»).
> Миграция выполняется **по этому файлу**, подшаг за подшагом.
> **Автономный режим: включён по умолчанию** — действия считаются заранее одобренными;
> живые LLM-вызовы с реальным ключом — только с явного согласия пользователя.
>
> **Цель этапа:** научить фасад хранилища `Store` работать с персистентным снимком состояния
> задачи `users/<id>/tasks/<task>/task_state.json` (`arch_den_13.md` §2.5–2.6). Store —
> единственный, кто знает пути; агент файлов не касается напрямую (инкапсуляция через фасад).
>
> **Источники истины:** `Задание_Д13.txt` (блоки save/load) → `$ARCH` §2.5–2.6.

---

## Входные условия этапа

- [ ] Этап 1 завершён: в `dev/migr_log.md` есть запись «Этап 1 … ✅».
- [ ] `core/state_machine.py` содержит `TaskStage`, `TaskState`, `save_state`/`load_state`.
- [ ] Переменные: `$KOD`, `$TST`, `$PY`, `$ARCH` — как в эталоне §0.5 п. 8.

## Правила этапа (наследуются из эталона §0.5 и §0.3)

1. Один подшаг = одна секция одного файла; после правки — `py_compile` + smoke → запись в `migr_log.md`.
2. Retry-цикл: при неуспехе — фиксация ❌, минимальная правка, повтор до ✅.
3. Smoke-файлы — только в `$TST/.tmp/`; не коммитить.
4. **Регресс-запрет:** существующие методы `Store` не менять (7 тестов `test_storage.py`
   обязаны остаться зелёными); только добавление новой секции.

---

## Подшаги этапа

### Подшаг 2.1 — Методы `task_state_*`
- Цель: фасад знает путь состояния задачи (§2.5): `task_state_path(user_id, task_name)` →
  `users/<id>/tasks/<task>/task_state.json`; `read_task_state(...) -> dict | None`
  (отсутствует/битый → None); `write_task_state(..., data) -> str` (через `write_json`
  с `ensure_task`).
- Прочитать: `storage/store.py` (секция «Рабочая память» — образец стиля), `$ARCH` §2.5–2.6.
- Изменить/создать: `storage/store.py` — новая секция «Состояние задачи (state machine)»
  после секции рабочей памяти; docstring-дерево в шапке модуля дополнить `task_state.json`.
- Проверка: `py_compile`; smoke: write→read roundtrip в `--memory-dir $TST/.tmp/store_fsm`.
- Запись в журнал: 2.1.
- Объём: ~60 строк чтения, ~30 строк правки.
- Кандидат на батч: да (с 3.1 Этапа 3 — разные файлы, независимы).

---

## Чек-лист приёмки этапа

- [ ] `py_compile storage/store.py` — OK.
- [ ] `task_state_path()` возвращает путь внутри `tasks/<task>/` (через `safe_name`).
- [ ] `read_task_state()` на отсутствующем/битом файле возвращает `None`, не падает.
- [ ] `write_task_state()` создаёт каталог задачи (`ensure_task`) и возвращает путь.
- [ ] roundtrip write→read сохраняет все поля (включая `previous_stage`, `error`).
- [ ] `unit_runner.py`: модуль `test_storage.py` (7 тестов) зелёный — регрессии нет.

## Последний пункт алгоритма этапа (выполняется только после ✅ приёмки)

1. Записать в `dev/migr_log.md` блок «## Этап 2 — Хранилище (`storage/store.py`)»
   по формату эталона §0.4.
2. **Перейти к выполнению следующего этапа: открыть `dev/migr_plan_3.md` и начать с его
   подшага 3.1.** (Если этап не принят — повторять подшаги/правки до успеха.)
