# ПРОМТ_readme — текстовый результат дня: README + Проверка.md (Этап 10)

## 1. Цель и граница
Подготовить текстовую часть результата (формат «Код / Текст»): `README.md` с
текстовым описанием модели памяти (что хранится в каждом типе и ПОЧЕМУ) и
`dev/Проверка.md` — человеческую версию чек-листа приёмки.

Что НЕ делаем: не пишем код; `Проверка.md` живёт ТОЛЬКО в `dev/`
(артефакт процесса, арх §4.2); в корне проекта её НЕТ (п.19 §7).

## 2. Вход
- `Задание_Д12.txt` (3 типа памяти, явный выбор «что куда», проверки «какие данные
  попадают в каждый слой» и «как влияет на ответы»).
- `Nedela_3/Суть_N3.md` §4.2 и §4.8 (канон содержимого и структуры).
- `ND/arch/arch_den_11.md` §2.2–2.3 (таблица памяти + иерархия хранения).

## 3. Содержание (обязательно)
### 3.1 README.md
1. Суть дня (одна фраза): явная модель памяти, физически разделённые типы, явная
   маршрутизация, дозированная доставка.
2. Запуск: `.sh`/`.desktop` и прямые команды (`--user`, `--deliver`, `--mock`).
3. **Модель памяти** (п.15 — обязательный текстовый блок): для каждого типа —
   ЧТО хранит, ГДЕ файл, ПОЧЕМУ так:
   - краткосрочная → текущий диалог → `session.json` (неизменяемые сообщения с parent_id);
   - рабочая → данные текущей задачи → `working_memory.json` (пересчитываемое состояние);
   - долговременная → профиль-ССЫЛКА + задачи + решения (выполнена/не выполнена) + знания
     → `long_term_memory.json`;
   - профиль → style + constraints + context → JSON в SQLite (ProfileRepository),
     зеркало `profile.json`.
4. Иерархия хранения (канон куратора) + команды REPL + демонстрации (п.14).
### 3.2 Проверка.md
Таблица приёмки по §7 п.1–19: `пункт / что проверяем / как проверить / статус`.
Пункт 18 — «фильтр инвариантов не реализуем (задел PolicyEngine)» со статусом
«выполнено/не применимо», НЕ «❌ открыто». Неавтоматизируемые (п.14 демонстрации) —
`manual` с указанием артефакта в `$DEV`.

## 4. Выход (scope)
- `README.md` (корень проекта), `dev/Проверка.md`.

## 5. Критерий готовности
README непустой, содержит раздел «Модель памяти» с 4 типами и обоснованием «почему»;
`Проверка.md` содержит таблицу 1–19 с заполненными статусами; `check_acceptance.sh`
(соседний артефакт Этапа 10) возвращает exit 0 по обязательным пунктам.

## 6. Правила дня
Отчёт — `logs_reports/stages/stage_10_final.md` (✅). Приёмка — §7 п.15, п.19;
запреты — §8 (исторические файлы не менять, не коммитить).