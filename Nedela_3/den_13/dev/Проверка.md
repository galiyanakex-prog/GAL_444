# Проверка.md — чек-лист приёмки дня 13 (человеческая версия §7 + arch_den_13 §4.3)

Артефакт процесса: живёт только в `dev/`. Автоматический чекер —
`tests_debug/check_acceptance.sh` (запуск: `cd "$KOD" && API_KEY=test-key bash ...`).

| Пункт | Что проверяем | Как проверить / команда | Статус |
|---|---|---|---|
| 1 | Комплектность и сборка | `$PY -m py_compile Kod.py core/*.py memory/*.py storage/*.py`; есть README.md, run.sh (+x), run.desktop | ✅ |
| 2 | 3 типа памяти + профиль — разные классы/файлы | `tests_debug/unit/test_memory.py::test_four_layers_present` | ✅ |
| 3 | Иерархия хранилища по канону | `test_storage.py` (иерархия + profiles.db + session.json + task_state.json) | ✅ |
| 4 | Явная маршрутизация (лог «что куда») | `test_memory.py::test_remember_routes_explicitly`; журнал `[Память] … ← …` | ✅ |
| 5 | Дозированная доставка | `test_prompt.py::test_dosed_delivery_cut_layer`; `/deliver` | ✅ |
| 6 | Идентификация на старте | `--user` или ввод до загрузки памяти (smoke) | ✅ |
| 7 | Интервью-инициализация нового ID | `test_agent.py::test_interview_initialization`; `scenario.py::scenario_interview` | ✅ |
| 8 | Понятие таски + переход | `test_agent.py::test_switch_task`; `/tasks`, `/task` | ✅ |
| 9 | Промт явными блоками | `test_prompt.py::test_blocks_order` | ✅ |
| 10 | LLM за интерфейсом (ABC + RouterAI + Mock) | `test_llm.py` | ✅ |
| 11 | State machine (День 13: полный автомат) | `test_state.py` (5) + `test_fsm.py` (15); `/state` | ✅ |
| 12 | Resume | `test_agent.py::test_resume_known_user`; `scenario.py::scenario_resume` | ✅ |
| 13 | Неизменяемые сообщения с parent_id; рабочая ≠ лог | `test_memory.py::test_short_term_append_and_parent` | ✅ |
| 14 | Демонстрации задания | `/compare` (влияние на ответы); `scenario.py` (маршрутизация, дозированная доставка, интервью, resume, персонализация, пауза/продолжение) — артефакт: вывод L3/L4 | ✅ (manual) |
| 15 | Текстовое описание модели памяти | `README.md` — раздел «Модель памяти» (что/где/почему для 4 типов) + «Состояние задачи» | ✅ |
| 16 | Уроки дня 10 перенесены | `Kod.py`: errors="replace", readline, токен-учёт+CSV; `llm_client.py`: retry 429; BASE_DIR | ✅ |
| 17 | Тесты без живого ключа | L2/L3/L4 через `API_KEY=test-key` и `MockClient`/заглушку requests | ✅ |
| 18 | Фильтр инвариантов НЕ реализуем | задел `PolicyEngine` + `visibility/scope` в memory/base.py | ✅ не применимо |
| 19 | Проверка.md только в dev/ | `ls ./Проверка.md` → нет файла | ✅ |
| 20 | Несколько профилей на пользователя | `test_person.py::test_multi_profile_roundtrip` (roundtrip двух профилей, list_profiles, get/set_default); `/profile list\|show\|use\|new` | ✅ |
| 21 | Миграция и обратная совместимость | `test_person.py::test_migration_old_schema` (старая БД → профиль `default`, данные не потеряны), `test_backward_compatibility` (load без id → default; MemoryContext с 3 аргументами); прежние 19 пунктов без регрессии | ✅ |
| 22 | Роутер и авто-выбор профиля | `test_person.py::test_router_*`, `test_agent_switch_profile`, `test_agent_auto_route_switches_before_prompt`, `test_two_profiles_different_answers`; `scenario.py::scenario_personalization`; `/profile route`, `/profile auto on`; лог «[Роутер]», «[Агент] активный профиль» | ✅ |
| 23 | Формализованное состояние: в любой момент явно записаны этап, текущий шаг, ожидаемое действие | `test_fsm.py` (dataclass-поля TaskState); `/state` — снимок в REPL (задача/id, этап, шаг N/M + текст шага, expected_action, previous_stage, error, число results); блок working в промте — «Этап: … (шаг N/M): …», «Ожидаемое действие: …» | ✅ |
| 24 | Пауза на любом этапе + продолжение без повторных объяснений | `test_fsm.py::test_pause_and_resume` (execution, шаг 2 → paused → resume → тот же шаг), `test_pause_on_any_working_stage` (planning/execution/validation), `test_pause_idempotent`, `test_resume_not_paused_is_noop`; `scenario.py::scenario_pause_resume` (перезапуск → resume с шага 1, план НЕ перестроен, задача НЕ переспрошена); `/pause`, `/resume` | ✅ |
| 25 | Персистентность и жизненный цикл | `test_fsm.py::test_save_load_roundtrip` (JSON, enum-значения, previous_stage), `test_load_missing_or_broken_returns_none`, `test_restart_continues_from_saved_step`, `test_completed_steps_not_repeated` (results без дублей, current_step монотонен), `test_validation_success_goes_done` / `test_validation_failure_goes_failed`, `test_transition_*` (только ALLOWED_TRANSITIONS); гейт-проверка №10 (`task_state.json` переживает перезапуск: пауза → resume → done); `/task retry` (failed → planning) | ✅ |

Итог: обязательные пункты — 25 из 25 зелёных (19 прежних + строки 20–22 персонализации +
строки 23–25 автомата, arch_den_13 §4.3).
Живой смоук — ⏸ отложен (нет явного согласия; финал не блокирует: автомат
детерминированный, проверяется на заглушках без сети).
