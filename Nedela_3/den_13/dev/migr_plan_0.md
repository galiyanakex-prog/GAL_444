# migr_plan_0.md — Рабочий план. Этап 0: Подготовка и приёмка базовой линии

> **Тип файла: РАБОЧИЙ ПЛАН** (создан из план-эталона `dev/migr_plan.md`, раздел «Этап 0»).
> Миграция выполняется **по этому файлу**, подшаг за подшагом.
> **Автономный режим: включён по умолчанию** — действия считаются заранее одобренными;
> живые LLM-вызовы с реальным ключом — только с явного согласия пользователя.
>
> **Цель этапа:** убедиться, что стартуем с зелёного проекта (копия `den_12`), и завести
> журналы этапа (`dev/migr_log.md`, `dev/logs_reports/stages/stage_F_state.md`).
>
> **Источники истины:** `Задание_Д13.txt` → `ND/arch/arch_den_13.md` → `dev/migr_plan.md`.

---

## Входные условия этапа

- [ ] Проект `Nedela_3/den_13/` на месте: `Kod.py`, `core/`, `memory/`, `storage/`, `dev/tests_debug/`.
- [ ] `dev/migr_plan.md` (эталон) и этот файл существуют.
- [ ] Переменные путей: `$KOD` = корень `den_13/`, `$DEV` = `$KOD/dev`, `$TST` = `$DEV/tests_debug`, `$PY` = `../../.venv/bin/python`, `$ARCH` = `ND/arch/arch_den_13.md`.

## Правила этапа (наследуются из эталона §0.5 и §0.3)

1. Один подшаг = один логический блок; после правки — проверка → запись в `migr_log.md`.
2. При неуспехе: фиксация в `migr_log.md` (команда, вывод, гипотеза, ❌) → минимальная
   правка → повтор — до успеха (retry-цикл не ограничен).
3. Временные файлы — только `$TST/.tmp/`; `Задание_Д13.txt` и чужие дни не трогать; не коммитить.
4. Восстановление контекста: `migr_log.md` (последний ✅) → этот файл → `$ARCH`.

---

## Подшаги этапа

### Подшаг 0.1 — Прогон базовой линии (L2+L4+гейт)
- Цель: зафиксировать «до» — все проверки зелёные на копии den_12.
- Прочитать: (ничего нового).
- Изменить/создать: (ничего).
- Проверка: `cd $KOD && env -u API_KEY $PY $TST/unit_runner.py && env API_KEY=test-key $PY $TST/scenario.py && API_KEY=test-key bash $TST/check_acceptance.sh` — все EXIT 0.
- Запись в журнал: 0.1.
- Объём: ~0 строк чтения, ~0 строк правки (только прогон).
- Кандидат на батч: нет (стартовая точка).

### Подшаг 0.2 — Ознакомление с целевой архитектурой
- Цель: сверить план с `arch_den_13.md` §2.2–2.9, §3.3, §4.3; выписать регресс-лист
  (контракты, которые меняются, и которые **не** меняются).
- Прочитать: `$ARCH` (§2.2–2.9, §3.3, §4.3), `Задание_Д13.txt` (разделы «Что именно
  протестировать», «Пример основного цикла»).
- Изменить/создать: `dev/logs_reports/stages/stage_F_state.md` (шапка «было → стало →
  проверка → статус» + таблица регресс-листа); `dev/migr_log.md` (шапка журнала, если нет).
- Проверка: файлы непустые; регресс-лист содержит 22 прежних критерия.
- Запись в журнал: 0.2.
- Объём: ~400 строк чтения, ~50 строк правки.
- Кандидат на батч: да (с 0.1 — чтение после прогона).

### Подшаг 0.3 — Чтение текущего кода точек изменения
- Цель: зафиксировать точные места вставки (строки/секции) в 4 файлах.
- Прочитать: `core/state_machine.py` (весь), `storage/store.py` (секция «Рабочая память»),
  `memory/working.py` (`as_prompt_block`), `core/agent.py` (`__init__`, `load_state`,
  `switch_task`, `save_state`), `Kod.py` (секции `/state`, `/task`, `/help`).
- Изменить/создать: (ничего; выводы — записью в `stage_F_state.md`).
- Проверка: в журнале перечислены все точки вставки по файлам.
- Запись в журнал: 0.3.
- Объём: ~600 строк чтения, ~20 строк правки.
- Кандидат на батч: да (с 0.2).

---

## Чек-лист приёмки этапа (все пункты — до перехода дальше)

- [ ] 0.1: гейт базовой линии 9/9, unit 40 тестов, scenario 6 — все EXIT 0.
- [ ] 0.2: `dev/migr_log.md` создан (шапка + запись Этапа 0); `stage_F_state.md` создан,
      регресс-лист из 22 критериев заполнен.
- [ ] 0.3: карта точек вставки (5 файлов) зафиксирована в `stage_F_state.md`.
- [ ] Незакрытых ❌/⛔ нет.

## Последний пункт алгоритма этапа (выполняется только после ✅ приёмки)

1. Записать в `dev/migr_log.md` блок «## Этап 0 — Подготовка и приёмка базовой линии»
   по формату эталона §0.4 (Статус ✅, Подшаги, Было/Стало, Проверки, Следующий этап).
2. **Перейти к выполнению следующего этапа: открыть `dev/migr_plan_1.md` и начать с его
   подшага 1.1.** (Если этап не принят — повторять подшаги/правки до успеха, п. 2 алгоритма.)
