# debug_plan_4.md — рабочий план этапа D4 «Документация: README / arch / Проверка»

> **Роль документа.** Рабочий план-алгоритм **одного этапа** дебага проекта `den_14`
> по плану-эталону `dev/meta_promt/debug_plan.md`. Разворачивает этап **D4** из
> `debug_plan.md` §5 в конкретные правки и проверки. Закрывает проблему **B4**.
> **Конец этого этапа — автоматический гейт в этап D5** (`debug_plan_5.md`).
> Источники: `debug_plan.md` §3 (B4), результаты этапов D1–D3.

---

## 0. Паспорт этапа

| Поле | Значение |
|---|---|
| Этап | **D4** — Документация |
| Рабочий план | `dev/meta_promt/debug_plan_4.md` (этот файл) |
| Зависит от | **D1–D3** (код приведён к spec'у — документируем фактическое) |
| Открывает | `dev/meta_promt/debug_plan_5.md` (D5 — Артефакты процесса) |
| Закрывает | **B4** (README/arch/Проверка отстают от кода) |
| Основные артефакты | `README.md`, `arch_den_14.md`, `dev/Проверка.md` |
| Живой ключ | **Не нужен** (правки только в `.md`) |
| Меняет поведение | **Нет** (только документация) |

**Цель этапа.** Синхронизировать документацию с кодом после D1–D3: удалить закрытые
пункты «Расхождения spec/кода» и «Косметика», описать `DELIVERABLE`/`--budget`,
дополнить критерий 27 проверкой из CLI, зафиксировать факт баг-фикса A1.

---

## 1. Вход и предусловия

- Результаты D1–D3 (записи в `debug_log.md`: что изменено, новые тесты, счётчики).
- `README.md`: §«Известные шероховатости и заделы» (пункты «Расхождения spec/кода»
  — 4 шт., «Косметика» — окно/ROLES/импорты/артефакты дня 12); §«Запуск» (флаги —
  11); §«Сборка промта»; §«Что зафиксировано в процессе создания».
- `arch_den_14.md`: §2.7 (инжект в промт), §2.12 (флаги), §2.13 (наследие).
- `dev/Проверка.md`: критерий 27 (учёт инвариантов в рассуждениях).
- D3 зелёный.

**Предусловия:** код стабилен (D1–D3 зелёные); правки документации не опережают код.

---

## 2. Шаги этапа (исполняемый алгоритм)

### Шаг 4.1 — `README.md`: раздел «Известные шероховатости и заделы»
1. **Удалить закрытые пункты** (перенести факт закрытия в журнал, не в README):
   - «Расхождения spec/кода» — все 4 (`MODEL_CONTEXT_LIMIT`/цены; `budget`;
     `summary` недостижим; `identify()` не вызывается) → закрыты D2/D1/D3;
   - «Косметика» — окно short_term, `Agent.ROLES`, неиспользуемые импорты →
     закрыты D3.
2. **Оставить** (ещё актуальны до D5): пункт про исторические артефакты дня 12 —
   будет закрыт в D5 (не трогать здесь, чтобы не править дважды; пометить
   «→ закрывается дебагом D5» не нужно — просто оставить как есть).
3. **Оставить** пункты «Заделы по замыслу» (PolicyEngine, next_state, parent_id,
   скиллы) — вне скоупа дебага.
4. **Ожидаемый результат:** раздел содержит только актуальные заделы и пункт
   про артефакты дня 12 (до D5).

### Шаг 4.2 — `README.md`: флаги, сборка промта, зафиксированное
1. §«Запуск», «Флаги (11)» → **«Флаги (12)»**: добавить `--budget <N>` (лимит
   входящих токенов промта, по умолчанию выключен; ориентир — `MODEL_CONTEXT_LIMIT`
   минус резерв под ответ).
2. §«Сборка промта»: дополнить — доставляемое подмножество блоков задаётся
   `DELIVERABLE` из `core/prompt_builder.py` (`profile, invariants, long_term,
   working, summary, short_term`; `role`/`current` — всегда); `summary` достижим
   из CLI (`--deliver profile,summary`).
3. §«Что зафиксировано в процессе создания»: добавить пункт «Баг доставки блоков
   (дебаг, ✅ исправлен): пересечение `deliver ∩ LAYER_ORDER` в `main()`/`/deliver`
   вырезало `invariants`/`summary` — блок инвариантов не попадал в промт CLI;
   исправлен каноническим набором `DELIVERABLE` (критерий 27 теперь проверяем из
   CLI)».
4. §«Тестирование»: обновить счётчик L2 (69 → новое число после D1–D3, взять
   фактическое из `debug_log.md`).
5. **Ожидаемый результат:** README описывает фактические флаги/наборы/счётчики.

### Шаг 4.3 — `arch_den_14.md`: минимальный дифф
1. §2.7 (инжект в промт): дополнить абзацем — доставляемые имена блоков —
   `DELIVERABLE` (канонический набор; `--deliver`/`/deliver` пересекаются с ним,
   не с `LAYER_ORDER`).
2. §2.12 (флаги): `--budget <N>` в перечень (дефолт `None` — выключен).
3. Больше **не менять** (arch — целевое состояние миграции; правки минимальны,
   чтобы не плодить вторую README).
4. **Ожидаемый результат:** arch согласован с кодом в двух точках правок.

### Шаг 4.4 — `dev/Проверка.md`: критерий 27 (+ опционально 31)
1. Критерий 27 («ассистент явно учитывает инварианты в рассуждениях»): дополнить
   команду проверки — помимо юнит-пути, прогон CLI: добавить инвариант → обычный
   запрос → в ответе `MockClient`/`Den_log.md` виден блок `[system: invariants]`
   (команда — по образцу шага 1.5 `debug_plan_1.md`, с `--memory-dir` в `.tmp/`).
2. **Опционально (решение на этапе, зафиксировать в журнале)**: критерий 31
   «бюджет урезает необязательные блоки» — `--budget 1` → блок profile опускается,
   запрос остаётся. Если добавляется — обновить счётчик «30 критериев» → «31»
   в README (§«Ручная проверка») и заголовок Проверки.
3. **Ожидаемый результат:** критерии воспроизводимы по тексту; счётчики согласованы.

### Шаг 4.5 — Сверка документация ↔ код
1. Пройтись по изменённым пунктам и сверить с кодом: имена (`DELIVERABLE`,
   `--budget`, `prompt_budget`, `window`, `recent`, `ROLES`), числа (флаги 12,
   тесты L2, критерии), формулировки команд.
2. `grep -n "budget\|DELIVERABLE\|identify\|ROLES\|SHORT_TERM_WINDOW" README.md
   arch_den_14.md dev/Проверка.md` — только согласованные упоминания.
3. **Ожидаемый результат:** расхождений формулировок нет.

---

## 3. Выход этапа

- `README.md` (шероховатости, флаги 12, сборка промта, зафиксированное, счётчики),
  `arch_den_14.md` (§2.7, §2.12), `dev/Проверка.md` (критерий 27 [, 31]).
- Запись этапа **D4** в `dev/meta_promt/debug_log.md`; статус B4 → ✅.

---

## 4. Автоматический гейт D4→D5

Гейт считается **зелёным**, если одновременно:
- [ ] закрытые пункты «Расхождения spec/кода» (4) и «Косметика» (3) удалены из
      README; заделы по замыслу и пункт про артефакты дня 12 — на месте;
- [ ] `--budget` описан во флагах (12); `DELIVERABLE` описан в сборке промта;
- [ ] факт баг-фикса A1 зафиксирован в «Что зафиксировано»;
- [ ] критерий 27 проверяем из CLI по тексту Проверки; счётчики согласованы;
- [ ] сверка документация ↔ код — без расхождений (grep-контроль);
- [ ] код не изменялся (этап только `.md`); L2/L4/гейт — контрольный прогон без
      изменений относительно D3.

**Зелёный** → запись D4 в `debug_log.md` (✅, B4 → ✅) → **перечитать
`debug_plan.md`** → создать/открыть `debug_plan_5.md`.
**Красный** → карточка ошибки `dev/logs_reports/errors/error_<ts>.md`, этап D4
остаётся открыт.

---

## 5. Запись в `debug_log.md` (форма §7.4 `debug_plan.md`)

```text
## Этап D4 — Документация
- Статус: ✅ завершён | ⚠️ обход | ❌ открыт
- Закрыто: B4
- Было: README содержал 4 расхождения spec/кода и 3 пункта косметики (уже
  закрыты кодом); флагов 11; критерий 27 проверялся только юнит-путём
- Стало: README/arch/Проверка синхронизированы с кодом (DELIVERABLE, --budget,
  флаги 12, критерий 27 из CLI [, критерий 31]); факт баг-фикса A1 зафиксирован
- Проверка: <grep-сверка документация↔код; контрольный прогон L2/L4/гейт>
- Артефакты: README.md, arch_den_14.md, dev/Проверка.md
- Спорное/риски: <решение по критерию 31 — добавить/не добавлять>
- Перечитывание debug_plan.md перед следующим этапом: ✅ выполнено (дата/время)
- Гейт D4→D5: ✅ пройден | ❌ не пройден
```

---

## 6. Следующий шаг

Перечитать актуальный `dev/meta_promt/debug_plan.md`, затем приступить к **D5** по
`dev/meta_promt/debug_plan_5.md` (архив артефактов дня 12, актуализация `scen_1.md`,
чистка `migr_log.md` — закрытие B1–B3).
