# debug_plan_6.md — рабочий план этапа D6 «Финал: прогон, приёмка, итог»

> **Роль документа.** Рабочий план-алгоритм **одного этапа** дебага проекта `den_14`
> по плану-эталону `dev/meta_promt/debug_plan.md`. Разворачивает этап **D6** из
> `debug_plan.md` §5 в конкретные команды и проверки. Финальный этап (закрытие
> дебага по DoD `debug_plan.md` §8).
> Источники: `debug_plan.md` §8 (DoD), §10 (трассируемость), записи D0–D5 в
> `debug_log.md`.

---

## 0. Паспорт этапа

| Поле | Значение |
|---|---|
| Этап | **D6** — Финал: прогон, приёмка, итог |
| Рабочий план | `dev/meta_promt/debug_plan_6.md` (этот файл) |
| Зависит от | **D0–D5** (все проблемы реестра закрыты) |
| Открывает | — (финальный этап дебага) |
| Закрывает | — (сверка DoD; итоговая запись) |
| Основные артефакты | итоговая запись в `dev/meta_promt/debug_log.md`; предложение коммита |
| Живой ключ | **Не нужен** (весь финал — на `MockClient` / `API_KEY=test-key`) |
| Меняет поведение | **Нет** (только прогоны и записи) |

**Цель этапа.** Полный прогон контура, приёмка по DoD, сверка реестра (все ID
закрыты), демонстрация баг-фикса «до/после», итоговая запись и предложение коммита.

---

## 1. Вход и предусловия

- Результаты D0–D5 (все записи в `debug_log.md` со статусом ✅).
- `debug_plan.md` §8 (DoD — 8 пунктов), §10 (трассируемость — таблица «ID →
  проверка закрытия»).
- Тестовый контур `dev/tests_debug/` (L1–L4 + гейт).
- `dev/Проверка.md` (30 [, 31] критериев — версия после D4).

**Предусловия:** гейты D0–D5 зелёные; открытых карточек ошибок нет.

---

## 2. Шаги этапа (исполняемый алгоритм)

### Шаг 6.1 — Полный прогон контура (без живого ключа)
```bash
cd Nedela_3/den_14
source ../../.venv/bin/activate
export API_KEY=test-key
python -m py_compile Kod.py core/*.py memory/*.py storage/*.py   # L1
env -u API_KEY python dev/tests_debug/unit_runner.py             # L2
env API_KEY=test-key python dev/tests_debug/smoke.py             # L3
env API_KEY=test-key python dev/tests_debug/scenario.py          # L4
env API_KEY=test-key bash dev/tests_debug/check_acceptance.sh    # гейт
```
1. Зафиксировать все числа и exit-коды (L2 — фактическое число после D1–D3;
   L4 — 8 сценариев; гейт — 12/12).
2. **Ожидаемый результат:** все EXIT 0; числа совпадают с зафиксированными
   в D3/D5 (после D3 код не менялся).

### Шаг 6.2 — Приёмка по `dev/Проверка.md`
1. Пройти чек-лист (30 [, 31] критериев) по командам из файла; особое внимание:
   - критерий 27 — **из CLI** (прогон с инвариантом → блок `[system: invariants]`
     виден в ответе `MockClient`/`Den_log.md`) — команда из обновлённой Проверки;
   - критерии 26–30 (инварианты) и 23–25 (автомат) — без регрессии;
   - критерий 15 (текстовое описание) — README актуален после D4/D5.
2. Отметки — в рабочей копии проверяющего (в `debug_log.md` — итог: N/N зелёные).
3. **Ожидаемый результат:** 30/30 [, 31/31] зелёные.

### Шаг 6.3 — Сверка реестра и трассируемости
1. По `debug_plan.md` §10 пройти таблицу «ID → проверка закрытия» — каждую
   проверку выполнить фактически (grep/тест/файл):
   - A1: блок инвариантов в промте CLI (шаг 6.2, критерий 27);
   - A2: `grep -n "MODEL_CONTEXT_LIMIT\|PRICE_" core/llm_client.py Kod.py`;
   - A3: `--budget 1` урезает блоки (смоук из D2, шаг 2.6);
   - A4: `grep -n "def identify" core/agent.py` — пусто;
   - A5: `ROLES` читается в `remember_message` (тест из D3);
   - A6: `grep -rn "SHORT_TERM_WINDOW\|\[-10:\]"` — пусто;
   - A7: импорты чистые (`py_compile` + grep);
   - B1: `dev/logs_reports/archive/` существует, указатель на месте;
   - B2: `scen_1.md` — заголовок/шаги Дня 14;
   - B3: `migr_log.md` — без «Этап 1 — …»/дубля «Этапа 0»;
   - B4: сверка README/arch/Проверка ↔ код (grep из D4, шаг 4.5).
2. Сверить статусы в таблице реестра `debug_log.md` — все ✅.
3. **Ожидаемый результат:** реестр закрыт полностью; расхождений трассируемости нет.

### Шаг 6.4 — Демонстрация баг-фикса «до/после» (итоговая строка)
1. Собрать пару фактов из журнала: D0 (шаг 0.3 — блок инвариантов отсутствовал)
   и D1 (шаг 1.5 — блок виден). Если прогон D1 не сохранил вывод — повторить
   контрольный прогон (шаг 6.2, критерий 27) и зафиксировать вывод.
2. Записать в итоговую запись: «баг A1: до — блок `[system: invariants]` не
   попадал в промт CLI ни при каком `--deliver`; после — виден при дефолтном
   запуске, отключается `--deliver` без `invariants`».
3. **Ожидаемый результат:** пара «до/после» зафиксирована в `debug_log.md`.

### Шаг 6.5 — Итоговая запись в `debug_log.md`
1. Записать этап D6 (форма §7.4) + блок «Итог дебага»:
   - что сделано (по группам A/B, с этапами);
   - что проверено (контур: L1/L2/L3/L4/гейт с числами; приёмка N/N; реестр
     11/11 закрыт);
   - что спорно/риски (например: состав промта при дефолте изменился — заявлено
     spec'ом, критерий 27; решение по критерию 31 из D4);
   - что вне скоупа (перенос из раздела «вне скоупа» — без изменений);
   - изменённые файлы (сводный список по D1–D5).
2. Обновить таблицу реестра в `debug_log.md` — все ID ✅.
3. **Ожидаемый результат:** журнал полный и самодостаточный.

### Шаг 6.6 — Предложение коммита
1. Сформировать предложение: список изменённых/созданных файлов (по `debug_log.md`),
   рекомендуемое сообщение коммита (например: `den_14: debug D0–D6 — фикс доставки
   блоков (DELIVERABLE), константы+--budget, косметика ядра, архив артефактов
   дня 12, синхронизация документации`).
2. **Не коммитить** — коммит только по явной команде пользователя (граница §0.2).
3. **Ожидаемый результат:** предложение зафиксировано в `debug_log.md`.

---

## 3. Выход этапа

- Итоговая запись D6 + блок «Итог дебага» в `debug_log.md`.
- Таблица реестра — все ID ✅.
- Предложение коммита (в журнале).

---

## 4. Финальный гейт (закрытие дебага)

Гейт считается **зелёным** (дебаг завершён по DoD `debug_plan.md` §8), если:
- [ ] L1 ok / L2 (фактическое число) OK / 0 FAIL / L3 OK / L4 8/8 / гейт 12/12 —
      все EXIT 0, без живого ключа;
- [ ] приёмка `dev/Проверка.md` — 30/30 [, 31/31] зелёные; критерий 27 проверен
      **из CLI**;
- [ ] реестр §3: A1–A7, B1–B4 — все закрыты, подтверждены проверками §10;
- [ ] пара «до/после» баг-фикса A1 зафиксирована;
- [ ] README / arch / Проверка соответствуют коду; исторические артефакты — в
      `archive/` с указателем; `scen_1.md` — День 14; `migr_log.md` чист;
- [ ] все этапы D0–D6 отражены в `debug_log.md` со статусом ✅; после каждого
      этапа `debug_plan.md` перечитан (фиксации в журнале);
- [ ] рабочие планы `debug_plan_0.md … debug_plan_6.md` созданы и исполнены;
- [ ] предложение коммита сформировано (коммит — по явной команде пользователя).

**Зелёный** → дебаг закрыт; статус «Итог дебага» в `debug_log.md`.
**Красный** → карточка ошибки `dev/logs_reports/errors/error_<ts>.md`; незакрытый
пункт возвращается в реестр (запись в `debug_log.md` с ID и этапом-владельцем).

---

## 5. Запись в `debug_log.md` (форма §7.4 `debug_plan.md`)

```text
## Этап D6 — Финал: прогон, приёмка, итог
- Статус: ✅ завершён | ⚠️ обход | ❌ открыт
- Закрыто: — (сверка DoD; реестр 11/11)
- Было: этапы D0–D5 завершены, итог не сведён
- Стало: полный прогон зелёный; приёмка N/N; реестр закрыт; итог дебага записан;
  предложение коммита сформировано
- Проверка: <L1/L2/L3/L4/гейт с числами; приёмка; таблица трассируемости §10>
- Артефакты: dev/meta_promt/debug_log.md (итог)
- Спорное/риски: <состав промта при дефолте изменился (заявлено критерием 27);
  решение по критерию 31>
- Перечитывание debug_plan.md: — (финальный этап)
- Гейт (финальный): ✅ пройден | ❌ не пройден
```

---

## 6. Следующий шаг

Дебаг завершён. Дальнейшие действия — по явной команде пользователя: коммит
(предложение в `debug_log.md`), живой смоук (⏸ отложен — требует согласия),
либо перенос накопленных уроков в следующие дни недели (`den_15+`).
