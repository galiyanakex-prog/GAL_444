# debug_log.md — журнал выполнения дебага `den_14` (по плану-эталону `debug_plan.md`)

> Журнал результатов дебага по рабочим планам `dev/meta_promt/debug_plan_0.md` …
> `debug_plan_N.md`. План-эталон: `dev/meta_promt/debug_plan.md` (реестр проблем — §3,
> формат записей — §7.4, правила повтора — §2).
> Обозначения: ✅ выполнено · ❌ не выполнено (повтор) · ⚠ допустимое временное
> отклонение · ⛔ блокирующая ошибка (ожидание указаний).

---

## Реестр проблем: статус закрытия

> Копия ID из `debug_plan.md` §3; статус обновляется по итогам этапов.
> Полный реестр (описания, файлы, этапы) — в `debug_plan.md` §3; здесь — только статус.

| ID | Проблема (кратко) | Этап | Статус |
|---|---|---|---|
| A1 | Баг: `invariants`/`summary` вырезаются из `deliver` пересечением с `LAYER_ORDER` | D1 | ✅ закрыт (D1) |
| A2 | `MODEL_CONTEXT_LIMIT`/цены отсутствуют в `llm_client.py`, продублированы в argparse | D2 | ✅ закрыт (D2) |
| A3 | `budget` не передаётся в рабочем пути | D2 | ✅ закрыт (D2) |
| A4 | `Agent.identify()` не вызывается | D3 | ✅ закрыт (D3) |
| A5 | `Agent.ROLES` — мёртвый код | D3 | ✅ закрыт (D3) |
| A6 | Окно short_term задвоено (`SHORT_TERM_WINDOW` + `[-10:]`) | D3 | ✅ закрыт (D3) |
| A7 | Неиспользуемые импорты (3 файла) | D3 | ✅ закрыт (D3) |
| B1 | Исторические артефакты дня 12 как «актуальные» | D5 | ✅ закрыт (D5) |
| B2 | `scen_1.md` описывает День 11/12, не День 14 | D5 | ✅ закрыт (D5) |
| B3 | `migr_log.md`: заглушки, дубль «Этапа 0», обрыв | D5 | ✅ закрыт (D5) |
| B4 | README/arch/Проверка отстанут от кода после D1–D3 | D4 | ✅ закрыт (D4) |

---

## Вне скоупа дебага (зафиксировано, не делаем — `debug_plan.md` §0.2)

- Пустые каталоги `dev/tests_debug/fixtures/` и `smoke/` (каркас, не мешают).
- Накопившиеся каталоги в `dev/tests_debug/.tmp/` (в `.gitignore`, не удаляются
  автоматически).
- Заделы по замыслу: `PolicyEngine` (пустой интерфейс), `next_state()` (не вызывается),
  `parent_id` (ветвление диалога), движок исполнения скиллов — программа следующих дней.
- Живой смоук LLM — ⏸ отложен (требует явного согласия, финал не блокирует).

---

## Этап D0 — Базовая линия и воспроизведение бага (2026-09-21)

- Статус: ✅ завершён
- Закрыто: — (инвентаризация; реестр A1–A7/B1–B4 подтверждён по коду)
- Было: baseline неизвестен; баг A1 не доказан прогоном; реестр — по ревью кода.
- Стало: baseline зелёный; баг A1 **воспроизведён и зафиксирован**; реестр подтверждён
  grep-сверкой (таблица «ID → файл:строка» ниже); неизменяемые контракты перечислены.
- Проверка:
  - Шаг 0.1: `python --version` → Python 3.13.5; дерево модулей полное (18 файлов
    core/memory/storage + Kod.py); EXIT 0;
  - Шаг 0.2 baseline: L1 `py_compile` → ok; L2 `unit_runner.py` → **69 OK, 0 FAIL**;
    L3 `smoke.py` → **SMOKE OK**; L4 `scenario.py` → **8 сценариев OK**; гейт
    `check_acceptance.sh` → **12 из 12** (FAIL=0); все EXIT 0;
  - Шаг 0.3 баг A1: прогон CLI (`--mock`, `--memory-dir` в `.tmp/dbg_a1/`):
    (а) с `--deliver profile,invariants,long_term,working,short_term` → в ответе
    `MockClient` блока инвариантов **нет** (grep «Обязательные инварианты» → 0);
    (б) с дефолтным `--deliver` → тоже **нет** (0). **Факт бага**: при любом
    `--deliver` блок `[system: invariants]` в промт CLI не попадает — пересечение
    `deliver & set(LAYER_ORDER)` (Kod.py:549, 641) вырезает `invariants` (его нет в
    `LAYER_ORDER`, manager.py:16); дефолт argparse тоже без `invariants`;
  - Шаг 0.4 реестр: grep-сверка по коду — все ID подтверждены (таблица ниже);
  - Шаг 0.5: «вне скоупа» — раздел выше (без изменений).
- Артефакты: изменённых файлов продукта нет (этап read-only); записи в этом журнале.
- Спорное/риски: нет. Юнит-тесты баг не ловят (передают `deliver` напрямую, минуя
  CLI-пересечение) — подтверждает план D1 (шаг 1.4, тест на уровне агента + смоук CLI).

### Таблица подтверждения реестра (ID → файл:строка)

| ID | Место в коде (подтверждено) |
|---|---|
| A1 | `Kod.py:549` (`main()`), `Kod.py:641` (`/deliver`) — `deliver & set(LAYER_ORDER)`; `memory/manager.py:16` — `LAYER_ORDER` без `invariants`/`summary`; дефолт `--deliver` (Kod.py:~78) без `invariants` |
| A2 | `core/llm_client.py` — констант `MODEL_CONTEXT_LIMIT`/`PRICE_*` нет (только URL/MODEL/REQUEST_TIMEOUT/RETRY_DELAYS); `Kod.py:89,91` — литералы `11.0`/`33.0` |
| A3 | `core/prompt_builder.py:63,118` — параметр `budget` есть, но `core/agent.py`/`Kod.py` его не передают (grep по обоим — только prompt_builder) |
| A4 | `core/agent.py:215` — `def identify`; вызовов нет (grep по Kod.py/core/dev — пусто) |
| A5 | `core/agent.py:163` — `ROLES = {"short_term": "user"}`; чтений нет |
| A6 | `memory/short_term.py:44` (`[-SHORT_TERM_WINDOW:]`) + `:50` (константа после класса); `core/agent.py:549` (литерал `[-10:]`) |
| A7 | `core/agent.py:16` (`import os` — `os.` не используется), `:19` (`uuid4` — не используется); `Kod.py:40` (`uuid4`), `:52` (`MemoryItem`), `:54` (`LLMClient`, `MODEL`) — не используются; `storage/store.py:23` (`datetime` — не используется) |
| B1 | `dev/logs_reports/`: `run_log.md`, `final_report.md`, `stages/` (13 файлов, вкл. день-12 `stage_00…stage_E_person`) лежат как актуальные |
| B2 | `dev/tests_debug/scenario/scen_1.md` — заголовок «День 11 — модель памяти агента», соответствие `Задание_Д12.txt` |
| B3 | `dev/migr_log.md:21` — обрыв «**Найдено расхождений:»; заглушка «Этап 1 — …» (1 шт.); дубль «Этап 0 — Подготовка и приёмка базовой линии» до M0–M8 |
| B4 | README §«Расхождения spec/кода» (4 пункта) + §«Косметика» — станут ложью после D1–D3 |

### Неизменяемые контракты (не трогать в D1–D5)

- `core/llm_client.py` — сигнатуры `LLMClient`/`RouterAIClient`/`MockClient`;
- `memory/*` — контракт `MemoryLayer` (read/write/as_prompt_block), `MemoryManager`;
- `memory/manager.py:16` — `LAYER_ORDER` (порядок слоёв памяти, не блоков промта);
- `storage/db.py` (`ProfileRepository`), фасад `Store` (только добавления по плану);
- `core/state_machine.py`, `core/profile_router.py`, `core/invariants.py`.

- Перечитывание debug_plan.md перед следующим этапом: ✅ выполнено (2026-09-21)
- Гейт D0→D1: ✅ пройден (baseline зелёный; баг A1 воспроизведён; реестр подтверждён;
  контракты зафиксированы; «вне скоупа» на месте)

---

## Этап D1 — Доставка блоков: DELIVERABLE (2026-09-21)

- Статус: ✅ завершён
- Закрыто: **A1**
- Было: `deliver ∩ LAYER_ORDER` в `main()`/`/deliver` вырезал `invariants`/`summary`;
  дефолт `--deliver` без `invariants`; блок `[system: invariants]` не попадал в
  промт CLI ни при каком `--deliver` (факт D0).
- Стало: `DELIVERABLE` в `core/prompt_builder.py` (канонический набор доставляемых
  имён: profile, invariants, long_term, working, summary, short_term); оба
  пересечения в `Kod.py` — по `DELIVERABLE`; дефолт `--deliver` полный (с
  `invariants`); импорт `LAYER_ORDER` из `Kod.py` убран (не используется).
- Проверка:
  - новые тесты: `test_deliverable_canonical`, `test_summary_reachable_via_deliverable`
    (test_prompt.py), `test_agent_deliver_keeps_invariants` (test_agent.py) — зелёные;
  - L1 `py_compile` → ok; L2 → **72 OK, 0 FAIL** (69 + 3); L3 → SMOKE OK; L4 →
    **8 сценариев OK** (вкл. `scenario_invariant_conflict`); гейт → **12 из 12**;
    все EXIT 0;
  - баг-фикс «до/после»: (до, D0) блок инвариантов — 0 вхождений при любом
    `--deliver`; (после, D1) дефолтный запуск → **1 вхождение** («Обязательные
    инварианты:» в ответе MockClient); `--deliver profile,working` → **0**
    (доставка отключается осознанно — работает в обе стороны).
- Артефакты: `core/prompt_builder.py`, `Kod.py`, `dev/tests_debug/unit/test_prompt.py`,
  `dev/tests_debug/unit/test_agent.py`.
- Спорное/риски: состав промта при дефолтном запуске изменился (+блок инвариантов) —
  это заявленное spec'ом поведение (критерий 27 приёмки); `LAYER_ORDER` в
  `memory/manager.py` не тронут; логика `build()` не изменена.
- Перечитывание debug_plan.md перед следующим этапом: ✅ выполнено (2026-09-21)
- Гейт D1→D2: ✅ пройден (блок виден при дефолте; отключается без `invariants`;
  summary достижим; L2 72 / L3 / L4 8 / гейт 12/12; контракты не тронуты)

---

## Этап D2 — Константы модели/цен + бюджет промта (2026-09-21)

- Статус: ✅ завершён
- Закрыто: **A2, A3**
- Было: константы `ПРОМТ_llm.md` §3.1 отсутствовали (`MODEL_CONTEXT_LIMIT`,
  `PRICE_IN_PER_M`, `PRICE_OUT_PER_M`); цены — литералы `11.0`/`33.0` в argparse;
  `budget` не передавался в рабочем пути (проверен только юнит-тестом билдера).
- Стало: три константы в `core/llm_client.py` (шесть из шести по `ПРОМТ_llm.md`);
  дефолты `--price-in`/`--price-out` — из констант; `Agent.prompt_budget` (None —
  выключено) + проводка `budget=` в `respond()`; флаг `--budget` (default None) +
  стартовая печать режима; тест проводки.
- Проверка:
  - `grep "default=11.0\|default=33.0" Kod.py` → пусто (дефолты из констант);
  - новый тест `test_prompt_budget_passed_in_respond` — зелёный (budget=1:
    `[profile]` опущен, запрос/роль на месте; None: блок присутствует);
  - L1 → ok; L2 → **73 OK, 0 FAIL** (72 + 1); L3 → SMOKE OK; L4 → 8/8; гейт →
    **12 из 12**; все EXIT 0;
  - смоук CLI: `--budget 1` → «Бюджет промта: 1 токенов» + MockClient видит
    **2 сообщения** (роль + запрос — необязательные опущены); без флага —
    **6 сообщений** (состав прежний, поведение не изменилось).
- Артефакты: `core/llm_client.py`, `core/agent.py`, `Kod.py`,
  `dev/tests_debug/unit/test_agent.py`.
- Спорное/риски: решение по импорту `MODEL` в `Kod.py` — **используется** (в
  help-тексте `--budget` через `MODEL_CONTEXT_LIMIT`; `MODEL` оставлен в импорте
  для симметрии констант — используется в `build_agent`? нет: проверено, `MODEL`
  не вызывается в `Kod.py` → убрать на D3 (A7), зафиксировано здесь). Автодефолт
  `budget = MODEL_CONTEXT_LIMIT` **не вводился** (запрет §3.3 эталона).
- Перечитывание debug_plan.md перед следующим этапом: ✅ выполнено (2026-09-21)
- Гейт D2→D3: ✅ пройден (константы на месте; литералы цен убраны; `--budget`
  работает, без флага поведение то же; L2 73 / L3 / L4 8 / гейт 12/12; сигнатуры
  LLM-классов не тронуты)

---

## Этап D3 — Косметика ядра (2026-09-21)

- Статус: ✅ завершён
- Закрыто: **A4, A5, A6, A7**
- Было: окно задвоено (`SHORT_TERM_WINDOW` после класса + литерал `[-10:]` в
  `build_context`); `ROLES = {"short_term": "user"}` — мёртвый; `identify()` не
  вызывался; неиспользуемые импорты: `os`/`uuid4` (agent.py), `uuid4`/
  `MemoryItem`/`LLMClient`/`MODEL` (Kod.py), `datetime` (store.py).
- Стало: `ShortTermMemory.window = 10` + `recent(ctx, limit=None)` — единственный
  владелец окна; `as_prompt_block()` и `Agent.build_context` — через `recent()`;
  `ROLES = {"user": "user", "assistant": "model"}` читается в
  `remember_message` (тернарник удалён); `identify()` удалён (идентификация — в
  `main()` `Kod.py`, как и была); импорты вычищены (в т.ч. `MODEL` из `Kod.py` —
  решение D2 подтверждено: не используется).
- Проверка:
  - grep-контроли: `SHORT_TERM_WINDOW|\[-10:\]` → пусто; `def identify` → пусто;
    вызовов `identify` в Kod.py/dev → нет;
  - новые тесты: `test_recent_window_and_limit`, `test_as_prompt_block_uses_window`
    (test_memory.py), `test_roles_mapping_in_session` (test_agent.py) — зелёные;
  - L1 → ok; L2 → **76 OK, 0 FAIL** (73 + 3); L3 → SMOKE OK; L4 → 8/8; гейт →
    **12 из 12**; все EXIT 0;
  - контроль состава промта: прогон CLI (как в D1) → блок инвариантов виден
    (1 вхождение) — состав промта идентичен D1, поведение не изменилось.
- Артефакты: `memory/short_term.py`, `core/agent.py`, `Kod.py`,
  `storage/store.py`, `dev/tests_debug/unit/test_memory.py`,
  `dev/tests_debug/unit/test_agent.py`.
- Спорное/риски: нет. Контракт `MemoryLayer` не тронут (добавлен только метод
  `recent`); `LAYER_ORDER` не тронут; окно осталось 10.
- Перечитывание debug_plan.md перед следующим этапом: ✅ выполнено (2026-09-21)
- Гейт D3→D4: ✅ пройден (grep-контроли чисты; L2 76 / L3 / L4 8 / гейт 12/12;
  состав промта = D1; контракт MemoryLayer не изменён)

---

## Этап D4 — Документация (2026-09-21)

- Статус: ✅ завершён
- Закрыто: **B4**
- Было: README описывал пересечение `deliver ∩ LAYER_ORDER`, флаги (11), дефолт
  `--deliver` «все 4», расхождения spec/кода и косметику как открытые; arch —
  флаги (11), без `DELIVERABLE`; критерий 27 в `Проверка.md` — только юнит-тест.
- Стало: README — §«Сборка промта» дополнен `DELIVERABLE` (достижимость `summary`
  из CLI, бюджет в рабочем пути); флаги **12** (+`--budget`), дефолт `--deliver` —
  полный канонический набор, дефолты цен — из констант; агентный цикл —
  `deliver ∩ DELIVERABLE`, `build(..., budget=prompt_budget)`; §«Что
  зафиксировано» + факт дебага Дня 14 (баг-фикс A1); §«Шероховатости» — расхождения
  и косметика помечены «закрыты в дебаге Дня 14» (ссылки на журнал); таблица
  файлов + `debug_plan.md`/`debug_log.md`. arch — §2.7: пересечение по
  `DELIVERABLE` (не `LAYER_ORDER`); §2.12: флаги 12 (+`--budget`); дерево —
  `prompt_builder.py` с `DELIVERABLE`. `Проверка.md` — критерий 27: проверка из
  CLI (`test_agent_deliver_keeps_invariants` + дефолтный `--deliver`).
- Проверка:
  - grep-страховка: упоминания `LAYER_ORDER` в README/arch — только корректные
    (описание слоёв памяти и история бага A1), пересечение описано по
    `DELIVERABLE`; «Флаги (12)»/«Флаги запуска — 12» — на месте;
  - контрольный прогон (критерий 27 из CLI): дефолтный запуск с инвариантом →
    блок «Обязательные инварианты:» виден (1 вхождение);
  - регрессия: L2 → **76 OK, 0 FAIL**; L3 → SMOKE OK; L4 → 8/8; гейт → **12 из 12**.
- Артефакты: `README.md`, `arch_den_14.md`, `dev/Проверка.md`.
- Спорное/риски: счётчик флагов в README/arch изменён 11→12 — согласовано с
  добавлением `--budget` (D2); критерий 31 (опциональный) не трогал.
- Перечитывание debug_plan.md перед следующим этапом: ✅ выполнено (2026-09-21)
- Гейт D4→D5: ✅ пройден (доки синхронны с кодом; критерий 27 проверяем из CLI;
  регрессия зелёная)

---

## Этап D5 — Артефакты процесса (2026-09-21)

- Статус: ✅ завершён
- Закрыто: **B1, B2, B3**
- Было: артефакты дня 12 (`run_log.md`, `final_report.md`,
  `stages/stage_00…stage_E_person.md`) лежали как «актуальные»; `scen_1.md` —
  сценарий Дня 11/12; `migr_log.md` — заглушки («Этап 1 — …»), дубль «Этапа 0»
  (таблица «Подшаги Этапа 0» + блок с пустыми «Проверки» + обрыв «**Найдено
  расхождений:»).
- Стало: **(1)** уникальные факты перенесены ДО архивирования: карточка
  `errors/error_2026-09-18_api_key_stdout.md` (инцидент `API_KEY` в stdout при
  `bash -x`, из `final_report.md` §5) + указатель `archive/README.md` (таблица
  переменных из `run_log.md`, факт отсутствия pytest → `unit_runner.py`, ссылка
  на карточку); **(2)** 12 файлов дня 12 → `dev/logs_reports/archive/` (mv);
  актуальными остались `stages/stage_F_state.md`, `errors/` (4 карточки),
  `migr_log.md`; **(3)** `scen_1.md` переписан на месте под День 14 (add →
  конфликт → отказ → `/check` → `set --yes` → автомат; таблица соответствия
  `Задание_Д14.txt`; замечания честности); **(4)** `migr_log.md` чист: заглушки
  и дубль удалены, факты M0–M8 и «Итог миграции» целы (+ примечание о чистке);
  **(5)** ссылки README/arch согласованы (инцидент → карточка; `stage_E_person`
  → `archive/`; таблица файлов; дерево §3.1 + §3.4 + §4.2 arch).
- Проверка:
  - grep-страховка: битых ссылок на `logs_reports/run_log|final_report|
    stages/stage_00|stage_10_final|stage_E_person` вне `archive/` нет
    (совпадения — только в `debug_plan.md`/`debug_plan_5.md`, описывающих сам
    этап D5, и в `archive/` — корректно);
  - контрольный прогон: L2 → **76 OK, 0 FAIL**; L4 → SCENARIO OK; гейт →
    **12 из 12** — числа те же, что после D3/D4 (код не задет, как и ожидалось).
- Артефакты: `dev/logs_reports/{archive/ (12 файлов + README.md),
  errors/error_2026-09-18_api_key_stdout.md}`, `dev/tests_debug/scenario/scen_1.md`,
  `dev/migr_log.md`, `README.md`, `arch_den_14.md`.
- Спорное/риски: `migr_log.md` — удалён черновой блок «Подшаги Этапа 0» с
  числами baseline (40 OK / 6 сценариев / 9 из 9), противоречащими фактическим
  записям M0 (56 OK / 7 / 10 из 10) — это был черновик дубля, фактическая
  базовая линия зафиксирована в M0; `scen_1.md` — путь не менялся (ссылки в
  README «Демонстрации»/«Ручная проверка» актуальны).
- Перечитывание debug_plan.md перед следующим этапом: ✅ выполнено (2026-09-21)
- Гейт D5→D6: ✅ пройден (факты перенесены до архивирования; только актуальное
  + archive/; scen_1 = День 14; migr_log чист; grep чист; L2 76 / L4 8 / гейт
  12/12 — без изменений)

---

## Этап D6 — Финал: полный прогон и приёмка (2026-09-21)

- Статус: ✅ завершён
- Закрыто: — (финальная приёмка; весь реестр A1–A7/B1–B4 закрыт на D1–D5)
- Было: этапы D0–D5 завершены, финальная сверка не проведена.
- Стало: полный прогон зелёный; трассируемость §10 сверена по каждому ID;
  приёмка 30/30; итог дебага зафиксирован (ниже).
- Проверка:
  - **L1** `py_compile` → ok; **L2** → **76 OK, 0 FAIL** (baseline 69 + 7 новых
    тестов: 3 в D1, 1 в D2, 3 в D3); **L3** → SMOKE OK; **L4** → 8 сценариев OK;
    **гейт** → **12 из 12** (FAIL=0); все EXIT 0, без живого ключа;
  - пара «до/после» баг-фикса A1: до (D0) — блок инвариантов в промте CLI
    **0 вхождений** при любом `--deliver`; после — **1 вхождение** при
    дефолтном запуске;
  - трассируемость §10 (grep-сверка по каждому ID): A1 — `DELIVERABLE` в
    `prompt_builder.py` (1) + `Kod.py` (3: импорт + 2 пересечения); A2 — 3
    константы в `llm_client.py`; A3 — `prompt_budget` в `agent.py` (:184
    инициализация, :556 проводка в `build`); A4 — `def identify` → 0; A5 —
    `ROLES` читается в `remember_message` (:570, :572); A6 — `window`/`recent`
    в `short_term.py`, литералов `[-10:]`/`SHORT_TERM_WINDOW` нет; A7 —
    неиспользуемых импортов 0/0/0; B1 — `archive/` 15 файлов (12 артефактов +
    README-указатель + 2 служебных); B2 — `scen_1.md` «День 14 — инварианты…»;
    B3 — заглушек/дублей в `migr_log.md` нет (совпадения — только в примечании
    о чистке); B4 — «Флаги (12)» в README, `DELIVERABLE` в README (6) и arch (2);
  - приёмка: `dev/Проверка.md` — **30 критериев, 30 закрыто ✅** (критерий 27
    теперь проверяем и из CLI).
- Артефакты: `dev/meta_promt/debug_log.md` (итог).
- Спорное/риски: критерий 31 (опциональный, живой смоук LLM) не выполнялся —
  «вне скоупа» дебага (⏸ требует согласия пользователя), приёмку не блокирует.
- Перечитывание debug_plan.md перед следующим этапом: — (финальный этап)
- Гейт (финальный): ✅ пройден
  - [x] полный прогон L1→L2→L3→L4→гейт зелёный, без живого ключа;
  - [x] реестр 11/11 закрыт (A1–A7, B1–B4), трассируемость §10 сверена;
  - [x] приёмка 30/30;
  - [x] пара «до/после» баг-фикса A1 зафиксирована;
  - [x] контракты дней 11–14 не изменены (сигнатуры LLM, MemoryLayer,
        LAYER_ORDER, db.py, state_machine, profile_router, invariants);
  - [x] все записи этапов D0–D6 в журнале по форме §7.4;
  - [x] `debug_plan.md` перечитан после каждого этапа (отметки в записях);
  - [x] предложение коммита сформировано (коммит — только по явной команде).

---

## Итог дебага den_14 (D0–D6)

- **Ключевой баг-фикс (A1)**: `deliver` пересекался с `LAYER_ORDER` (порядок
  слоёв памяти, без `invariants`/`summary`) — блок `[system: invariants]`
  **никогда не попадал в промт в CLI** (критерий 27 фактически не выполнялся).
  Введён канонический набор `DELIVERABLE` в `core/prompt_builder.py`;
  пересечения в `main()`/`/deliver` идут по нему; дефолт `--deliver` полный.
- **Спец/код (A2–A3)**: 3 константы модели/цен в `llm_client.py` (6/6 по
  `ПРОМТ_llm.md`), дефолты argparse — из констант; `budget` проводится в рабочем
  пути (`Agent.prompt_budget` ← `--budget`, default None — выключен; автодефолт
  от `MODEL_CONTEXT_LIMIT` не вводился — запрет §3.3).
- **Косметика (A4–A7)**: окно short_term уникализировано
  (`ShortTermMemory.window` + `recent()`); `ROLES` задействован; `identify()`
  удалён; неиспользуемые импорты вычищены (3 файла).
- **Процесс/доки (B1–B4)**: артефакты дня 12 заархивированы (`archive/` +
  указатель, уникальные факты перенесены до архивирования: карточка
  `error_2026-09-18_api_key_stdout.md`); `scen_1.md` переписан под День 14;
  `migr_log.md` вычищен (факты M0–M8 целы); README/arch/Проверка синхронны с
  кодом (флаги 12, `DELIVERABLE`, критерий 27 из CLI).
- **Тестовый контур**: L2 69 → **76 OK** (+7 тестов: 3/1/3 в D1/D2/D3); L3 OK;
  L4 8/8; гейт 12/12 — все прогоны без живого ключа, временные файлы в `.tmp/`.
- **Приёмка**: 30/30 ✅ (критерий 31 — опциональный, вне скоупа).
- **Наследие не тронуто**: `llm_client.py` (сигнатуры), `memory/*` (контракт
  `MemoryLayer`, `LAYER_ORDER`), `storage/db.py`, `state_machine.py`,
  `profile_router.py`, `core/invariants.py`.
- **Файлы продукта изменены**: `core/prompt_builder.py`, `core/agent.py`,
  `core/llm_client.py`, `memory/short_term.py`, `storage/store.py` (импорт),
  `Kod.py`. **Служебные**: `README.md`, `arch_den_14.md`, `dev/Проверка.md`,
  `dev/migr_log.md`, `dev/logs_reports/{archive/, errors/}`,
  `dev/tests_debug/{scenario/scen_1.md, unit/test_prompt.py,
  unit/test_agent.py, unit/test_memory.py}`, `dev/meta_promt/debug_log.md`.
- **Вне скоупа** (не делалось, осознанно): пустые `fixtures/`/`smoke/`, мусор в
  `.tmp/`, заделы (PolicyEngine, `next_state()`, `parent_id`, скиллы), живой
  смоук LLM (⏸ согласие пользователя), коммит (только по явной команде).

### Предложение коммита (не выполнено — по явной команде пользователя)

```
den_14: дебаг D0–D6 — баг-фикс доставки блоков (DELIVERABLE), константы
модели/цен, бюджет промта, косметика ядра, архив артефактов дня 12

- A1: deliver ∩ DELIVERABLE (не LAYER_ORDER) — блок [system: invariants]
  попадает в промт CLI; дефолт --deliver полный
- A2/A3: MODEL_CONTEXT_LIMIT/PRICE_* в llm_client.py; --budget →
  Agent.prompt_budget → build(budget=...) (default None)
- A4–A7: окно short_term уникализировано (window+recent), ROLES задействован,
  identify() удалён, импорты вычищены
- B1–B4: archive/ дня 12 + указатель + карточка api_key_stdout; scen_1.md →
  День 14; migr_log.md чист; README/arch/Проверка синхронны
- Тесты: L2 69→76 OK; L4 8/8; гейт 12/12; приёмка 30/30
```
