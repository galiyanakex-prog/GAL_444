# archive/README.md — указатель архива исторических артефактов

## Что здесь лежит

Артефакты **Дня 11/12** (создание проекта в `den_12`, перенесены как есть при
копировании `den_12` → `den_13` → `den_14`):

- `run_log.md` — сводный журнал прогонов создания (этапы 0–11);
- `final_report.md` — финальный отчёт Дня 11 (таблица приёмки §7 п.1–19);
- `stage_00_env.md … stage_10_final.md` — отчёты этапов создания;
- `stage_E_person.md` — отчёт Этапа E «Персонализация» (День 12).

## Почему в архиве

Эти файлы описывают состояние **Дня 11/12** и не отражают текущий код `den_14`.
Актуальные источники фактов:

| Факт | Актуальный артефакт |
|---|---|
| День 13 (миграция на arch_den_13, state machine) | `../stages/stage_F_state.md`, `../../migr_log.md` |
| День 14 (мирация на arch_den_14, инварианты, M0–M8) | `../../migr_log.md` |
| День 14 (дебаг, D0–D6) | `../../meta_promt/debug_log.md` |

Архивировано в дебаге Дня 14 (этап D5, `debug_plan_5.md`): уникальные факты
перенесены **до** архивирования (см. ниже), сами файлы не изменялись.

## Уникальные факты, сохранённые из архива

### Таблица переменных проекта (из `run_log.md` §«Переменные», фиксация для аудита)

| Переменная | Значение (вычислено на хосте дня 12) |
|---|---|
| `AI9_ROOT` | /home/u/Документы/111111/Обучение_Курсы/Основной_репозиторий/AI_9 |
| `KOD` | $AI9_ROOT/Nedela_3/den_12 |
| `DEV` | $KOD/dev |
| `MP` | $DEV/meta_promt |
| `LOGS` | $DEV/logs_reports |
| `TST` | $DEV/tests_debug |
| `VENV_ACT` | $AI9_ROOT/.venv/bin/activate |
| `PY` | $AI9_ROOT/.venv/bin/python |
| `MODEL` | stepfun/step-3.5-flash |

Хост-порядок: пути выведены в сессии дня 12; на другом хосте вычисляются заново
(хардкод запрещён). Литералы — фиксация для аудита.

### Отсутствие pytest в venv (из `run_log.md` Этап 0)

`pytest` в venv недели **отсутствует** (и не устанавливается — venv недели не
должен зависеть от лишних пакетов). Юнит-уровень L2 выполняется через
`dev/tests_debug/unit_runner.py` — допустимая альтернатива.

### Инцидент безопасности (из `final_report.md` §5)

Утечка реального `API_KEY` в stdout при `bash -x check_acceptance.sh`
(2026-09-18) → карточка
`../errors/error_2026-09-18_api_key_stdout.md` (создана в D5 при переносе).
