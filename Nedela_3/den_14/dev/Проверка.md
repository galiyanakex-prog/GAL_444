# Проверка.md — чек-лист приёмки дня 14 (человеческая версия)

Артефакт процесса: живёт **только в `dev/`** (в корне дня файла нет — критерий 19).
Автоматический чекер — `dev/tests_debug/check_acceptance.sh`
(запуск: `cd "$KOD" && API_KEY=test-key bash "$TST/check_acceptance.sh"`).

Обозначения: ✅ — зелёный · 🟡 — задел (не обязателен) · — критерий дня 14.

## Память и персонализация (наследие дней 11–12, без регрессии)

| Пункт | Что проверяем | Как проверить / команда | Статус |
|---|---|---|---|
| 1 | Комплектность и сборка | `$PY -m py_compile Kod.py core/*.py memory/*.py storage/*.py`; есть README.md, run.sh (+x), run.desktop | ✅ |
| 2 | 3 типа памяти + профиль — разные классы/файлы | `unit/test_memory.py::test_four_layers_present` | ✅ |
| 3 | Иерархия хранилища по канону | `unit/test_storage.py` (иерархия + profiles.db + session.json) | ✅ |
| 4 | Явная маршрутизация (лог «что куда») | `unit/test_memory.py::test_remember_routes_explicitly`; журнал `[Память] … ← …` | ✅ |
| 5 | Дозированная доставка | `unit/test_prompt.py::test_dosed_delivery_cut_layer`; `/deliver` | ✅ |
| 6 | Идентификация на старте | `--user` или ввод до загрузки памяти (smoke) | ✅ |
| 7 | Интервью-инициализация нового ID | `unit/test_agent.py::test_interview_initialization`; `scenario.py::scenario_interview` | ✅ |
| 8 | Понятие таски + переход | `unit/test_agent.py::test_switch_task`; `/tasks`, `/task` | ✅ |
| 9 | Промт явными блоками | `unit/test_prompt.py::test_blocks_order` | ✅ |
| 10 | LLM за интерфейсом (ABC + RouterAI + Mock) | `unit/test_llm.py` | ✅ |
| 11 | Задел state machine | `unit/test_state.py` | ✅ |
| 12 | Resume | `unit/test_agent.py::test_resume_known_user`; `scenario.py::scenario_resume` | ✅ |
| 13 | Неизменяемые сообщения с parent_id; рабочая ≠ лог | `unit/test_memory.py::test_short_term_append_and_parent` | ✅ |
| 14 | Демонстрации задания | `/compare` (влияние на ответы); `scenario.py` (маршрутизация, доставка, интервью, resume) | ✅ (manual) |
| 15 | Текстовое описание модели памяти | `README.md` — раздел «Модель памяти» | ✅ |
| 16 | Уроки дня 10 перенесены | `Kod.py`: errors="replace", readline, токен-учёт+CSV; `llm_client.py`: retry 429; BASE_DIR | ✅ |
| 17 | Тесты без живого ключа | L2/L3/L4 через `API_KEY=test-key` и `MockClient` | ✅ |
| 18 | Задел фильтра инвариантов | `PolicyEngine` + `visibility`/`scope` в `memory/base.py`; в Дне 14 **задействован** | ✅ |
| 19 | Проверка.md только в `dev/` | `test ! -e "$KOD/Проверка.md"` | ✅ |

## Персонализация (день 12, без регрессии)

| Пункт | Что проверяем | Как проверить / команда | Статус |
|---|---|---|---|
| 20 | Несколько профилей («призм») | `unit/test_person.py::test_multi_profile_roundtrip`; `/profile list|new` | ✅ |
| 21 | Миграция и обратная совместимость | `unit/test_person.py::test_migration_old_schema`, `test_backward_compatibility` | ✅ |
| 22 | Роутер и авто-выбор профиля | `unit/test_person.py::test_router_triggers_and_none`, `test_agent_auto_route_switches_before_prompt` | ✅ |

## Состояние задачи (день 13, без регрессии)

| Пункт | Что проверяем | Как проверить / команда | Статус |
|---|---|---|---|
| 23 | Формализованное состояние (этап/шаг/действие) | `unit/test_fsm.py`; `/state`, `/plan`, `/step` | ✅ |
| 24 | Пауза на любом этапе + продолжение без повторных объяснений | `unit/test_fsm.py::test_pause_and_resume`; `scenario.py::scenario_pause_resume` | ✅ |
| 25 | Персистентность и жизненный цикл (save/load, перезапуск) | `unit/test_fsm.py::test_save_load_roundtrip`, `test_restart_continues_from_saved_step` | ✅ |

## Инварианты и ограничения состояния (день 14) — критерии 26–30

| Пункт | Что проверяем | Как проверить / команда | Статус |
|---|---|---|---|
| 26 | Инварианты хранятся **отдельно от диалога** | `invariants.json` существует; очистка `session.json` не удаляет набор (`unit/test_invariants.py::test_clearing_session_keeps_invariants`); гейт №11 | ✅ |
| 27 | Ассистент **явно учитывает** инварианты в рассуждениях | блок `[system: invariants]` в промте (`unit/test_invariants.py::test_invariants_block_in_prompt`; виден в `/deliver`); правило роли «не предлагай нарушающих решений» | ✅ |
| 28 | Ассистент **отказывается** предлагать решения, нарушающие инварианты | `InvariantChecker.check` → нарушения → действие не выполняется, инструмент не вызван (`test_forbidden_action_does_not_call_tool`); `/check`; гейт №12 | ✅ |
| 29 | **Конфликт** запроса и инварианта + объяснение отказа | `scenario.py::scenario_invariant_conflict`: отказ называет `id` инварианта, причину и альтернативу (`test_refusal_names_invariant`, `test_refusal_offers_alternative`) | ✅ |
| 30 | Изменение инварианта требует **отдельного подтверждения** | `unit/test_invariants.py::test_update_requires_confirmation` (`PermissionError` без авторизации); `/invariant set <id> <текст> --yes` меняет правило | ✅ |

Итог: обязательные пункты — **30 из 30 зелёные** (19 прежних + 20–22 персонализация +
23–25 автомат + 26–30 инварианты). Автоматический дубль — `check_acceptance.sh` (**12 из 12**).

Машиночитаемый прогон без живого ключа: L2 (69 OK) → L3 (SMOKE OK) → L4 (8 сценариев) → гейт (12/12).
