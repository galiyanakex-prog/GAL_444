# migr_plan_0.md — рабочий план этапа M0 «Базовая линия и инвентаризация»

> **Роль документа.** Рабочий план-алгоритм **одного этапа** миграции проекта `den_15`
> на целевую архитектуру `Nedela_3/den_15/arch_den_15.md`. Разворачивает этап **M0** из
> `migr_plan.md` §4 в конкретные команды, правки и ожидаемые результаты.
> **Конец этого этапа — автоматический гейт в этап M1** (`migr_plan_1.md`).
> Источники: `migr_plan.md` (эталон), `arch_den_15.md` (целевое состояние),
> `Задание_Д15.txt` (неизменяемый первоисточник).

---

## 0. Паспорт этапа

| Поле | Значение |
|---|---|
| Этап | **M0** — Базовая линия и инвентаризация |
| Рабочий план | `dev/migr_plan_0.md` (этот файл) |
| Зависит от | — (стартовый этап) |
| Открывает | `dev/migr_plan_1.md` (M1 — Ядро: состояния, карта, API контроля) |
| Тип изменений | **Только чтение/прогон** — код проекта не меняется |
| Живой ключ | **Не нужен** (`API_KEY=test-key` / `MockClient`) |
| Точка отката | Исходное состояние репозитория (этап ничего не ломает) |

**Цель этапа.** Зафиксировать текущее состояние `den_15` (копия `den_14` после дебага
D0–D6) и убедиться, что наследие зелёное **до** изменений; перечислить расхождения с
`arch_den_15.md` §2.1 и зафиксировать точки внедрения, которые менять нельзя.

**Факт (2026-09-22):** этап **уже исполнен** в момент создания процессной документации
(план-эталон `migr_plan.md` + журнал `migr_log.md` создавались по итогам прогонов);
запись M0 в `migr_log.md` заполнена. Этот план фиксирует выполненные шаги для
трассируемости.

---

## 1. Вход и предусловия

- Текущее дерево `Nedela_3/den_15/` (копия `den_14` после дебага D0–D6: L2 76 OK,
  L4 8/8, гейт 12/12 в исходнике).
- `Nedela_3/den_15/arch_den_15.md` (§2.1 — дерево модулей, §2.13 — неизменяемое
  наследие).
- `.venv` недели (`source ../../.venv/bin/activate`).
- `dev/tests_debug/` (существующий контур: `unit_runner.py`, `smoke.py`,
  `scenario.py`, `check_acceptance.sh` — 12 проверок).
- `dev/migr_log.md` (журнал — сюда пишется результат этапа).

**Предусловия:** ничего не коммитить; `users/` проекта не трогать.

---

## 2. Шаги этапа (исполняемый алгоритм)

### Шаг 0.1 — Активировать окружение и зафиксировать точку старта
1. `cd Nedela_3/den_15 && source ../../.venv/bin/activate`; `python --version`
   (ожидание: Python 3.13.5).
2. Зафиксировать дерево: `find core memory storage -maxdepth 1 -type f | sort`.
3. **Ожидаемый результат:** окружение активно; дерево = 18 модулей + `Kod.py`
   (наследие дней 11–14 полное).

### Шаг 0.2 — Сверить фактическое дерево с `arch_den_15.md` §2.1
1. Сопоставить построчно; составить таблицу «есть / нет / отличается».
2. Ожидаемо **нет** (закрывается M1–M6): `NEW`/`PLAN_APPROVED`/`IMPLEMENTATION` в
   `TaskStage`; `can_transition`/`try_transition`/`InvalidTransitionError`;
   `REFUSAL_RULES`; `transition_log`; `approve_plan`; миграция снимков `"execution"`;
   `Agent.approve_plan`/`attempt_transition`/`transitions_report`; CLI `/approve`,
   `/goto`, `/transitions`; `unit/test_transitions.py`;
   `scenario_controlled_transitions`; гейт 15; `README.md` (отсутствует в копии);
   строки 31–35 `Проверка.md`; `scen_1.md` Дня 15.
3. **Ожидаемый результат:** таблица расхождений заполнена (в `migr_log.md` M0).

### Шаг 0.3 — Зафиксировать точки внедрения (не менять)
Перечислить неизменяемые контракты дней 11–14 (по `arch_den_15.md` §2.13):
`llm_client.py`, `memory/*` (кроме зеркала `current_state` в `working.py` — M2),
`storage/db.py`, `profile_router.py`, `invariants.py`, `prompt_builder.py`.
**Ожидаемый результат:** список зафиксирован в `migr_log.md` M0.

### Шаг 0.4 — Регрессионный baseline (без живого ключа)
```bash
export API_KEY=test-key
python -m py_compile Kod.py core/*.py memory/*.py storage/*.py   # L1
env -u API_KEY python dev/tests_debug/unit_runner.py             # L2
python dev/tests_debug/smoke.py                                  # L3
python dev/tests_debug/scenario.py                               # L4
bash dev/tests_debug/check_acceptance.sh                         # гейт (12 проверок)
```
**Ожидаемый результат:** L1 ok; L2 **76 OK, 0 FAIL**; L3 SMOKE OK; L4 **8/8**; гейт
**10 из 12** — 2 красные ([2] «README непустой», [7] «README содержит раздел модели
памяти») **только из-за отсутствия `README.md` в корне копии**; код и тесты зелёные.
Отклонение ⚠ зафиксировать в `migr_log.md` (закрывается M6).

### Шаг 0.5 — Проверить работоспособность запуска
`bash run.sh --help` (или безопасный неинтерактивный вызов) — без ошибок импорта.

### Шаг 0.6 — Составить перечень расхождений для M1–M6
Таблица «что отсутствует → каким этапом закрывается» (трассировка `migr_plan.md` §9) —
в `migr_log.md` M0.

---

## 3. Выход этапа

- Отчёт-сверка дерева (таблица «есть / нет / отличается»).
- Baseline: L2 76 / L3 OK / L4 8 / гейт 10+2⚠ (с причиной).
- Список точек внедрения (неизменяемые контракты).
- Перечень расхождений с привязкой к M1–M6.
- Запись этапа **M0** в `dev/migr_log.md`.

---

## 4. Автоматический гейт M0→M1

Гейт считается **зелёным**, если одновременно:
- [x] baseline зафиксирован (L2 76 / L3 OK / L4 8 / гейт 10+2⚠ с причиной ⚠ README);
- [x] расхождения с `arch_den_15.md` §2.1 перечислены;
- [x] точки внедрения (неизменяемые контракты) зафиксированы;
- [x] регрессия до изменений подтверждена (код/тесты зелёные);
- [x] `run.sh` работает; окружение активируется.

**Зелёный** → запись M0 в `migr_log.md` (✅) → **перечитать `migr_plan.md`** → открыть
`migr_plan_1.md`.
**Красный** → карточка ошибки `dev/logs_reports/errors/error_<ts>.md`, этап M0 открыт.

---

## 5. Запись в `migr_log.md` (форма §6.4 `migr_plan.md`)

Заполнена в `migr_log.md` (этап исполнен 2026-09-22 при создании процессной
документации).

---

## 6. Следующий шаг

Перечитать актуальный `dev/migr_plan.md`, затем приступить к **M1** по
`dev/migr_plan_1.md` (модернизация `core/state_machine.py`: TaskStage 8, карта,
`can_transition`/`try_transition`/`InvalidTransitionError`, `REFUSAL_RULES`,
`transition_log`, `approve_plan`, новый `run_task`).
