# migr_plan_7.md — рабочий план этапа M7 «Финал: прогон и приёмка»

> **Роль документа.** Рабочий план-алгоритм **одного этапа** миграции проекта
> `den_15` на целевую архитектуру `Nedela_3/den_15/arch_den_15.md`. Разворачивает
> этап **M7** (финальный) из `migr_plan.md` §4. Закрывает приёмку
> (`arch_den_15.md` §4.3, §5.2) и итоговую запись.
> **Конец этого этапа — финальный гейт миграции** (DoD `migr_plan.md` §7).
> Источники: `migr_plan.md` (эталон), `arch_den_15.md` §4–6, `Задание_Д15.txt`.

---

## 0. Паспорт этапа

| Поле | Значение |
|---|---|
| Этап | **M7** — Финал: прогон и приёмка (финальный) |
| Рабочий план | `dev/migr_plan_7.md` (этот файл) |
| Зависит от | **M0–M6** (все этапы) |
| Открывает | — (миграция завершается; далее — только явная команда пользователя) |
| Закрывает | `arch_den_15.md` §4.3 (критерии 31–35 + нерегрессия 30), §5.2 (финал), §6 (итог) |
| Основные артефакты | `dev/migr_log.md` (итог + предложение коммита) |
| Живой ключ | **Не нужен** (`API_KEY=test-key` / `MockClient`) |
| Меняет поведение | **Нет** (только прогон и записи) |

**Цель этапа.** Полный прогон всех проверок, приёмка 35/35, сверка трассируемости
`migr_plan.md` §9 по каждому разделу arch, итоговая запись «Итог миграции» в
`migr_log.md`, предложение коммита (без выполнения).

---

## 1. Вход и предусловия

- Реализация M1–M6 зелёная; гейт 15/15; L2 0 FAIL; L4 9/9.
- `dev/Проверка.md` — 35 критериев; `README.md` — актуален.
- Все записи M0–M6 в `migr_log.md` — ✅.

**Предусловия:** ничего не коммитить; `users/` не трогать; живой смоук LLM — вне
скоупа (⏸ согласие пользователя, приёмку не блокирует).

---

## 2. Шаги этапа (исполняемый алгоритм)

### Шаг 7.1 — Полный прогон (без живого ключа)
```bash
cd Nedela_3/den_15 && source ../../.venv/bin/activate && export API_KEY=test-key
python -m py_compile Kod.py core/*.py memory/*.py storage/*.py   # L1
env -u API_KEY python dev/tests_debug/unit_runner.py             # L2
python dev/tests_debug/smoke.py                                  # L3
python dev/tests_debug/scenario.py                               # L4 (9)
bash dev/tests_debug/check_acceptance.sh                         # гейт (15)
```
**Ожидаемый результат:** L1 ok; L2 **0 FAIL** (все модули, вкл.
`test_transitions.py`); L3 SMOKE OK; L4 **9/9**; гейт **15 из 15**; все EXIT 0.

### Шаг 7.2 — Приёмка 35/35
1. Прогнать `dev/Проверка.md` построчно (команда проверки каждого критерия);
   зафиксировать 31–35 зелёные + прежние 30 без регрессии.
2. Демонстрация канона задания (ручной прогон в `.tmp/`): попытки недопустимых
   переходов (`/goto implementation` из planning; `/goto done` из implementation) —
   отказ + состояние не меняется + запись в журнале; реакция ассистента (правило +
   корректный шаг); продолжение после паузы (перезапуск → resume → тот же шаг).
**Ожидаемый результат:** 35/35 ✅.

### Шаг 7.3 — Сверка трассируемости (migr_plan.md §9)
Grep/факт-сверка по каждому разделу `arch_den_15.md` §2.2–2.13, §3.2, §4.1–4.3 →
этап M1–M6 (таблица «раздел → факт в коде/доках → этап»). Особо:
- §2.3 API: `can_transition`/`try_transition`/`InvalidTransitionError` — имена из
  задания;
- §2.4 запреты: `PLANNING → IMPLEMENTATION` и `* → DONE` — дуг нет (grep карты);
- §2.5: `run_task` из `planning` останавливается;
- §2.7: `transition_log` в снимке;
- §2.8: миграция `"execution"`;
- §2.9: `PAUSED: set()` + `resume_task` → `previous_stage`;
- §2.13: контракты дней 11–14 не тронуты (сверка сигнатур/импортов).
**Ожидаемый результат:** каждый раздел закрыт; расхождений нет.

### Шаг 7.4 — Итоговая запись в `migr_log.md`
1. Запись этапа M7 (форма §6.4).
2. Блок «Итог миграции den_15 → arch_den_15.md»: что сделано (M0–M7, таблица),
   ключевые решения (whitelist-карта; утверждение плана как стадия; пауза — не
   лазейка; миграция `execution`; `next_state()` → рабочий API), тестовый контур
   (L2/L3/L4/гейт 15/15, приёмка 35/35), изменённые файлы, наследие не тронуто.
3. **Предложение коммита** (текст сообщения; коммит — только по явной команде
   пользователя).
**Ожидаемый результат:** журнал полный; DoD `migr_plan.md` §7 — все 7 пунктов ✅.

---

## 3. Выход этапа

- Итоговая запись + «Итог миграции» + предложение коммита в `dev/migr_log.md`.
- Приёмка 35/35 (отметки в `dev/Проверка.md`).

---

## 4. Финальный гейт (DoD `migr_plan.md` §7)

Миграция завершена, если одновременно:
- [ ] целевые артефакты `arch_den_15.md` §4.1 созданы (модернизированный
      `state_machine.py`, `transition_log` в снимках, README с разделом);
- [ ] критерии 31–35 зелёные; прежние 30 — без регрессии (35/35);
- [ ] гейт 15/15; L2 0 FAIL; L4 9/9; L3 OK — без живого ключа;
- [ ] этапы M0–M7 в `migr_log.md` — ✅; открытых ошибок нет;
- [ ] `migr_plan_0.md … migr_plan_7.md` созданы и исполнены;
- [ ] `migr_plan.md` перечитан после каждого этапа (фиксации в записях);
- [ ] предложение коммита сформировано (коммит — только по явной команде).

**Зелёный** → миграция завершена; пользователю — итог + предложение коммита.
**Красный** → карточка ошибки, этап M7 открыт.

---

## 5. Запись в `migr_log.md` (форма §6.4 + итог)

```text
## Этап M7 — Финал: прогон и приёмка
- Статус: ✅ завершён
- Было: реализация M1–M6 зелёная по гейтам этапов
- Стало: полный прогон зелёный; приёмка 35/35; трассируемость сверена; итог
  зафиксирован
- Проверка: <L1/L2/L3/L4/гейт 15/15; Проверка.md 35/35; демонстрация канона>
- Артефакты: dev/migr_log.md (итог)
- Спорное/риски: <живой смоук LLM — ⏸ вне скоупа, согласие пользователя>
- Перечитывание migr_plan.md перед следующим этапом: — (финальный этап)
- Гейт (финальный): ✅ пройден

## Итог миграции den_15 → arch_den_15.md
<таблица M0–M7; ключевые решения; контур; файлы; наследие; предложение коммита>
```

---

## 6. Следующий шаг

Миграция завершена. Дальнейшие действия — только по явной команде пользователя:
коммит (рекомендуемое сообщение — в `migr_log.md`), живой смоук LLM (⏸ согласие),
либо переход к следующему дню недели.
