# migr_log.md — журнал выполнения миграции `den_15` → `arch_den_15.md`

> Журнал результатов миграции по рабочим планам `dev/migr_plan_0.md` … `dev/migr_plan_7.md`.
> План-эталон: `dev/migr_plan.md` (формат записей — §6.4, правила повтора — §6.2).
> Обозначения: ✅ выполнено · ❌ не выполнено (повтор) · ⚠ допустимое временное отклонение ·
> ⛔ блокирующая ошибка (ожидание указаний).

---

## Этап M0 — Базовая линия и инвентаризация (2026-09-22)

- Статус: ✅ завершён
- Было: `den_15` — копия `den_14` после дебага D0–D6; `arch_den_15.md` создан;
  `dev/migr_plan.md`/`migr_log.md` отсутствовали; baseline неизвестен.
- Стало: baseline зафиксирован; расхождения с `arch_den_15.md` §2.1 перечислены
  (таблица ниже); точки внедрения (неизменяемые контракты) зафиксированы; план-эталон
  `dev/migr_plan.md` и этот журнал созданы.
- Проверка:
  - L1 `py_compile Kod.py core/*.py memory/*.py storage/*.py` → ok;
  - L2 `env -u API_KEY .../unit_runner.py` → **76 OK, 0 FAIL** (EXIT 0);
  - L3 `smoke.py` → **SMOKE OK** (EXIT 0);
  - L4 `scenario.py` → **8 сценариев OK** (EXIT 0);
  - гейт `check_acceptance.sh` → **10 из 12** (FAIL=2): [2] «README непустой» и
    [7] «README содержит раздел модели памяти» — **только из-за отсутствия
    `README.md` в корне копии** (код и тесты зелёные); допустимое временное
    отклонение ⚠, закрывается этапом M6 (документация) — зафиксировано в
    `migr_plan.md` §3/§6.3.
- Артефакты: `dev/migr_plan.md` (новый), `dev/migr_log.md` (новый); изменённых
  файлов продукта нет (этап read-only).
- Спорное/риски: R1 (README в копии) — см. выше, закрывается M6; R2 (гейт до M5
  содержит 12 проверок, после M5 — 15) — счёт гейта меняется на M5, ожидаемо.

### Таблица расхождений «текущее → целевое» (для M1–M6)

| Целевое (`arch_den_15.md`) | Текущее | Этап |
|---|---|---|
| `TaskStage` — 8 состояний (`NEW`, `PLAN_APPROVED`, `IMPLEMENTATION`…) | 6 состояний (`EXECUTION`, без `NEW`/`PLAN_APPROVED`) | **M1** |
| `ALLOWED_TRANSITIONS` — whitelist с `PLAN_APPROVED`; из `PLANNING` нет дуги в реализацию | карта дня 13/14 (`PLANNING → EXECUTION` напрямую) | **M1** |
| `can_transition` / `try_transition` / `InvalidTransitionError` | только `transition() -> bool` + `next_state()` (задел) | **M1** |
| `REFUSAL_RULES` (тексты правил отказов) | нет | **M1** |
| `TaskState.transition_log` + `_log_transition` | нет | **M1** |
| `approve_plan()` (planning → plan_approved) | нет | **M1, M3** |
| `run_task` останавливается в `planning` (утверждение — человек) | `run_task` сразу переходит в реализацию | **M1, M3** |
| Миграция снимков `"execution"` → `implementation`; `transition_log` в JSON | нет миграции | **M1, M2** |
| Зеркало `current_state` = `implementation` | `execution` | **M2** |
| `Agent.approve_plan` / `attempt_transition` / `transitions_report` | нет | **M3** |
| Правило роли «не перепрыгивай этапы» в промте | нет | **M3** |
| CLI `/approve`, `/goto`, `/transitions`; `/plan`/`/state` адаптация | нет | **M4** |
| `unit/test_transitions.py`; адаптация `test_fsm.py` | нет | **M5** |
| `scenario_controlled_transitions` (9-й сценарий) | 8 сценариев | **M5** |
| Гейт 15 проверок (+3 переходных) | 12 проверок | **M5** |
| `README.md` (+ раздел «Контролируемые переходы») | **отсутствует в копии** | **M6** |
| `dev/Проверка.md` строки 31–35 | 30 строк | **M6** |
| `scen_1.md` под День 15 | сценарий Дня 14 | **M6** |

### Точки внедрения (НЕ менять — контракты дней 11–14, `arch_den_15.md` §2.13)
- `core/llm_client.py` (`LLMClient`/`RouterAIClient`/`MockClient` + константы);
- `memory/*` (`MemoryLayer`, `MemoryManager`, 4 слоя; касание — только зеркало
  `current_state` в `working.py`, M2);
- `storage/db.py` (`ProfileRepository`);
- `core/profile_router.py` (`ProfileRouter`);
- `core/invariants.py` (`ConstraintSet`/`InvariantChecker`/`RuleBasedChecker`);
- `core/prompt_builder.py` (`BLOCK_ORDER`/`DELIVERABLE`/`build(ctx, deliver, budget)`).

- Перечитывание migr_plan.md перед следующим этапом: ✅ выполнено (2026-09-22)
- Гейт M0→M1: ✅ пройден (baseline зафиксирован: L2 76 / L3 OK / L4 8 / гейт 10+2⚠
  с зафиксированной причиной; расхождения перечислены; контракты зафиксированы)

---

## Этап M1 — Ядро: состояния, карта, API контроля (2026-09-22)

- Статус: ✅ завершён
- Было: `TaskStage` — 6 (без `NEW`/`PLAN_APPROVED`, с `EXECUTION`); карта с прямой
  дугой `PLANNING → EXECUTION`; `transition() -> bool` + задел `next_state()`;
  `run_task` сразу ведёт в реализацию; `TaskState` — 9 полей (без журнала);
  `pause_task` без контроля стадий; `load_state` без миграции.
- Стало: `core/state_machine.py` — строгая машина состояний канона Дня 15:
  `TaskStage` — 8 (`NEW`, `PLANNING`, `PLAN_APPROVED`, `IMPLEMENTATION` (бывший
  `execution`, алиас-миграция `STAGE_ALIASES`), `VALIDATION`, `DONE`, `PAUSED`,
  `FAILED`); `ALLOWED_TRANSITIONS` — whitelist (из `PLANNING` нет дуги в
  `IMPLEMENTATION`; в `DONE` только из `VALIDATION`; `PAUSED`/`DONE` — пустые);
  `can_transition(from, to) -> bool`; `InvalidTransitionError(current, proposed)`
  (объясняет правило + список разрешённых); `try_transition(state, proposed)` —
  единственная точка смены этапа (отказ → исключение, состояние НЕ меняется,
  попытка в журнал `allowed=False`); `REFUSAL_RULES` + `refusal_text()` —
  детерминированные тексты отказов (правило + корректный шаг, без LLM);
  `TaskState.transition_log` (10-е поле) + `_log_transition` (append-only,
  `{from,to,allowed,reason,at}`); `to_dict`/`from_dict` (миграция `"execution"`
  → `IMPLEMENTATION`, без журнала → `[]`, без stage → `NEW`); `approve_plan()`
  (planning → plan_approved, идемпотентные сообщения из других стадий);
  `pause_task` (разрешена из 5 рабочих стадий, из DONE/FAILED — отказ с записью
  в журнал); `resume_task` (возврат в `previous_stage`, fallback
  `IMPLEMENTATION`); `run_task` — новый поток (new → planning + план →
  **остановка на утверждении**; plan_approved → implementation (первый шаг тем
  же проходом); шаги → validation → done|failed); `transition()` — обёртка над
  `try_transition` (обратная совместимость). Callers: `core/agent.py` —
  `start_task` (stage=NEW), `run_to_end` (останавливается в planning + лог
  «план ожидает утверждения»), `pause`/`resume` (лог-параметр), `retry_task`
  (expected_action = утверждение), `load_task_state`/`_persist_task_state`
  (через `to_dict`/`from_dict`), + новые методы `approve_plan()` /
  `attempt_transition(target)` / `transitions_report()`; `Kod.py` — роль
  «не перепрыгивай этапы», команды `/approve`, `/goto <этап>`, `/transitions`,
  `/plan` (остановка на утверждении), `/state` (разрешённые переходы + счётчик
  отказов), `/help`; `memory/working.py` — зеркало `implementation`/
  `plan_approved`; `storage/store.py` — дефолт зеркала `new`, докстринг
  миграции.
- Проверка:
  - L1 `py_compile` → ok (после фикса кавычки в `/goto`-сообщении);
  - примитив-смоук `dev/tests_debug/m1_smoke.py` → **9/9 блоков OK** (EXIT 0):
    can_transition, try_transition-отказ, полный поток с утверждением,
    остановка `/run`, миграция снимков, пауза/resume, обёртка, round-trip,
    refusal_text;
  - L2 `unit_runner.py` → **77 OK, 0 FAIL** (адаптация `test_state.py` — 8
    состояний/can_transition; `test_fsm.py` — новый поток с `/approve`,
    15 тестов; ⚠ M1 «test_fsm красный до M5» **закрыт сразу** — адаптация
    сделана в этом же этапе, +1 тест к 76);
  - L3 `smoke.py` → SMOKE OK (fsm-смоуки адаптированы: `/approve` в потоке);
  - L4 `scenario.py` → 8/8 OK (`scenario_pause_resume` адаптирован под
    `/approve` + проверка `transition_log` в снимке);
  - гейт `check_acceptance.sh` → **10 из 12** (2 ⚠ README [2]/[7] — ожидаемо,
    закрывается M6; проверка [10] обновлена под новый поток: `/approve` в
    прогоне 1 — без него пауза была недостижима, т.к. `/step` из planning
    больше не ведёт в реализацию).
- Артефакты: `core/state_machine.py` (модернизация), `core/agent.py`,
  `Kod.py`, `memory/working.py`, `storage/store.py`;
  `dev/tests_debug/m1_smoke.py` (новый, примитив-смоук этапа);
  `dev/tests_debug/unit/test_state.py`, `unit/test_fsm.py` (адаптация);
  `dev/tests_debug/smoke.py`, `scenario.py` (адаптация fsm-веток);
  `dev/tests_debug/check_acceptance.sh` (проверка [10] под новый поток).
- Спорное/риски: (1) проверка [10] гейта изменена в M1, а не в M5 —
  необходимость вызвана самим ядром (пауза недостижима без `/approve`);
  формально это опережение M5, зафиксировано здесь; (2) `next_state()` удалён
  (задел дней 11–12) — заменён `can_transition`, callers не осталось (grep
  чист); (3) `run_task` из `PLAN_APPROVED` выполняет первый шаг тем же
  проходом — соответствует arch §2.5 («/step | /run → plan_approved →
  implementation: выполнение шагов»).
- Перечитывание migr_plan.md перед следующим этапом: ✅ выполнено (2026-09-22)
- Гейт M1→M2: ✅ пройден (все критерии: карта = arch §2.3 построчно; отказ не
  меняет состояние + запись `allowed=False`; полный поток проходит; `/run` из
  planning останавливается; миграция снимков; L1 зелёный; L2 77/0; гейт
  10+2⚠ без новых красных; контракты дней 11–14 вне автомата не тронуты)

---

## Этап M2 — Хранение: transition_log + миграция снимков (2026-09-22)

- Статус: ✅ завершён
- Было: сериализация снимка — вручную в `agent.py` (`asdict` + конверсия enum),
  без `transition_log`; миграции `"execution"` не было; зеркало `current_state`
  писало `execution`; дефолт зеркала в `store.py` — `planning`.
- Стало: сериализация — `TaskState.to_dict()`/`from_dict()` (включая
  `transition_log` и миграцию `"execution"` → `IMPLEMENTATION` через
  `STAGE_ALIASES`); `agent._persist_task_state`/`load_task_state` — через
  `to_dict`/`from_dict`; `memory/working.py` — зеркало `implementation`/
  `plan_approved` (блок `working` в промте); `storage/store.py` — дефолт
  зеркала `new`, докстринг миграции. Фактически закрыто в M1 (сериализация —
  часть ядра); M2 — доказательство на уровне фасада.
- Проверка: смоук `dev/tests_debug/m2_smoke.py` → **4/4 блока OK** (EXIT 0):
  (1) round-trip `transition_log` через Store+Agent (успехи и отказы, включая
  отклонённый `/goto implementation`); (2) старый снимок дня 14 (`"execution"`)
  → `IMPLEMENTATION`, журнал `[]`, шаг сохранён; (3) битый `task_state.json`
  не роняет приложение (задачи нет → `start_task` со стадии NEW); (4) зеркало
  `current_state` синхронно (`plan_approved` → `implementation`). Регрессия:
  L2 77/0, L3 OK, L4 8/8, гейт 10+2⚠ — без изменений (проверено в M1).
- Артефакты: `dev/tests_debug/m2_smoke.py` (новый, смоук этапа); правки
  хранения сделаны в M1 (`core/state_machine.py` to_dict/from_dict,
  `core/agent.py`, `memory/working.py`, `storage/store.py`).
- Спорное/риски: этап M2 выполнен преимущественно в M1 (сериализация — ядро
  dataclass); решение — оставить как есть, M2 зафиксировал доказательства
  фасада. `db.py` не тронут (контракт).
- Перечитывание migr_plan.md перед следующим этапом: ✅ выполнено (2026-09-22)
- Гейт M2→M3: ✅ пройден (round-trip равен; старый снимок мигрируется; битый
  файл не роняет; зеркало синхронно; L1/L2/L3/L4/гейт — без новых красных)

---

## Этап M3 — Оркестратор: контроль переходов в Agent (2026-09-22)

- Статус: ✅ завершён
- Было: `Agent` без методов контроля переходов; `start_task` сразу планировал
  (`stage=PLANNING`); `run_to_end` крутил до done без остановки на утверждении;
  роль без правила этапов.
- Стало: `Agent.approve_plan()` (делегат `state_machine.approve_plan` +
  персистентность; из неподходящих стадий — отказ с объяснением);
  `Agent.attempt_transition(target)` (парсинг имени стадии, `try_transition`,
  отказ → «[Автомат] Попытка … ОТКАЗАНО / Правило: … / Состояние не изменено»,
  попытка в `transition_log`); `Agent.transitions_report()` (карта с отметкой
  текущей + журнал с счётчиком отказов); `start_task` (stage=NEW);
  `run_to_end` (останавливается в planning + лог «план ожидает утверждения»);
  `retry_task` (expected_action = утверждение); роль в `Kod.py` — «работай в
  рамках текущего этапа задачи, не перепрыгивай этапы; переходы контролирует
  код» (двойная защита). Инварианты дня 14 не тронуты.
- Проверка: REPL-смоук `dev/tests_debug/m34_smoke.py` ветка 2 (пауза →
  перезапуск → resume → done без дублей) + инварианты: `scenario_invariant_conflict`
  зелёный (L4 8/8 в M1); L2 77/0 (`test_agent.py` зелёный без адаптации —
  тесты не завязаны на старый флоу `start_task`).
- Артефакты: `core/agent.py` (методы контроля + адаптация цикла), `Kod.py`
  (роль). Реализовано в M1 (единая правка ядра+оркестратора); M3 зафиксирован
  смоуком.
- Спорное/риски: объединение правок M1+M3 в одном проходе — ядро и его
  оркестратор менялись согласованно (иначе проект не компилировался бы);
  зафиксировано здесь.
- Перечитывание migr_plan.md перед следующим этапом: ✅ выполнено (2026-09-22)
- Гейт M3→M4: ✅ пройден (все критерии: `/goto implementation` из planning →
  отказ с правилом, состояние не изменилось, запись в журнале; `approve` →
  `plan_approved`; `/run` из planning останавливается; pause/resume на новых
  стадиях работают; блок `working` несёт новые стадии; инварианты не тронуты)

---

## Этап M4 — CLI: /approve, /goto, /transitions (2026-09-22)

- Статус: ✅ завершён
- Было: CLI без команд контроля; `/plan` сразу в реализацию; `/state` без
  разрешённых переходов и счётчика отказов; `/help` без новых команд.
- Стало: `Kod.py`: `/approve` (→ `agent.approve_plan()`), `/goto <этап>` (→
  `agent.attempt_transition()`, без аргумента — подсказка со списком стадий),
  `/transitions` (→ `agent.transitions_report()`); `/plan` — печать «план
  ожидает утверждения» (через `expected_action`); `/state` — разрешённые
  переходы из текущей стадии + счётчик отказов; `/help` — 3 новые команды +
  обновлённые описания `/plan`/`/run`; строка списка команд в шапке REPL.
  Унаследованные команды дней 11–14 не тронуты.
- Проверка: REPL-смоук `dev/tests_debug/m34_smoke.py` → **ВСЁ OK** (EXIT 0):
  ветка 1 — `/plan` → `/goto implementation` (ОТКАЗАНО + правило + «Состояние
  не изменено: planning») → `/state` (стадия не изменилась, «Отказов в
  журнале переходов: 1») → `/approve` → `/goto done` (ОТКАЗАНО «нельзя
  завершать без реализации и валидации») → `/run` → done → `/goto new`
  (ОТКАЗАНО, терминальная) → `/transitions` (журнал 8 записей, 3 отказа);
  снимок на диске: `stage=done`, 3 отказа с `reason`; ветка 2 — пауза →
  перезапуск процесса → `/resume` (implementation, шаг 1/3) → `/run` → done,
  3 результата без дублей. Регрессия наследия: `/memory`, `/profile list`,
  `/deliver`, `/invariant add`, `/invariants`, `/check`, `/summary`,
  `/tokens`, `/cost` — работают (прогон в `.tmp/m34_heritage/`). Флаги — 12,
  без изменений.
- Артефакты: `Kod.py`; `dev/tests_debug/m34_smoke.py` (новый, REPL-смоук
  этапа). Реализовано в M1; M4 зафиксирован смоуком.
- Спорное/риски: нет.
- Перечитывание migr_plan.md перед следующим этапом: ✅ выполнено (2026-09-22)
- Гейт M4→M5: ✅ пройден (REPL-смоук канона «Проверьте» проходит полностью;
  ветка паузы с перезапуском; наследие не регрессирует; L1 зелёный; флаги 12)

---

## Этап M5 — Тесты и отладка (2026-09-22)

- Статус: ✅ завершён
- Было: контур дня 14 (76 тестов L2, 8 сценариев L4, гейт 12); адаптации
  `test_state.py`/`test_fsm.py`/`smoke.py`/`scenario.py`/проверки [10] сделаны
  в M1 (иначе проект не компилировался/не проходил); `test_transitions.py`,
  9-й сценарий и 3 проверки гейта — отсутствовали.
- Стало: `unit/test_transitions.py` — **новый, 14 тестов** канона arch §3.2
  (can_transition разрешённые/запрещённые; try_transition не меняет состояние;
  InvalidTransitionError объясняет; полный поток с утверждением; `/run` не
  перепрыгивает утверждение; журнал попыток/отказов; REFUSAL_RULES — правило +
  корректный шаг; пауза/resume с теми же переходами; перезапуск сохраняет
  состояние и журнал; миграция снимка; пауза — не лазейка; карта = arch §2.3
  построчно; approve_plan идемпотентность/отказы); `scenario.py` +
  **`scenario_controlled_transitions`** (9-й, сквозной: ветка 1 — отказы
  `/goto` ×3 + `/approve` + `/run` → done + терминальная + `/transitions`
  «отказов: 3»; ветка 2 — пауза → перезапуск → resume → done без дублей);
  `check_acceptance.sh` — гейт **12 → 15 проверок** ([13] недопустимый переход
  блокируется и логируется; [14] флоу `/approve`; [15] пауза → перезапуск →
  resume, шаг не повторён — счётчик «Результат шага» 1 → 3).
- Проверка: полный прогон — L1 ok; L2 **91 OK, 0 FAIL** (77 + 14); L3 SMOKE
  OK; L4 **9 сценариев OK**; гейт **13 из 15** (2 ⚠ README [2]/[7] — ожидаемо,
  закрываются M6); повторный прогон гейта — тот же результат (идемпотентность);
  всё без живого ключа, временные файлы только в `.tmp/`, `users/` не тронут.
- Артефакты: `dev/tests_debug/unit/test_transitions.py` (новый);
  `dev/tests_debug/scenario.py` (+9-й сценарий); 
  `dev/tests_debug/check_acceptance.sh` (гейт 15).
- Спорное/риски: (1) при отладке [13] найден и исправлен баг паттерна grep —
  `'"allowed": false"'` с лишней закрывающей кавычкой (в JSON после `false`
  идёт запятая); диагностика через `bash -x -c` (внутренний `bash -c` не
  наследует трассировку внешнего `bash -x`); (2) в [15] `grep -c | grep -q`
  заменён на `test "$(grep -c ...)" = "N"` — пайп с `set -e` маскировал
  результат; (3) адаптации `test_state`/`test_fsm`/`smoke`/`scenario`/
  проверки [10] выполнены в M1 (необходимость ядра), здесь — только сверка
  покрытия: прежние проверки дня 13 сохранены (переходы по карте, пауза/
  resume идемпотентность, round-trip, max_passes, неповторение шагов,
  done/failed).
- Перечитывание migr_plan.md перед следующим этапом: ✅ выполнено (2026-09-22)
- Гейт M5→M6: ✅ пройден (все критерии: `test_transitions.py` зелёный (14);
  ⚠ M1–M4 закрыты — L2 0 FAIL; L4 9/9; гейт 15 проверок, 13 зелёных + 2 ⚠
  README с зафиксированной причиной; без живого ключа; идемпотентность)

---

## Этап M6 — Документация (2026-09-22)

- Статус: ✅ завершён
- Было: `README.md` в копии `den_15` отсутствовал (единственная причина ⚠ [2]/[7]
  гейта с M0); `dev/Проверка.md` — 30 строк (день 14); `scen_1.md` — сценарий
  дня 14 (инварианты).
- Стало: **`README.md` создан** (~700 строк) на базе `den_14/README.md` (761 строка,
  актуален после дебага D0–D6): заголовок/шапка/суть — День 15 (контролируемые
  переходы); «Что нового относительно Дня 14» — 10 пунктов (TaskStage 8, карта,
  API can_transition/try_transition/InvalidTransitionError, /approve-флоу,
  REFUSAL_RULES, transition_log, 3 команды, миграция снимков, пауза-не-лазейка,
  тестовый контур); **новый раздел «Контролируемые переходы состояний (День 15)»**
  (модель 8 состояний + таблица 10 полей + семантика expected_action, карта
  whitelist + таблица запретов, API контроля, утверждение плана, отказ с
  объяснением, журнал, персистентность/миграция, пауза/продолжение, инжект в
  промт, пример сессии по канону); счётчики обновлены: состояния 8, команды 23
  (32 формы), сценарии 9, гейт 15, приёмка 35, L2 91 (10 модулей +transitions 14);
  таблица команд +/approve /goto /transitions; «Демонстрации задания» + 5 строк
  контролируемых переходов; «Что зафиксировано» + миграция M0–M7; «Файлы проекта»
  — под дерево den_15 (без битых ссылок копии: meta_promt пуст, журналы дебага —
  в den_13/den_14); «Заделы» + transition_log-рост и судьба m1/m2/m34_smoke;
  «Результат» — контролируемый жизненный цикл. **`dev/Проверка.md`** — строки
  31–35 (явный набор состояний; явная карта; блокировка кодом; реакция ассистента;
  корректность продолжения после паузы — с тестами/гейтами по каждому) + итог
  35/35 + счётчик гейта 15/15 в хвосте. **`scen_1.md` переписан под День 15**
  (6 шагов: интервью → /transitions карта → /plan + /goto implementation отказ +
  task_state.json → /approve + /run → терминальная + пауза/перезапуск/resume →
  инварианты-vs-переходы; таблица соответствия заданию; финальная фраза).
- Проверка: grep-сверка формулировок README с кодом — совпадают
  («утвердить план (/approve)», «начать реализацию (/step или /run)», «Нельзя
  делать реализацию до утверждённого плана», «план уже утверждён»); гейт —
  **15 из 15** (⚠ [2]/[7] закрыты созданием README); L1–L4 без изменений (M5).
- Артефакты: `README.md` (новый, корень); `dev/Проверка.md` (строки 31–35);
  `dev/tests_debug/scenario/scen_1.md` (переписан).
- Спорное/риски: (1) счётчик «32 формы команд» в README — 23 команды верхнего
  уровня + подкоманды /profile (6) + /invariant (3 формы) + /task retry;
  сверено с таблицей; (2) строки модулей в дереве архитектуры README — по
  фактическому `wc -l` на дату записи; (3) пункт «смоуки этапов M1–M4 — судьба
  решена на финальном этапе» в README отсылает к M7 (на момент записи M6 —
  корректно, M7 закроет).
- Перечитывание migr_plan.md перед следующим этапом: ✅ выполнено (2026-09-22)
- Гейт M6→M7: ✅ пройден (README существует и непустой [2] ✅; раздел «Контролируемые
  переходы состояний» с моделью памяти/состояний [7] ✅; Проверка.md 31–35; scen_1.md
  под День 15; гейт 15/15)

---

## Этап M7 — Финал: прогон и приёмка (2026-09-22)

- Статус: ✅ завершён
- Было: все этапы M0–M6 закрыты; гейт 15/15; документация создана; финальный
  прогон и приёмка 35/35 — не выполнены; судьба смоуков этапов — не решена.
- Стало: **полный прогон** — L1 ok; L2 **91 OK, 0 FAIL**; L3 SMOKE OK; L4
  **9 сценариев OK**; гейт **15 из 15** (идемпотентен) — всё без живого ключа.
  **Ручная демонстрация канона задания** (`.tmp/m7_demo.sh`): ветка 1 —
  `/plan` → `/goto implementation` (ОТКАЗАНО, правило, состояние не изменено,
  `/state`: отказов 1) → `/approve` → `/goto done` (ОТКАЗАНО) → `/run` → done →
  `/goto new` (ОТКАЗАНО, терминальная) → `/transitions` (журнал 8 записей,
  отказов 3); ветка 2 — `/plan` → `/approve` → `/step` → `/pause` → перезапуск
  процесса → `/resume` (та же стадия/шаг 1/3, без повторов) → `/run` → done;
  снимок `task_state.json` с `transition_log` на диске. **Трассируемость §9**
  эталона сверена по каждому разделу arch (§2.2–§2.13, §3.2, §4.1–4.3 — все
  артефакты на месте). **Судьба смоуков этапов решена**: `m1_smoke.py`,
  `m2_smoke.py`, `m34_smoke.py` — **остаются** в `dev/tests_debug/` как
  доказательства этапов M1–M4 (запускаются вручную, вне гейта; в дереве
  `arch_den_15.md` §3.1 не перечислены — зафиксировано как допустимое
  отклонение в README «Заделы»).
- Проверка: приёмка **35/35** (критерии 31–35 зелёные + прежние 30 без
  регрессии — L2/L4/гейт покрывают); DoD §7 эталона — все 7 пунктов выполнены
  (артефакты §4.1; 31–35; 30 прежних; гейт 15/15; M0–M7 в журнале ✅;
  рабочие планы 0–7 исполнены; эталон перечитан после каждого этапа).
- Артефакты: `dev/tests_debug/.tmp/m7_demo.sh` (демонстрация канона);
  записи M6/M7 + «Итог миграции» в этом журнале.
- Спорное/риски: (1) смоуки этапов остаются вне дерева arch §3.1 —
  задокументировано; (2) живой смоук — ⏸ отложен (требует явного согласия на
  живые LLM-вызовы; карта переходов детерминирована, проверена на заглушках);
  (3) изменения миграции не закоммичены — коммит только по явной команде
  пользователя.
- Перечитывание migr_plan.md перед следующим этапом: ✅ выполнено (2026-09-22,
  финальный — миграция завершена)
- Гейт M7 (финальный): ✅ пройден (31–35 зелёные и прежние 30 зелёные; гейт
  15/15; артефакты arch §4 присутствуют; все этапы M0–M7 в migr_log.md ✅)

---

## Итог миграции (2026-09-22)

**Миграция `den_14` → `arch_den_15.md` завершена**: все этапы M0–M7 ✅, гейты
пройдены, DoD §7 выполнен полностью.

### Что сделано (дерево)

- `core/state_machine.py` — **переписан**: `TaskStage` 8 состояний (new,
  planning, plan_approved, implementation, validation, done, paused, failed;
  миграция `"execution"` через `STAGE_ALIASES`), `TaskState` 10 полей
  (+`transition_log`), `ALLOWED_TRANSITIONS` (whitelist: из planning нет дуги в
  implementation; в done только из validation; paused/done — пустые),
  `can_transition`/`try_transition`/`InvalidTransitionError`,
  `REFUSAL_RULES`+`refusal_text`, `approve_plan`/`pause_task`/`resume_task`,
  `run_task` с остановкой на утверждении; `next_state()` удалён.
- `core/agent.py` — `approve_plan`/`attempt_transition`/`transitions_report`,
  адаптация start/step/run/pause/resume/retry под новый поток, персистентность
  через `to_dict`/`from_dict`.
- `Kod.py` — команды `/approve`, `/goto <этап>`, `/transitions`; `/plan`,
  `/state`, `/help` адаптированы; правило двойной защиты в SYSTEM_PROMPT.
- `storage/store.py` — миграция снимков `"execution"` → `implementation`;
  `memory/working.py` — зеркало `current_state` → `"new"`.
- Тесты: `unit/test_transitions.py` (новый, 14), `test_state.py`/`test_fsm.py`
  (переписаны), `scenario_controlled_transitions` (L4 → 9), `smoke.py` (fsm-ветки
  с `/approve`), гейт 12 → 15 проверок; смоуки этапов `m1/m2/m34_smoke.py`.
- Документация: `README.md` (создан, раздел «Контролируемые переходы состояний»),
  `dev/Проверка.md` (строки 31–35, итог 35/35), `scen_1.md` (переписан под
  День 15).

### Что проверено

L1 ok / L2 **91 OK, 0 FAIL** / L3 SMOKE OK / L4 **9/9** / гейт **15/15** /
приёмка **35/35** / ручная демонстрация канона — всё без живого ключа,
`users/` проекта не тронуты, временные файлы только в `.tmp/`.

### Контракты дней 11–14 — без регрессии

`llm_client.py`, `memory/*` (кроме зеркала working.py), `storage/db.py`,
`profile_router.py`, `core/invariants.py`, `prompt_builder.py` — не менялись;
прежние 30 критериев приёмки зелёные.

### Спорные пункты (сводно)

1. Правки ядра/оркестратора/CLI слились в один проход M1 (иначе проект не
   компилировался) — этапы M2–M4 выполнены как доказательные смоуки.
2. Баг паттерна гейта [13]: `'"allowed": false"'` с лишней кавычкой (в JSON
   после `false` — запятая); найден через `bash -x -c` (внутренний `bash -c`
   не наследует трассировку внешнего `bash -x`).
3. Смоуки этапов `m1/m2/m34_smoke.py` остаются вне дерева arch §3.1 —
   доказательства этапов, задокументировано.
4. Живой смоук — отложен (явное согласие пользователя).

### Предложение коммита

Изменения миграции готовы к коммиту (коммит — только по явной команде
пользователя). Предлагаемое сообщение:

```
den_15: строгая машина состояний задачи — контролируемые переходы (arch_den_15)

- TaskStage 8 состояний + ALLOWED_TRANSITIONS (whitelist) +
  can_transition/try_transition/InvalidTransitionError + REFUSAL_RULES
- /approve-флоу: план строится и останавливается на утверждении
- transition_log: журнал попыток и отказов, переживает перезапуск
- CLI: /approve, /goto, /transitions; миграция снимков "execution"
- тесты: test_transitions (14), L2 91/0, L4 9/9, гейт 15/15, приёмка 35/35
```
