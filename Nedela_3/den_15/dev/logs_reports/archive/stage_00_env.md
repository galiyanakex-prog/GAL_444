# stage_00_env.md — Этап 0. Окружение + метапромты

## Было
- Только `START-PROMT.md` и `Задание_Д12.txt`; переменные окружения не зафиксированы;
  метапромтов частей не было.

## Стало
- Проверено окружение: `$PY --version` = Python 3.13.5 (exit 0); `$VENV_ACT` существует (exit 0).
- Зависимости: `requests` 2.34.2 ✅, `dotenv` ✅; `pytest` ❌ отсутствует.
  Решение (§6.1): L2 выполняется `$PY $TST/unit_runner.py` (допустимая альтернатива).
- Справочники на месте: Step-3.5-Flash_params.md, МЕТА-ПРОМТ_den_N.md, DONOR,
  Суть_N3.md, Задание_Д12.txt, arch_den_11.md, arch_den_11_plan_2.md.
- Подобраны флаги koda (§5): неинтерактивный режим `koda --yolo -p <файл промта>`
  (сабкоманды `koda run` НЕТ). Зафиксировано в run_log.md.
- Сгенерированы и ПРОВЕРЕНЫ 8 метапромтов частей в `$MP`:
  ПРОМТ_{storage,memory,llm,prompt,agent,state,cli,readme}.md.
  Каждый по структуре §4: цель/граница → вход → контракты → выход → критерий готовности →
  правила дня, с явной привязкой к пунктам §7 и к §6.1.

## Корректировки по итогам ревью метапромтов (до написания кода)
- Унифицирован критерий: `$PY $TST/unit_runner.py` вместо `pytest` (его в venv нет).
- `storage`: добавлены `safe_name`, схема SQLite `profiles`, схема `session.json` с parent_id,
  семантика «profile.json — зеркало, авторитет — SQLite».
- `memory`: контракт дополнен `PolicyEngine` (п.18), сигнатурами 4 слоёв и инвариантами
  (неизменяемость+parent_id; рабочая память = состояние, не лог; профиль = merge).
- `llm`: зафиксированы константы MODEL/URL/цены/окно/таймаут/задержки 429 (п.16).
- `prompt`: устранена циклическая зависимость (PromptContext объявлен в agent.py),
  раскрыт «резерв» как лимит бюджета, а не сообщение.
- `agent`: добавлены поля deliver/session_id/message_counter/initialized и механика
  switch_task (отметка + новая задача со ссылкой на источник).
- `cli`: убран запрет «не переносить команды донора» (противоречил п.16 токен-учёт+CSV);
  добавлены /tokens /cost и флаги --token-log/--price-*/--fresh.
- Добавлен `ПРОМТ_readme.md` (п.15 текстовая часть + п.19 Проверка в dev).

## Проверка
- `$PY --version` → exit 0.
- `koda --help` → exit 0 (флаги `-p`, `-y` подтверждены).
- `ls $MP/*.md` → 8 файлов.

## Статус
✅ Этап 0 закрыт. Следующий этап — Этап 1 «Скаффолд» (каркас файлов по $META).