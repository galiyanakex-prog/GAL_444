# этот файл не подлежит изменениям ни при каких обстотельствах
AI Advent AI_9   🔥 День 18. Планировщик и фоновые задачи

Руководитель проекта (куратор): Алексей Гладков (нужно следовать его указаниям).


Сделайте MCP-инструмент с отложенным или периодическим выполнением.

Пример:

👉 reminder
👉 периодический сбор данных
👉 регулярный summary

Инструмент должен:

👉 сохранять данные (JSON или SQLite (в зависимости от вашей архитектуры))
👉 выполняться по расписанию
👉 возвращать агрегированный результат

Результат:

Агент, который работает 24/7 и периодически выдаёт сводку

Формат отчёта:

Видео + Код




Чат проекта:

Алексей Гладков:
MCP же это протокол, не мы его описали, не мы его реализовали

Ну ок мы его реализовали но по спеке)

У нейронки нет стейта

Ему присылается каждый раз ВЕСЬ диалог

И на базе ВСЕГО диалога вычисляется следующий шаг )

Каждый такой ход это отдельный turn

То есть

Turn 1.
User: Hello, find my github repos + tools_list

В ллмку улетело вот это
В ответ прилетело user wants github repo, get gh_list tool

Turn 2.
User: Clone repo1

В ллмку улетело вот это
User: Hello, find my github repos + tools_list
LLM: user wants github repo, get gh_list tool
User: Clone repo1

Ну и так далее

Там есть доп разметка чтобы отделять system_prompt, user, assisstant

Но в целом нейронка это огромный калькулятор которым пользуются миллионы людей

Она не подстравивается под конкретного юзера)

Меняется только Input )




Алексей Гладков:
Что я имею ввиду под раздутием контекста

Turn 1. Get tool list = допустим описание схемы 4000 токенов

Дальше гет тул лист не передается еще раз с каждым ходом заново, но! вырезать его нельзя, поэтому с каждым ходом он будет жрать эти 4000 токенов даже если ты всю схему не используешь (ну например тебе на 1000 ходов понадобилось вызвать 1 тул только их схемы но много раз)

Но расход все равно будет 4 млн токенов потому что в каждый ход будет улетать схема

Это частично нивелируется кэшем

Konstantin Svitkov:
Ага. Понял. В том плане, что любой сервер даст стабильный оверхед на список всех своих тулзов.

MaxOnMars:
Правильно ли я понимаю, что можно сделать типа каждые n-минут worker получает погоду через MCP и сохраняет результат в SQLite. Например, раз в час worker собирает сохранённые наблюдения, считает фактические показатели и передаёт их модели. Модель пишет короткую сводку на основе этих данных. Затем можно спросить «Покажи последнюю сводку» — MCP-инструмент вернёт уже сохранённый текст, без нового запроса к модели.

Иван Щитов 🚂:
О, я ровно так и сделал. Ну, разве что не раз в час, а раз в минуту для наглядности

Виктор 👺:
И вот у меня тоже не понимание, почему это выходит лучше чем skill+cli, если там по сути также тянутся все описания команд и тд. Или дело в том, что скилы будут подгружаться только когда нам нужно с конкретной областью поработать,а mcp get_tools будут от всех mcp всегда в контектсе

