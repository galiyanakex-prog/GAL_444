# migr_plan_10.md — рабочий план этапа M10 «Тесты и отладка»

> Рабочий план этапа **M10** миграции `den_16` (Ревизия 2). **Конец — гейт в M11**.

---
## 0. Паспорт этапа

| Поле | Значение |
|---|---|
| Этап | **M10** — детерминированно закрыть новый функционал тестами |
| Зависит от | M1–M9 |
| Открывает | `migr_plan_11.md` (M11 — документация) |
| Тип изменений | `dev/tests_debug/*` |
| Живой ключ | **Не нужен** (всё на заглушках, без сети) |
| Точка отката | Гейт M9→M10 |

**Цель.** Расширить тестовый контур: HTTP-транспорт, `call_tool`, `ToolPolicy`/
`ToolExecutor`, tool-use цикл, блок `[tools]`; сохранить нерегрессию.

---

## 1. Шаги этапа

### Шаг 10.1 — `unit/test_mcp.py` (расширение)
- HTTP-транспорт: выбор по конфигу; фикс `input_schema`; `call_tool` (fake: успех/
  `isError`/исключение); нормализация `CallToolResult`.
- `ToolPolicy`: enabled / схема / стадия / инвариант / confirmation.
- `ToolExecutor`: исполнение, отказ, аудит (`append_tool_audit`).
- tool-use цикл (MockClient): намерение → tool call → ответ; лимит итераций.
- Блок `[tools]` + `ToolPromptPolicy` лимиты.
- «MCP не трогает состояние»; «MCP off по умолчанию».

### Шаг 10.2 — `scenario.py` (L4)
- `scenario_llm_tool_use` (mock: «найди город Москва» → вызов → ответ);
- `scenario_tool_denied` (policy-отказ);
- сохранить 10 прежних сценариев (регрессия).

### Шаг 10.3 — `smoke.py` (L3)
- MCP tool-use in-process (fake).

### Шаг 10.4 — `check_acceptance.sh` (гейт)
- Добавить проверки: реальный `call_tool` на fake → результат; tool-use на MockClient;
  блок `[tools]` в промте; аудит-запись. Сохранить идемпотентность.

### Шаг 10.5 — Прогон
- L1 → L2 → L3 → L4 → гейт; всё без живого ключа/сети; `.tmp/` изолирован.

---

## 2. Выход этапа
- Расширенные unit/scenario/smoke/гейт; нерегрессия подтверждена.
- Запись M10 в `migr_log.md`.

## 3. Гейт M10→M11
- [ ] unit — все зелёные (включая новые проверки MCP/tool-use);
- [ ] `scenario.py` — зелёные (включая `scenario_llm_tool_use`);
- [ ] `smoke.py` — SMOKE OK;
- [ ] `check_acceptance.sh` — расширен, идемпотентен, зелёный (кроме известных ⚠, если есть);
- [ ] всё без живого ключа/сети; `users/` не тронуты.

**Зелёный** → запись ✅ → перечитать `migr_plan.md` → `migr_plan_11.md`.

## 4. Запись в `migr_log.md`
По форме §6.4.

## 5. Следующий шаг
Перечитать `dev/migr_plan.md` → M11 (`migr_plan_11.md`): документация.