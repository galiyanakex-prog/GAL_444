# migr_plan_8.md — рабочий план этапа M8 «Документация + финал»

> **Роль документа.** Рабочий план-алгоритм **одного этапа** миграции проекта `den_16`
> на целевую архитектуру `Nedela_4/den_16/arch_den_16.md`. Разворачивает этап **M8** из
> `migr_plan.md` §4. Документация фактической реализации + финальная приёмка
> (`arch_den_16.md` §4.1–4.3, §5). **Это финальный этап миграции** — после него
> записывается «Итог миграции» в `migr_log.md` и предлагается коммит (коммит — только
> по явной команде пользователя).
> Источники: `migr_plan.md` (эталон, перечитан перед созданием этого плана),
> `arch_den_16.md` §4–§5, `Nedela_3/den_15/README.md` (база README),
> `dev/Проверка.md` (35 строк + задел 36–40), `dev/tests_debug/scenario/scen_1.md`
> (сценарий den_15 — переписывается под День 16).

> **Синхронизировано с фактом реализации: 2026-09-24.** Правки внесены строго по
> `README.md` (факт реализации) и `dev/migr_log.md` (история процесса) — оба
> равноправные источники истины. `dev/migr_log.md` не изменялся; исходная версия —
> в `dev/old_vers/`.

---
## 0. Паспорт этапа

| Поле | Значение |
|---|---|
| Этап | **M8** — Документация + финал |
| Рабочий план | `dev/migr_plan_8.md` (этот файл) |
| Зависит от | **M0–M7** (весь вертикальный срез + тестовый контур) |
| Открывает | — (финальный этап; далее — «Итог миграции» + предложение коммита) |
| Закрывает | `arch_den_16.md` §4.1–4.3 (артефакты + критерии 36–40), §5 (финал) |
| Основные артефакты | `README.md` (корень дня), `dev/Проверка.md` (36–40), `dev/tests_debug/scenario/scen_1.md` |
| Живой ключ | **Не нужен** (финальный прогон — MockClient/FakeMCPTransport) |
| Меняет поведение | **Нет** (только документация; код не трогается) |

**Цель этапа.** Описать фактическую реализацию (README с разделом «Подключение MCP»,
Проверка.md 36–40, scen_1.md под День 16), закрыть 2 известных ⚠ гейта ([2] README
непустой, [7] раздел «Модель памяти») → гейт **18/18**, приёмка **40/40**, запись
«Итог миграции» в `migr_log.md`, предложение коммита.

---

## 1. Вход и предусловия

- M0–M7 ✅ (журнал `dev/migr_log.md`): L2 **112 OK / 0 FAIL** (11 модулей), L3 SMOKE
  OK (6 тестов), L4 **10/10**, гейт **16 из 18** (2 ⚠ — только README [2]/[7]),
  интеграционный stdio-прогон demo_server — EXIT=0 (3 тула).
- `README.md` в корне дня **отсутствует** — создаётся на базе `den_15/README.md`.
- `dev/Проверка.md` — 35 строк зелёные, строки 36–40 — задел 🟡.
- `dev/tests_debug/scenario/scen_1.md` — сценарий den_15, переписывается под День 16.
- Спорное из M7 (решается на этом этапе): судьба смоуков этапов `.tmp/` (m1–m6) —
  по традиции den_15 остаются как доказательства этапов; `handle_mcp_command` читает
  приватное `gateway.gateway._servers` — задокументировать как известную
  шероховатость (кандидат на публичный accessor дня 17+).

**Предусловия:** документация сверяется с кодом (без расхождений); счётчики в README —
по факту (команды, флаги 14, сценарии 10, гейт 18, приёмка 40, L2 112).

---

## 2. Шаги этапа (исполняемый алгоритм)

### Шаг 8.1 — `README.md` в корне дня
База — `Nedela_3/den_15/README.md`; изменения:
1. Заголовок/шапка — День 16 «Подключение MCP» (неделя 4, формула ценности:
   соединение + список инструментов — не хак, а первый вертикальный срез подсистемы).
2. Раздел «Что нового относительно Дня 15» — MCP-срез (контракты, реестр, MCP-слой,
   хранение, CLI, тестовый контур).
3. **Раздел «Модель памяти»** — сохраняется из den_15 (закрывает ⚠ [7] гейта).
4. **Раздел «Подключение MCP»** (новый): модель инструментов (`core/tools.py`),
   реестр (`core/tool_registry.py`), MCP-слой (`integrations/mcp/*` — config /
   transport / client / gateway / provider / demo_server), хранение
   (`users/<id>/integrations/mcp/{servers,catalog}.json`), команды `/mcp` (6 форм),
   флаги `--mcp` / `--mcp-probe`, изоляция прав (allowed/denied_tools),
   «инструмент ≠ переход», MCP off по умолчанию, деградация при недоступном сервере.
5. Счётчики по факту: флаги **14** (12 + 2), команды REPL + семейство `/mcp`,
   сценарии **10**, гейт **18**, приёмка **40**, L2 **112**.
6. Разделы den_15 (персонализация, инварианты, переходы, запуск, архитектура,
   тестирование, заделы) — обновляются под факт den_16 (дерево с `integrations/`,
   `core/tools.py`, `core/tool_registry.py`; заделы недели 4).
**Ожидаемый результат:** README непустой, содержит «Модель памяти» и
«Подключение MCP» → проверки гейта [2]/[7] зелёные.

### Шаг 8.2 — `dev/Проверка.md` строки 36–40
1. Строки 36–40: статус 🟡 → ✅, команды проверки — по факту (гейт №16–18,
   `test_mcp.py`, `scenario_mcp_discovery`, `--mcp-probe`).
2. Итог файла: **40/40** (35 прежних без регрессии + 36–40); машиночитаемый дубль —
   гейт **18/18**.
**Ожидаемый результат:** чек-лист приёмки 40/40, формулировки = код.

### Шаг 8.3 — `dev/tests_debug/scenario/scen_1.md` под День 16
Переписать сценарий ручной демонстрации (база — den_15):
1. Шаги MCP: `--mcp-probe` (one-shot: READY → 3 тула → DISCONNECTED), REPL
   `/mcp status` → `/mcp servers` → `/mcp connect` → `/mcp tools` → `/mcp refresh` →
   `/mcp disconnect`; фильтр прав (denied_tools в servers.json); недоступный сервер
   (деградация, REPL жив).
2. Сохранить краткие шаги наследия (память/профили/переходы — 1–2 демо на выбор),
   соответствие требованиям `Задание_d16.txt`, «замечания для честности демо»
   (fake vs живой stdio, MCP off по умолчанию).
**Ожидаемый результат:** сценарий демонстрирует результат задания («код, который
подключается к MCP и выводит список доступных инструментов»).

### Шаг 8.4 — сверка документации с кодом
Прогнать grep-сверку ключевых формулировок README/Проверки с фактом (имена команд,
флаги, счётчики, пути файлов, тексты вывода probe). Расхождения — устранить
(правится документация, не код).

### Шаг 8.5 — финальный прогон (L1 → L4 → гейт → приёмка)
1. L1: `py_compile` всей сборки (включая `integrations/mcp/*.py`).
2. L2: `env -u API_KEY ../../.venv/bin/python dev/tests_debug/unit_runner.py` →
   **112 OK / 0 FAIL**.
3. L3: `API_KEY=test-key ... smoke.py` → SMOKE OK (6 тестов).
4. L4: `API_KEY=test-key ... scenario.py` → **10/10**.
5. Гейт: `API_KEY=test-key bash dev/tests_debug/check_acceptance.sh` → **18/18,
   exit 0** (README закрывает [2]/[7]); повторный прогон — идемпотентность.
6. Приёмка: 36–40 зелёные + прежние 35 без регрессии → **40/40**.
**Ожидаемый результат:** гейт 18/18 exit 0; приёмка 40/40.

### Шаг 8.6 — запись в `migr_log.md` + предложение коммита
1. Запись этапа M8 по форме §6.4 (статус ✅, артефакты, прогоны, гейт финальный).
2. Запись «Итог миграции» (DoD §7: артефакты §4.1, 36–40 зелёные, 35 без регрессии,
   гейт 18/18, этапы M0–M8 ✅, планы 0–8 исполнены, перечитывания зафиксированы).
3. Решение по смоукам `.tmp/` (m1–m6): остаются как доказательства этапов
   (традиция den_15) — фиксируется в журнале.
4. Черновик сообщения коммита — пользователю (коммит — только по явной команде).

---

## 3. Выход этапа

- `README.md` (корень дня) — с разделами «Модель памяти» и «Подключение MCP».
- `dev/Проверка.md` — 40/40.
- `dev/tests_debug/scenario/scen_1.md` — под День 16.
- `dev/migr_log.md` — этап M8 ✅ + «Итог миграции».
- Гейт **18/18 exit 0**, приёмка **40/40**, L2 112/0, L4 10/10.

## 4. Гейт (финальный)

Критерии (`migr_plan.md` §4 M8): 36–40 зелёные **и** прежние 35 зелёные; гейт 18/18;
артефакты `arch_den_16.md` §4 присутствуют; все этапы M0–M8 в `migr_log.md` (✅).
Зелёный → миграция завершена (DoD §7); предложение коммита пользователю.