# migr_plan_12.md — рабочий план этапа M12 «Финал + актуализация arch_den_16.md»

> Рабочий план этапа **M12** миграции `den_16` (Ревизия 2). **Финал.**

---
## 0. Паспорт этапа

| Поле | Значение |
|---|---|
| Этап | **M12** — привести архитектурный документ к факту и закрыть миграцию |
| Зависит от | M0–M11 |
| Открывает | — (финальный гейт) |
| Тип изменений | `arch_den_16.md` (актуализация), `dev/migr_log.md` |
| Живой ключ | Для финального прогона — да (живой прогон «найди Москва») |
| Точка отката | Гейт M11→M12 |

**Цель.** Актуализировать `arch_den_16.md` под фактическую реализацию (реальный сервер,
HTTP, `tools/call`, tool-use, аудит, память) и закрыть миграцию.

---

## 1. Шаги этапа

### Шаг 12.1 — Актуализация `Nedela_4/den_16/arch_den_16.md`
Привести документ в соответствие с реализацией (равноправные источники истины: README и
`migr_log.md`):
- §1 (идея/принципы): MCP — реальный внешний сервер (HTTP), не только stdio; `tools/call`
  + LLM tool-use — часть дня 16 (не «задел дня 17+»); сохранённые границы («инструмент ≠
  переход», MCP off по умолчанию, модель MCP не течёт в `core`).
- §2.1 (дерево): + `core/tool_executor.py`, `core/tool_policy.py`, `HttpMCPTransport`,
  обновлённые `llm_client`/`prompt_builder`/`agent`/`Kod`.
- §2.2–2.4: `call_tool` (не задел), `ToolExecutionResult`/`ToolExecutionState`;
  HTTP-транспорт и выбор по конфигу; реальный сервер.
- Новые разделы: **LLM tool-use** (нативный + fallback, блок `[tools]`, `ToolPromptPolicy`,
  цикл агента, лимит итераций); **`ToolExecutor`/`ToolPolicy`** (процедура/проверки/аудит);
  **аудит** `tool_audit.jsonl` (задействован); **память** — `external_actions[]`, сырое не
  в память.
- §4 (артефакты/критерии приёмки): переписать под реальное соединение/список/вызов/tool-use.
- §5–7: обновить порядок достижения, DoD, формулу/краткое описание, сводную механику.
- Убрать утверждения, ставшие неверными (например, «tools/call — задел», «HTTP — задел»,
  «живые внешние серверы — вне дня 16»).

### Шаг 12.2 — Финальный прогон
- L1 → L2 → L3 → L4 → гейт (без живого ключа/сети);
- живой прогон «найди город Москва» (LLM + реальный MCP) — подтверждение.

### Шаг 12.3 — Запись итога
- «Итог миграции (Ревизия 2)» в `migr_log.md`; предложение коммита (коммит — только по
  явной команде пользователя).

---

## 2. Выход этапа
- `arch_den_16.md` актуализирован под факт.
- Финальные прогоны зелёные; живой прогон подтверждён.
- Итог в `migr_log.md`.

## 3. Финальный гейт
- [ ] `arch_den_16.md` соответствует реализации (реальный сервер/HTTP/`call`/tool-use/
      аудит/память/дерево/критерии);
- [ ] L1/L2/L3/L4/гейт зелёные (без живого ключа/сети);
- [ ] живой прогон «найди город Москва» подтверждён;
- [ ] прежние критерии приёмки без регрессии;
- [ ] все этапы M0–M12 в `migr_log.md` (✅).

## 4. Запись в `migr_log.md`
По форме §6.4 + «Итог миграции (Ревизия 2)».

## 5. Завершение
Миграция закрыта. Предложить коммит (только по явной команде пользователя).