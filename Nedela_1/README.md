# AI Advent — практика работы с LLM API

Учебный репозиторий с практическими заданиями курса AI Advent.

Проект последовательно знакомит с работой через LLM API: от первого запроса и сохранения истории диалога до управления форматом ответа, температуры генерации, способов рассуждения и сравнения разных моделей.

## Выполненные задания

| День | Тема | Результат |
|---|---|---|
| [День 1](den_01) | Первый запрос к LLM через API | Консольный чат-бот с историей диалога |
| [День 2](den_02) | Управление форматом ответа | Сравнение свободного, JSON и stop-sequence режимов |
| [День 3](den_03) | Способы рассуждения | Четыре решения одной задачи и их автоматическое сравнение |
| [День 4](den_04) | Температура генерации | Исследование влияния temperature на точность, креативность и разнообразие |
| [День 5](den_05) | Версии моделей | Сравнение слабой, средней и сильной моделей по качеству, скорости и стоимости |
| [GUI-интерфейс](tk_ned_1) | Графический запуск программ | Tkinter-чат для запуска всех Den_*.py с настройкой опций |

## Технологии

- Python;
- [routerai.ru API](https://routerai.ru/api/v1/chat/completions);
- модель `stepfun/step-3.5-flash` (основная);
- дополнительные модели: `sao10k/l3-lunaris-8b`, `openai/gpt-5-nano`, `deepseek/deepseek-v4-pro-0813`;
- характеристики, свойства и параметры модели — см. [Step-3.5-Flash_params.md](Step-3.5-Flash_params.md);
- `python-dotenv`, `requests`, `tkinter`.

## Структура репозитория

```text
Nedela_1/
├── den_01/          # Первый запрос к LLM, чат-бот с историей
│   ├── Den_1_Kod.py
│   ├── README_d1.md
│   ├── run_den1.sh
│   └── run_den1.desktop
├── den_02/          # Управление форматом ответа
│   ├── Den_2_Kod.py
│   ├── README_d2.md
│   ├── run_den2.sh
│   └── run_den2.desktop
├── den_03/          # Способы рассуждения
│   ├── Den_3_Kod.py
│   ├── README_d3.md
│   ├── run_den3.sh
│   └── run_den3.desktop
├── den_04/          # Температура генерации
│   ├── Den_4_Kod.py
│   ├── README_d4.md
│   ├── run_den4.sh
│   └── run_den4.desktop
├── den_05/          # Версии моделей
│   ├── Den_5_Kod.py
│   ├── README_d5.md
│   ├── DeepSee-V4-Pro-0813.md
│   ├── GPT-5-Nano.md
│   ├── Llama-3-8B-Lunaris.md
│   ├── run_den5.sh
│   └── run_den5.desktop
├── tk_ned_1/        # Графический интерфейс (Tkinter)
│   ├── main-tk.py
│   ├── README_tk.md
│   ├── chat-log.md
│   ├── run_main-tk.sh
│   └── run_main-tk.desktop
├── .env.example
├── .gitignore
├── Step-3.5-Flash_params.md
└── README.md
```

Каждая директория содержит самостоятельное практическое задание, запускаемый код и отдельное описание. Для удобства запуска из графического окружения в каждой папке есть `.sh`-скрипт и `.desktop`-ярлык (двойной клик или `./run_*.sh`).

## Подготовка проекта

### 1. Клонирование

```bash
git clone <url>
cd Nedela_1
```

### 2. Виртуальное окружение

Создайте виртуальное окружение:

```bash
python -m venv .venv
```

Активируйте его.

Linux:

```bash
source .venv/bin/activate
```

Windows:

```bash
.venv\Scripts\activate
```

### 3. Зависимости

```bash
pip install python-dotenv requests
```

### 4. API-ключ

Получите ключ в [RouterAI](https://routerai.ru).

Создайте в корне проекта файл `.env` на основе `.env.example`:

```env
API_KEY=ваш_ключ_от_routerai
```

Файл `.env` уже добавлен в `.gitignore`. Не публикуйте его и не передавайте другим людям.

## Запуск заданий

Все команды выполняются из корня репозитория (`Nedela_1/`).

### Графический запуск

Для запуска из файлового менеджера двойным кликом — используйте `.desktop`-ярлыки в каждой папке:

- `den_01/run_den1.desktop`
- `den_02/run_den2.desktop`
- `den_03/run_den3.desktop`
- `den_04/run_den4.desktop`
- `den_05/run_den5.desktop`
- `tk_ned_1/run_main-tk.desktop`

### Терминальный запуск

#### День 1 — чат-бот

```bash
# Через скрипт (из корня Nedela_1/)
./den_01/run_den1.sh

# Или напрямую (с активированным .venv)
python den_01/Den_1_Kod.py
```

### День 2 — управление форматом ответа

```bash
# Через скрипт
./den_02/run_den2.sh

# Или напрямую (с активированным .venv)
python den_02/Den_2_Kod.py

# Свободный режим
./den_02/run_den2.sh --free "Напиши рецепт салата Цезарь"
# ...
```

### День 3 — способы рассуждения

```bash
# Через скрипт
./den_03/run_den3.sh

# Или напрямую (с активированным .venv)
python den_03/Den_3_Kod.py

# Прямой ответ
./den_03/run_den3.sh --direct "Решите задачу: у фермера есть куры и кролики. Всего 15 голов и 42 ноги."
# ...
```

### День 4 — температура генерации

```bash
# Через скрипт
./den_04/run_den4.sh

# Или напрямую (с активированным .venv)
python den_04/Den_4_Kod.py

# Температура 0.3 — детерминированный
./den_04/run_den4.sh --temp-0.3 "Как распространяются короткие радиоволны в пространстве?"
# ...
```

### День 5 — версии моделей

```bash
# Через скрипт
./den_05/run_den5.sh

# Или напрямую (с активированным .venv)
python den_05/Den_5_Kod.py

# Слабая модель (Llama 3 8B)
./den_05/run_den5.sh --weak "У фермера есть куры и кролики. Всего 15 голов и 42 ноги."
# ...
```

### GUI-интерфейс (Tkinter)

Графический интерфейс для запуска всех Den_*.py программ из одного окна:

```bash
# Через скрипт
./tk_ned_1/run_main-tk.sh

# Или напрямую (с активированным .venv)
python tk_ned_1/main-tk.py
```

Возможности:
- выбор программы из выпадающего списка (Den_1…Den_5);
- настройка опций каждой программы (чекбоксы, текстовые и числовые поля);
- отправка сообщений через окно чата;
- автоматическое логирование сессии в `tk_ned_1/chat-log.md`.

Подробности — см. [tk_ned_1/README_tk.md](tk_ned_1/README_tk.md).

## Что изучено

### День 1

- создание клиента для работы с LLM API;
- роли `system`, `user` и `assistant`;
- отправка запроса и получение ответа;
- хранение истории текущего диалога;
- обработка ошибок API.

### День 2

- управление структурой ответа через промпт;
- смысловое ограничение длины;
- параметр `max_tokens`;
- явное условие завершения ответа (`stop sequence`);
- сравнение результатов с одинаковым пользовательским запросом;
- валидация JSON-ответа.

### День 3

- прямой запрос без дополнительных инструкций;
- пошаговое решение (chain-of-thought);
- генерация промпта самой моделью;
- имитация группы экспертов;
- автоматическая оценка нескольких решений (LLM-as-a-Judge);
- обработка ограничения токенов в минуту (TPM-лимит);
- опциональная сверка с эталоном.

### День 4

- влияние параметра `temperature` на ответы модели;
- детерминированный ответ (temperature = 0.3);
- баланс точности и разнообразия (temperature = 0.7);
- высокая креативность (temperature = 1.3);
- произвольная температура (0.0–2.0);
- автоматическое сравнение и оценка через LLM-as-a-Judge;
- обработка TPM-лимита с автоматическим retry.

### День 5

- сравнение моделей разного уровня мощности;
- слабая модель (Llama 3 8B Lunaris) — быстрая и дешёвая;
- средняя модель (GPT-5 Nano) — баланс цена/качество;
- сильная модель (DeepSeek V4 Pro) — максимальное качество;
- замер времени ответа, количества токенов и стоимости;
- расчёт стоимости на основе прайса API;
- сводная таблица сравнения метрик;
- LLM-as-a-Judge для оценки соотношения качество/ресурсы.

## Ограничения API

Доступное количество запросов и токенов зависит от тарифного плана. При превышении лимита токенов в минуту (HTTP 429) программы автоматически повторяют запрос после паузы 60 секунд (до 2 попыток).

## Цель репозитория

Главная цель проекта — на небольших запускаемых примерах понять, как формулировка промпта и параметры API влияют на поведение языковой модели.
