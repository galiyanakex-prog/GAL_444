---
## Отчёт по шагу «0.1 Приёмка каркаса» (2026-09-13)
- Каркас `den_08` создан по соглашению МЕТА-ПРОМТа: 11 файлов, пустые — все рабочие
  (README, код, .sh, .desktop, оба промта, `agents_memory_8.md`), непустые — `plan_8.md`
  (37,4 КБ) и шаблон `mem_8.md` (5,2 КБ), плюс `START-PROMT_d8.md` (6,4 КБ).
- Справочные файлы в день не копированы; чужие дни не изменены (git: только новые файлы
  внутри `den_08/`).
- Запись в mem_8.md: 0.1. Следующий шаг: 1.1 Ознакомление с `AI_9_main.md`.
---
## Отчёт по шагам «1.1 AI_9_main.md» и «1.2 Суть_N2.txt» (2026-09-13)
- AI_9_main.md: выписан чек-лист комплектности дня (README_d8.md, den_8_Kod.py, den_8_run.sh,
  den_8_run.desktop), требование запуска только в venv недели → в коде не зависим от пакетов
  вне venv (tiktoken отсутствует — не используем обязательно).
- Суть_N2.txt: теоретическая опора Дня 8 — §2.3 (окно, 8–10K токенов на диалог с кодом,
  ~100 сообщений до лимита), §2.2 «Токенизация и обрезка», §2.1 stateless-модель (контекст
  пересылается заново → токены запроса растут), §7.2 (JSON жрёт токены).
- Записи в mem_8.md: 1.1, 1.2. Следующий шаг: 1.3 README_N2.md.
---
## Отчёт по шагам «1.3 README_N2.md», «1.4 Step-3.5-Flash_params.md», «1.5 Задание_Д8.txt» (2026-09-13)
- README_N2.md: взят как эталон структуры README дня; отмечено, что файл отстал (только День 6)
  и что формулировка про 429 (60 с, 2 попытки) расходится с кодом донора (2→4→8 с) — в
  README_d8.md пишем фактическое поведение.
- Step-3.5-Flash_params.md: зафиксированы числа для расчётов — окно 262K, 11 ₽/1M in,
  33 ₽/1M out, max_tokens поддерживается → идут в константы + флаги.
- Задание_Д8.txt: зафиксированы 6 трактовок из чата курса (prompt_tokens = текущий запрос;
  история = сумма всех usage; локальная оценка chars/4; переполнение — искусственным лимитом;
  «что ломается» показывать кодом; локальная оценка = приблизительная).
- Записи в mem_8.md: 1.3, 1.4, 1.5. Следующий шаг: 1.6 донор den_7_Kod.py (стр. 1–300).

---
## Отчёт по шагам «1.6–1.8 Донор den_7_Kod.py» (2026-09-13)
- Прочитаны стр. 1–300 (шапка/импорты/флаги/константы), 360–540 (`_ask_llm`,
  `_build_messages_*`), 1099–1235 и 1470–1667 (JSON-снимок, CLI-цикл).
- Главный вывод: `_ask_llm` читает только `choices`, `usage` выбрасывается — это точка роста
  Дня 8; `max_tokens` в запрос не кладётся; 4xx про переполнение даётся общей «Ошибка HTTP».
- Заготовки: идиома фиксации путей через BASE_DIR, `.get()`-совместимость снимка (v1
  загрузится в v2), идиома веток команд в CLI.
- Записи в mem_8.md: 1.6, 1.7, 1.8. Следующий шаг: 1.9.

---
## Отчёт по шагам «1.9–1.11» (2026-09-13)
- 1.9: снят эталон README дня и комплекта запуска (cd по dirname скрипта + venv недели +
  проброс аргументов, абсолютный Exec в .desktop); в README_d8 добавим разделы «Тарификация»
  и «Рост стоимости».
- 1.10: пустота 6 рабочих файлов подтверждена; выписаны 5 обязательных уроков (readline,
  reconfigure errors='replace', BASE_DIR, «День 8» в description, CSV/печать не роняют чат).
- 1.11: итог ознакомления — 6 проблем (размытая формулировка, игнор `usage` в доноре,
  цена реального переполнения, отсутствие tiktoken, отставший README_N2, «День 7» в
  description) и план правок 3.1–3.12.
- Записи в mem_8.md: 1.9, 1.10, 1.11. Следующий шаг: 2.1 (ПРОМТ_den8_.txt).

---
## Отчёт по шагам «2.1 ПРОМТ_den8_.txt», «2.2 ПРОМТ_den8_Kod.txt» (2026-09-13)
- Блок 1 (ознакомление) закрыт: 11 записей в mem_8.md, все ✅.
- 2.1: написан пошаговый промт создания проекта (232 строки) — с разделами «что переносим
  из den_07 без изменений» и «что меняем под День 8», ШАГИ 1–6 и ПОДШАГИ 2.1–2.11,
  7 критериями приёмки дня.
- 2.2: написан промт кода (175 строк) — точные требования к den_8_Kod.py: константы цен,
  флаги, поля usage, 10 функциональных требований, структура 14 блоков, 10 проверяемых
  критериев приёмки, 8 запретов (в т.ч. «не жечь живой API», «не требовать tiktoken»).
- Записи в mem_8.md: 2.1, 2.2. Следующий шаг: 3.1 (копия донора + шапка «День 8»).

---
## Отчёт по шагу «3.1 Копия донора + шапка День 8» (2026-09-13)
- Создан `den_8_Kod.py` (1672 строки) копией донора; имена дня полностью переименованы,
  `CONTEXT_VERSION = 2`, шапка переписана под идею Дня 8 (stateless-модель, тариф, три
  счётчика из usage, локальная оценка, дефект донора).
- Проверки: py_compile OK; grep по «den_7/Den_7/den_07/День 7/…» — пусто; дефолты артефактов
  уже ден_8-ные.
- Запись в mem_8.md: 3.1. Следующий шаг: 3.2 (константы цен и флаги токен-учёта).

---
## Отчёт по шагу «3.2 Константы и флаги дня 8» (2026-09-13)
- Добавлены 5 флагов (--max-context-tokens, --max-tokens, --token-log, --price-in,
  --price-out), путь TOKEN_LOG по идиоме BASE_DIR, константы PRICE_IN_PER_M=11.0,
  PRICE_OUT_PER_M=33.0, MODEL_CONTEXT_LIMIT=262144, блок эффективных значений и валидации.
- description argparse переведён на День 8 (попутно исправлен битый перенос литерала).
- Проверки: py_compile OK; --help показывает все новые флаги; невалидные лимиты (0, -5)
  дают внятный RuntimeError.
- Запись в mem_8.md: 3.2 (порядок записей 3.1→3.2 восстановлен). Следующий шаг: 3.3
  (estimate_tokens / estimate_messages_tokens).

---
## Отчёт по шагу «3.3 Локальная оценка токенов» (2026-09-13)
- Перенумерованы блоки 5–9 → 6–10, вставлен блок «5. Токен-функции»: ленивый опциональный
  tiktoken (ошибка/отсутствие пакета не мешают), estimate_tokens (кириллица /3, прочее /4,
  округление вверх), estimate_messages_tokens (+4 служебных токена на сообщение).
- Проверки: py_compile OK; изолированный тест блока: A*100→25, а*300→100, hello world→3,
  2 сообщения→29, пустое→0; tiktoken в venv недоступен → работает эвристика. Тест удалён.
- Запись в mem_8.md: 3.3. Следующий шаг: 3.4 (чтение usage в _ask_llm + max_tokens).

---
## Отчёт по шагу «3.4 usage в _ask_llm» (2026-09-13)
- В __init__ добавлены 4 параметра (лимиты/цены) и поля last_usage, usage_total, exchanges,
  token_log_warned; создан _record_usage (нормализация, досчёт total, честный пустой usage).
- _ask_llm: max_tokens в тело запроса; usage читается до проверки choices; контракт
  «текст или None» сохранён; Agent(...) создаётся с новыми аргументами.
- Проверки: py_compile OK; тест на заглушке: 2 обмена → usage_total 240/60/300, requests=2,
  max_tokens=77 дошёл до API. Тестовые файлы /tmp/d8test удалены.
- Запись в mem_8.md: 3.4. Следующий шаг: 3.5 (счётчики запроса/истории/ответа + стоимость).

---
## Отчёт по шагу «3.5 Счётчики и стоимость» (2026-09-13)
- Добавлены tokens_request / tokens_history / tokens_response и cost_rubles / cost_total
  (раздельный тариф входящих и исходящих; None вместо выдуманного нуля при отсутствии usage).
- Найден и закрыт дефект: автосжатие (B/C) шло тем же _ask_llm и перезаписало бы last_usage.
  Введено is_main: служебные токены честно идут в накопительную стоимость, но не подменяют
  счётчики текущего обмена. В send_message добавлен self.exchanges += 1.
- Проверки: py_compile OK; тест (compression, keep=2, 6 сообщений → 6 основных + 1 служ.):
  request=120, response=30 (не 9999), history=11399, cost_total совпал с ручной формулой.
- Запись в mem_8.md: 3.5. Следующий шаг: 3.6 (проверка лимита контекста до отправки).

---
## Отчёт по шагу «3.6 Переполнение контекста» (2026-09-13)
- Добавлены CONTEXT_OVERFLOW_MARKERS (8 маркеров отказа по длине), метод
  _context_limit_check (локальная оценка vs лимит, текст с цифрами и подсказками),
  проверка в send_message между сборкой запроса и _ask_llm (реплика снимается, история целая).
- В обработчике HTTPError — распознавание маркеров и отдельное сообщение про окно модели.
- Проверки: py_compile OK; тест 4 сценариев (каждый с чистого листа): блокировка при
  --max-context-tokens 60 без вызова API; проход короткой реплики; проход при окне модели;
  распознавание 400 «maximum context length» с целой историей.
- Запись в mem_8.md: 3.6. Следующий шаг: 3.7 (CSV-журнал den_8_tokens.csv).

---
## Отчёт по шагу «3.7 CSV-журнал токенов» (2026-09-13)
- Добавлены import csv и append_token_log(agent): строка на обмен, заголовок 10 колонок
  один раз, стоимости с 6 знаками, пустые ячейки при отсутствии usage; ошибки записи
  (битый путь) не роняют чат — предупреждение один раз за сессию (token_log_warned).
- Вызов вставлен в send_message после exchanges += 1.
- Проверки: py_compile OK; тест: 2 обмена → 3 строки CSV с верными номерами, токенами
  и стоимостями; битый путь → оба обмена прошли, предупреждение единственное.
- Запись в mem_8.md: 3.7. Следующий шаг: 3.8 (usage в JSON-снимке, v2 + загрузка v1).

---
## Отчёт по шагу «3.8 usage в JSON-снимке» (2026-09-13)
- save_context_json: добавлен ключ usage_total (копия словаря). load_context_json:
  восстановление с .get-дефолтами (v1 → нули + предупреждение «снимок старого формата»),
  битый блок перехватывается (ValueError/TypeError → нули, загрузка не падает).
- Дефект найден тестом (int("мусор") падал) и исправлен try/except.
- Проверки: py_compile OK; тест: v2 сохраняет/восстанавливает 300 токенов и 2 обмена;
  v1 грузится с предупреждением и нулями; битый usage не роняет загрузку.
- Запись в mem_8.md: 3.8. Следующий шаг: 3.9 (команды /tokens и /cost).

---
## Отчёт по шагу «3.9 Команды /tokens и /cost» (2026-09-13)
- Добавлены cmd_tokens (3 счётчика + локальная оценка последнего сообщения, None →
  «нет данных») и cmd_cost (обмен / сессия / прогноз на 100 обменов); ветки в главном
  цикле по образцу /save, доступны во всех 3×3 режимах.
- Проверки: py_compile OK; тест: до обмена «нет данных» и «Прогноз недоступен»; после —
  120/30/150, 0.002310 ₽, прогноз 0.23 ₽; 9/9 комбинаций режимов отработали.
- Запись в mem_8.md: 3.9. Следующий шаг: 3.10 (команда /scenario с y/N перед живым API).

---
## Отчёт по шагу «3.10 Команда /scenario» (2026-09-13)
- Добавлены cmd_scenario (короткий / длинный / сверх-лимита; длинные тексты генерируются
  локально) и ветка в главном цикле. Живой API только после явного «y»; иначе пометка
  «оценка без API». Сценарий сверх лимита блокируется до запроса (0 ₽).
- Проверки: py_compile OK; тест: Enter/n → только оценки; y → живые прогоны влезающих
  сценариев + блокировка сверх-лимита; 9/9 режимов. Живых запросов с реальным ключом нет.
- Запись в mem_8.md: 3.10. Следующий шаг: 3.11 (проверка reconfigure + readline в коде).

---
## Отчёт по шагу «3.11 Уроки Дней 6–7 в коде» (2026-09-13)
- Правок не требуется: readline (try/except ImportError), stdin.reconfigure(utf-8,
  errors=replace), пути через BASE_DIR, log_error со стеком — всё на месте (стр. 43–307).
- Проверки: grep подтвердил все 4 урока; тест: CP1251-байты и escape-стрелки не ломают
  чат (2 обмена, история целая).
- Запись в mem_8.md: 3.11. Следующий шаг: 3.12 (вычитка: grep, wc -l, --help, сводка).

---
## Отчёт по шагу «3.12 Вычитка блока 3» (2026-09-13)
- Сводная проверка без правок кода: py_compile OK; grep старых имён пуст; 2336 строк
  (донор 1672 + ~664 дня 8); --help: 5 новых флагов + «Чат-бот Дня 8»; смоук test-key —
  старт/сохранение/выход без падения; артефакты смоука удалены.
- Блок 3 (код) ЗАВЕРШЁН: 3.1–3.12, все записи в mem_8.md ✅.
- Запись в mem_8.md: 3.12. Следующий шаг: Блок 4 (README_d8.md, den_8_run.sh, den_8_run.desktop).

---
## Отчёт по шагу «4.1 Комплект дня 8» (2026-09-13)
- Созданы den_8_run.sh (cd dirname + venv недели + "$@", +x), den_8_run.desktop (абс. Exec,
  Terminal=true, +x), README_d8.md (~13 КБ по структуре README_d7: тарификация, рост
  стоимости, /scenario, 3 сценария проверки, таблица файлов с CSV).
- Проверки: запуск .sh из чужого CWD (/tmp) — работает, артефакты в den_08; оба файла +x;
  комплектность соответствует AI_9_main.md.
- Запись в mem_8.md: 4.1. Следующий шаг: Блок 5 (8 тестов приёмки + сводная таблица).

---
## Отчёт по шагу «5.1 Сводная таблица тестов» (2026-09-13)
- Все 8 тестов приёмки ✅: py_compile/--help; смоук test-key ×2; счётчики на заглушке
  точны (120/30/150, CSV); рост входящих виден; блокировка --max-context-tokens 60;
  устойчивость (битый JSON, v1, битый CSV, CP1251, стрелки); .sh из чужого CWD;
  команды в 9/9 режимах.
- Два ложных падения — ошибки тестовой обвязки (короткая реплика; clean() удалял
  подготовленный файл), код не менялся. Тестовые файлы удалены.
- Запись в mem_8.md: 5.1 (сводная таблица). Следующий шаг: Блок 6 (итоговый вывод).

---
## Отчёт по шагу «6.1 Итог дня 8» (2026-09-13)
- Проект Дня 8 ЗАВЕРШЁН: комплект (den_8_Kod.py 2336 строк, README_d8.md, run.sh/.desktop,
  2 метапромта), все требования задания реализованы, 8/8 тестов приёмки ✅.
- Живой прогон с реальным ключом и коммит не выполнялись — только по явному запросу.
- Запись в mem_8.md: 6.1. План plan_8.md выполнен полностью (блоки 0–6).

---
## Отчёт по шагу «6.2 Синхронизация plan_8.md с МЕТА-ПРОМТ» (2026-09-13)
- В plan_8.md (п. 4 «Правил работы») «не больше трети контекстного окна» заменено на
  «без риска выйти за 70% контекстного окна» — по прямой правке пользователя в
  МЕТА-ПРОМТ_den_N.md. Проверено: «треть» нигде не осталось, «70%» в обоих файлах.
- Запись в mem_8.md: 6.2.
