ПРОМТ: День 7 — проект den_07 «Сохранение контекста» (пошаговое создание проекта)

Роль

Ты — опытный Python-разработчик и преподаватель. Ты ведёшь учебный проект AI Advent AI_9 (неделя N2, день 7) и выполняешь шаги этого промта строго последовательно, один за другим. После каждого шага делай запись в журнале .mem_7 (den_07/.mem_7) и краткий отчёт в agents_memory_7/agents_memory_7.txt.

Контекст проекта

- Репозиторий: AI_9 (только Python-стек). Неделя: Nedela_2. День: den_07.
- Базовые правила: AI_9/AI_9_main.md — каждый день обязан содержать README_*.md, den_*_Kod*.py, den_*_run*.sh, den_*_run*.desktop.
- Задание дня: den_07/Задание_Д7.txt — «Сохранение контекста»:
  1) хранить историю диалога (messages) в JSON;
  2) при перезапуске агента загружать историю обратно;
  3) продолжать диалог так, как будто агент не выключался.
  Проверка на практике: начать диалог → перезапустить приложение → продолжить диалог и убедиться, что агент помнит прошлые сообщения.
- Теоретическая база: Nedela_2/Суть_N2.txt (§2.1 — «иллюзия памяти создаётся подгрузкой сохранённых данных из файлов»; §3.1 — Session Memory исчезает после сессии; Д7 закрывает этот недостаток).
- База кода: den_06/den_6_Kod.py — CLI-чат-бот с LLM (класс Agent, 3 стратегии контекста/памяти через флаги --context/--memory, retry при 429, Markdown-лог). День 7 реализует ОДНУ программу на базе этой заготовки: den_07/den_7_Kod.py (уже скопирован в den_07 как заготовка).
- API: RouterAI, OpenAI-совместимый, endpoint https://routerai.ru/api/v1/chat/completions, модель stepfun/step-3.5-flash; ключ — переменная API_KEY в .env (load_dotenv() находит .env вверх по дереву каталогов).
- Стиль кода: учебный однофайловый скрипт, комментарий почти к каждой строке на русском, класс Agent, зависимости только requests и python-dotenv.
- Окружение: общий venv недели Nedela_2/.venv (python 3.13); .sh делает cd "$(dirname "$0")", активирует ../../.venv/bin/activate и запускает python den_7_Kod.py "$@".

ШАГ 1. Создай пошаговый промт ПРОМТ_den7_Kod.txt по созданию кода

Файл: den_07/МетаПромт_7/ПРОМТ_den7_Kod.txt (сейчас пустой).

Напиши промт генерации/переработки den_7_Kod.py по образцу den_06/МетаПромт/ПРОМТ_Den_6_Kod(3in1).txt (разделы: Роль → Задача → Контекст проекта → Ключевая идея → Класс Agent → Функциональные требования → Структура кода → Формат вывода в консоли → Критерии приёмки → Чего делать НЕ нужно). Обязательные требования к коду:

1. Основа — заготовка den_07/den_7_Kod.py (копия den_6_Kod.py): сохранить класс Agent с сигнатурой __init__(system_prompt, context_strategy="sliding_window", memory_type="session", window=10, keep=6, load_layers="all"), стратегии _build_messages_*, _ask_llm (retry при 429: 3 попытки, 2→4→8 сек, таймаут 30 сек), общий каркас CLI.
2. Главное новое — JSON-персистентность контекста (требование Задание_Д7.txt):
   - метод Agent.save_context_json(filepath) — полный снимок состояния агента в JSON: версия формата, context_strategy, memory_type, window, full_history (все записи с role/content/timestamp и layer для layered), archived, summary, evicted_total, дата сохранения;
   - метод Agent.load_context_json(filepath) — восстановление состояния из JSON; при отсутствии/повреждении файла — понятное сообщение [Память] и чистая сессия (не падать);
   - при старте программы: если файл контекста существует — загрузить его автоматически и сообщить [Память] Контекст восстановлен: N сообщений (саммари: M символов); если нет — [Память] Прошлый контекст не найден, начинаю новую сессию;
   - при /exit (и при Ctrl+C/Ctrl+D) — автоматически сохранить контекст в JSON и сообщить [Память] Контекст сохранён: <файл>;
   - имя JSON-файла по умолчанию: den_7_context.json (флаг --context-file для переопределения); путь привязан к BASE_DIR (каталогу скрипта), как и лог.
3. Привести тексты заготовки к Дню 7: шапка-комментарий с ключевой идеей дня (JSON-персистентность = «агент не выключался»), заголовки вывода «🤖 Чат-бот — День 7 …», лог Den_7_log.md, журнал ошибок den_7_error.log, упоминания «День 6» заменить на «День 7».
4. Команды CLI сохранить: /history, /window, /summary, /compress, /layers, /layer, /clear, /exit. Дополнить: /save — ручное сохранение контекста в JSON; /load — ручная загрузка контекста из JSON (поверх автозагрузки при старте). /clear очищает контекст сессии в памяти (JSON-файл на диске не удаляется — долговременная память управляется явно).
5. Markdown-лог (Den_7_log.md) сохранить как есть — это человекочитаемая копия диалога; JSON — машиночитаемое состояние для восстановления.
6. Критерии приёмки ПРОМТ_den7_Kod.txt:
   - сценарий задания выполняется: диалог → /exit → повторный запуск → агент помнит прошлые сообщения (продолжение диалога без повторного приветствия «с чистого листа»);
   - JSON валидный, читается json.load, кодировка UTF-8, ensure_ascii=False;
   - повреждённый/отсутствующий JSON не роняет программу;
   - все стратегии (--context/--memory) работают как в дне 6, состояние каждой корректно сохраняется и восстанавливается;
   - py_compile проходит; каждая значимая строка прокомментирована на русском.
7. Чего делать НЕ нужно: не менять API/модель/retry-логику; не подключать векторные БД, tool calls, субагентов; не хранить ключ API в коде; не переписывать каркас с нуля — только переработка заготовки.

ШАГ 2. Выполни ПРОМТ_den7_Kod.txt

- Переработай den_07/den_7_Kod.py по промту из ШАГА 1 (заготовка уже в den_07).
- Проверь: python3 -m py_compile den_07/den_7_Kod.py → OK.
- Прогони смоук-тест с тестовым ключом (API_KEY=test-key → 401 без retry-пауз): старт, /exit — EXIT 0; проверь автозагрузку/автосохранение JSON.
- Прогони функциональный тест с заглушкой _ask_llm (в /tmp, реальные логи не трогать): диалог → сохранение → новый экземпляр → загрузка → история на месте.

ШАГ 3. Создай и заполни файлы-зависимости

- den_07/README_d7.md (сейчас пустой): по образцу README_d6.md — суть дня, что нового относительно Дня 6 (JSON-персистентность), команды, запуск, файлы проекта, результат. Упомянуть сценарий проверки из задания.
- den_07/den_7_run.sh (сейчас пустой):
  #!/bin/bash
  cd "$(dirname "$0")"
  source ../../.venv/bin/activate
  python den_7_Kod.py "$@"
  (chmod +x)
- den_07/den_7_run.desktop (сейчас пустой): по образцу den_6_run.desktop — Name=День 7 — Сохранение контекста, Exec=<абсолютный путь к den_7_run.sh>, Terminal=true.
- Проверь комплектность дня по AI_9_main.md: README_d7.md, den_7_Kod.py, den_7_run.sh, den_7_run.desktop — все на месте и непустые.

ШАГ 4. Итоговое тестирование проекта

- Соответствие Задание_Д7.txt: сценарий «диалог → перезапуск → агент помнит» — проверить фактически (с заглушкой API или тестовым ключом), в /tmp.
- Соответствие ПРОМТ_den7_.txt и ПРОМТ_den7_Kod.txt: все критерии приёмки закрыты.
- Наличие невнесённых правок из .mem_7: перечитать журнал, убедиться, что всё учтено.
- py_compile, смоук-тест .sh (echo "/exit" | ./den_7_run.sh из чужого CWD — артефакты пишутся в den_07).

ШАГ 5. Финал

- Запиши подробный итоговый вывод о проделанной работе в .mem_7 (итоговый блок) и краткий отчёт в agents_memory_7/agents_memory_7.txt.

Чего делать НЕ нужно (на уровне всего проекта)

- Не менять исторические документы: Задание_Д7.txt, agents_memory_7/.plan_7.
- Не трогать файлы den_06 и другие дни.
- Не коммитить без явного запроса пользователя.
