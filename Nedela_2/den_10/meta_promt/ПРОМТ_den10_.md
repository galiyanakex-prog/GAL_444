ПРОМТ: День 10 — проект den_10 «Управление контекстом: разные стратегии (без summary)» (пошаговое создание проекта)

Роль

Ты — опытный Python-разработчик и преподаватель. Ты ведёшь учебный проект AI Advent AI_9
(неделя N2, день 10) и выполняешь шаги этого промта строго последовательно, один за другим.
После каждого шага делай запись в журнале agents_memory_10/.mem_10 и краткий отчёт (3–5 строк)
в agents_memory_10/.agents_memory_10. Не переходи к следующему шагу, пока не записан результат
текущего.

Контекст проекта

- Репозиторий: AI_9 (только Python-стек). Неделя: Nedela_2. День: den_10 (нумерация дней
  сквозная для всех недель).
- Базовые правила: AI_9/AI_9_main.md — каждый день обязан содержать README_*.md,
  den_*_Kod*.py, den_*_run*.sh, den_*_run*.desktop. Справочные файлы недели (AI_9_main.md,
  Суть_N2.txt, README_N2.md) в каталог дня НЕ копируются — только чтение по месту и выжимки
  в журналы.
- Задание дня: den_10/Задание_Д10.txt — «Управление контекстом: разные стратегии (без
  summary)»: реализовать минимум 3 стратегии управления контекстом и переключатель между
  ними; протестировать на одном сценарии («собираем ТЗ 10–15 сообщений»); сравнить качество
  ответа, стабильность, расход токенов, удобство. Результат: агент с 3 стратегиями
  (Sliding Window / Facts / Branching) + сравнение результатов. Формат: видео + код.
- Разрешение неоднозначностей задания (чат курса — считать авторитетным; в .plan_10 Ч1–Ч4):
  Ч1 — «отбрасывание» = УДАЛЕНИЕ из контекста, не сжатие (Алексей Гладков); N — параметр;
  Ч2 — окно едет «сырым», обогащённым JSON фактов в том же промпте (Ivan Bazhenov);
  Ч3 — facts — «сухая выжимка по шаблону», обновлять только при появлении новых фактов
  (Dmitry Sevostianov);
  Ч4 — факты можно хранить «многие ко многим» и не в одном месте (Алексей Гладков) —
  усложнение сверх минимума, фиксируется в README как вариант развития.
- Требования пользователя к Branching (обязательные): ветки хранятся в отдельной директории
  `Branches/` в папке дня; каждая ветка — отдельный файл; активная ветка видна в терминале;
  `/branch list` — просмотр; `/branch switch <имя>` — переключение.
- Теоретическая база: Nedela_2/Суть_N2.txt — §2.2 (Sliding Window: простая, для одной темы;
  Topic Branches: дробление по темам, промышленное решение), §3.1 (Vector Base Memory:
  факты→эмбеддинги «очень много жрёт токенов» — поэтому в Д10 факты БЕЗ эмбеддингов,
  key-value), §2.1 (отсутствие памяти между сессиями).
- Модель и тариф: Nedela_2/Step-3.5-Flash_params.md — окно 262K, входящие 11 ₽/1M, исходящие
  33 ₽/1M, temperature 0–2.
- База кода: den_09/den_9_Kod.py (2846 стр.) — CLI-чат-бот: класс Agent, 3 стратегии
  контекста (sliding_window/history_compression/context_leveling), 3 типа памяти,
  токен-учёт (usage, CSV, /tokens /cost /scenario), /compare (до/после сжатия),
  /compress on|off, JSON-снимок v3, retry при 429. Sliding Window уже есть — День 10
  добавляет Facts и Branching и сравнение стратегий.
- API: RouterAI, OpenAI-совместимый, endpoint https://routerai.ru/api/v1/chat/completions,
  модель stepfun/step-3.5-flash; ключ — переменная API_KEY в .env (load_dotenv()).
- Окружение: общий venv недели Nedela_2/.venv (python 3.13; requests, python-dotenv).
- Стиль кода: учебный однофайловый скрипт, комментарий почти к каждой строке на русском,
  класс Agent, блоки разделены комментариями «1. Импорты…», «4. Константы…» и т.д.

ЧТО ПЕРЕНОСИМ ИЗ den_09 БЕЗ ИЗМЕНЕНИЙ

- Каркас файла и порядок блоков (импорты → load_dotenv + проверка API_KEY → флаги →
  BASE_DIR и пути артефактов → константы → log_error → estimate_tokens → detect_layer →
  класс Agent → функции лога → CSV → CLI-цикл).
- Токен-учёт целиком: usage в _ask_llm, счётчики, стоимость, CSV-лог, /tokens /cost
  /scenario, контроль лимита (--max-context-tokens).
- Стратегии sliding_window, history_compression, context_leveling и типы памяти — задание
  говорит «минимум 3», готовое не выбрасываем.
- Сжатие истории Дня 9 (порог, консолидация, архив, /compress on|off) — остаётся для
  режима compression.
- Retry при HTTP 429 (RETRY_DELAYS = [2, 4, 8]), REQUEST_TIMEOUT = 30, log_error со стеком.
- JSON-персистентность и Markdown-лог, команды /history /window /summary /compress /layers
  /layer /save /load /clear /exit.
- Уроки Дней 6–9: readline в try/except, sys.stdin.reconfigure(errors='replace'), пути через
  BASE_DIR, приоритет «явный флаг > снимок > константа», отдельные накопители служебных
  запросов, комментарий почти к каждой строке.

ЧТО МЕНЯЕМ ПОД ДЕНЬ 10

- Шапка-комментарий: идея дня — не одна техника, а НАБОР стратегий + переключатель +
  сравнение на одном сценарии; «без summary» относится к трём обязательным стратегиям
  (окно/факты/ветки), режим compression Дня 9 сохраняется как дополнительный.
- Имена артефактов: Den_10_log.md, den_10_error.log, den_10_context.json, den_10_tokens.csv,
  den_10_facts.json, директория Branches/. CONTEXT_VERSION = 4.
- Флаг --context: choices += facts, branching.
- Новые поля агента: self.facts (dict), self.facts_usage (накопитель служебных запросов
  обновления фактов — по образцу summary_usage), self.branches, self.active_branch.
- Новые методы: _update_facts (служебный LLM-запрос по шаблону, is_main=False),
  _build_messages_facts, _build_messages_branching, branch_create/list/switch.
- Новые команды: /facts (печать блока фактов), /branch list|switch|new, /compare-strategies
  (сценарий «ТЗ» на трёх стратегиях, сравнение по 4 критериям, живой API за y/N).
- Приглашение ввода показывает активную ветку: «Вы [main]:» (режим branching).
- Чего НЕ было в Дне 9 и не должно появиться «случайно»: потери сообщений при переключении
  веток; падения на битом файле ветки; вызова _update_facts вне режима facts; упоминаний
  «День 9»/«den_9».

ОБЩИЕ ПРАВИЛА РАБОТЫ (для всех шагов)

- Каждый подшаг — не больше трети контекста: читай файл частями, меняй точечно.
- После каждой правки кода: python -m py_compile den_10_Kod.py.
- Живой прогон с реальным API-ключом — только с явного согласия пользователя (деньги).
  Для тестов: API_KEY=test-key (401 без пауз) или заглушка модуля requests.
- Тестовые файлы — в /tmp, после теста удалять. Рабочие артефакты чужих дней не трогать.
- Исторические файлы (Задание_*, .plan_*, чужие .mem*, README_N2.md, коды Дней 6–9) не
  менять. Не коммитить без прямого запроса пользователя.

ШАГ 1. Создай промт генерации кода ПРОМТ_den10_Kod.txt

Файл: den_10/meta_promt/ПРОМТ_den10_Kod.txt (сейчас пустой).
Напиши промт переработки den_09/den_9_Kod.py → den_10/den_10_Kod.py по образцу
den_09/meta_promt/ПРОМТ_den9_Kod.txt. Обязательные разделы: Роль → Задача → Контекст
проекта → Ключевая идея дня → Механика трёх стратегий (что откуда берётся) → Класс Agent
(что добавить) → Функциональные требования (нумерованный список) → Структура кода (порядок
блоков) → Формат вывода в консоли (примеры) → Критерии приёмки → Чего делать НЕЛЬЗЯ.
В «Чего делать НЕЛЬЗЯ» включить: не терять сообщения при переключении веток; не ронять чат
на битом файле ветки/снимка; не вызывать _update_facts вне режима facts; не менять публичный
контракт _ask_llm; не отправлять живые запросы без y/N; не оставлять в коде и файлах слово
«День 9»/«den_9»/«Den_9».

ШАГ 2. Код den_10_Kod.py (подшаги 3.1–3.10, после каждого — py_compile)

ПОДШАГ 3.1. Скопируй донора и перепиши шапку

Файл: den_10/den_10_Kod.py.
Скопируй den_09/den_9_Kod.py в den_10/den_10_Kod.py. Перепиши шапку-комментарий: ключевая
идея Дня 10 (набор стратегий + переключатель + сравнение; три обязательные стратегии и где
они в коде) + примеры запуска. Замени все вхождения имён дня: den_9 → den_10, Den_9 →
Den_10, все падежи «День 9» → «День 10». CONTEXT_VERSION = 4.
Проверка: python -m py_compile; grep -c "den_9\|Den_9\|День 9\|Дня 9\|Дне 9\|Дней 9" = 0.

ПОДШАГ 3.2. Флаги и константы стратегий

--context choices += facts, branching; strategy_map дополнить; константы: FACTS_FILE
(den_10_facts.json), BRANCHES_DIR (BASE_DIR/Branches), DEFAULT_BRANCH = "main", FACTS_PROMPT
(шаблон: извлеки факты — цель/ограничения/предпочтения/решения/договорённости — в формате
«ключ: значение»; верни только новые/изменённые; если новых нет — пусто; язык = язык
переписки).
Проверка: py_compile; --help показывает новые choices.

ПОДШАГ 3.3. Стратегия Facts: поля агента и обновление фактов

__init__: self.facts = {} (загрузка из FACTS_FILE), self.facts_usage (накопитель по образцу
summary_usage). Метод _update_facts(user_message): служебный LLM-запрос (is_main=False,
temperature=self.compress_temperature), мердж результата в self.facts, сохранение в
FACTS_FILE; вызов из send_message после добавления реплики пользователя, ТОЛЬКО в режиме
facts. Токены — в facts_usage, не в «текущий запрос».
Проверка: py_compile; тест с заглушкой requests: факты извлекаются и мержатся.

ПОДШАГ 3.4. Стратегия Facts: сборщик запроса _build_messages_facts

system + «Важные факты из диалога (ключ: значение): …» (если facts не пуст) + последние N
сообщений «сырыми» (Ч2). Подключить в диспетчер _build_messages. Команда /facts — печать
текущего блока фактов.
Проверка: py_compile; тест с заглушкой: в теле запроса есть facts и окно.

ПОДШАГ 3.5. Стратегия Branching: хранение веток

BRANCHES_DIR создаётся при старте режима branching; self.branches (имя → путь),
self.active_branch; файл ветки — JSON (имя, создан, родитель/checkpoint, messages[]);
методы: _branch_path, branch_create(name) (копирует текущий диалог как checkpoint),
branch_list(), branch_switch(name) (сохранить текущую → загрузить выбранную); автосоздание
main при старте режима; в общий снимок v4 — только active_branch и список имён.
Проверка: py_compile; тест в /tmp: файлы веток создаются, переключение меняет историю.

ПОДШАГ 3.6. Стратегия Branching: сборщик запроса и видимость активной ветки

_build_messages_branching: system + сообщения АКТИВНОЙ ветки (полная история ветки, без
сжатия). Приглашение ввода: «Вы [main]:»; шапка при старте печатает активную ветку; после
/branch switch — подтверждение.
Проверка: py_compile; тест: приглашение содержит имя ветки.

ПОДШАГ 3.7. Команды /branch list, /branch switch, /branch new

cmd_branch(agent, parts) в CLI-цикле: list (таблица с пометкой активной), switch <имя>
(несуществующая — ошибка со списком), new <имя> (создать от текущего места); без аргумента —
подсказка; в режимах не-branching — сообщение, что режим не branching.
Проверка: py_compile; CLI-тест: list/switch/new работают.

ПОДШАГ 3.8. Сравнение стратегий: /compare-strategies

Сценарий «собираем ТЗ» (10–15 коротких сообщений с фактами: цель, сроки, ограничения,
решения — генерируются локально) прогоняется на каждой из трёх стратегий (sliding-window /
facts / branching) во ВРЕМЕННЫХ экземплярах Agent с артефактами в /tmp; печатается
сравнение по 4 критериям задания: качество (контрольный вопрос про ранние детали),
стабильность (вхождения ключевых фактов в ответ), расход токенов (usage), удобство
(описание поведения). Живой прогон — только после явного y/N; без него — локальная оценка.
Проверка: py_compile; тест с заглушкой: таблица печатается, «N» — без API.

ПОДШАГ 3.9. Снимок v4 и подключение команд в CLI-цикл

CONTEXT_VERSION=4: в снимок — facts (режим facts), active_branch и список веток (branching);
загрузка v3 без падения; приоритет «явный флаг > снимок» сохраняется; команды /facts,
/branch, /compare-strategies встроены в цикл и в стартовую подсказку.
Проверка: py_compile; тест: снимок v3 грузится, v4 сохраняется с новыми полями.

ПОДШАГ 3.10. Финальная вычитка

grep остатков «День 9»/«den_9» = 0; все новые команды в --help; общий смоук с заглушкой.
Проверка: grep = 0; py_compile OK; смоук-прогон.

ПОДШАГИ 3.1–3.10 выполняются внутри файла den_10_Kod.py; после каждого —
запись в .mem_10 (Файл/Было/Стало/Проверка/Статус/Дата) и отчёт в .agents_memory_10.

ШАГ 3. README_d10.md

Файл: den_10/README_d10.md. По образцу den_09/README_d9.md: Суть проекта → Что нового
относительно Дня 9 → Команды (таблица, включая /facts, /branch list|switch|new,
/compare-strategies) → Запуск (bash-блок + флаги) → Механика трёх стратегий (схема) →
Сравнение стратегий (таблица 4 критериев по результатам сценария) → «Что показал чат курса»
(Ч1–Ч4 + вариант «многие ко многим» как развитие) → Файлы проекта (включая Branches/) →
Общее с Днём 9 → Результат.

ШАГ 4. den_10_run.sh и den_10_run.desktop

den_10_run.sh: #!/bin/bash, комментарий, cd "$(dirname "$0")",
source ../../.venv/bin/activate, python den_10_Kod.py "$@". chmod +x.
den_10_run.desktop: Name=День 10 — Стратегии контекста, Comment, абсолютный Exec на
den_10_run.sh, Icon=utilities-terminal, Terminal=true, Type=Application,
Categories=Development;. chmod +x.
Проверка: запуск .sh из чужого каталога с API_KEY=test-key.

ШАГ 5. Тесты

5.1 py_compile + --help. 5.2 смоук API_KEY=test-key (старт/exit во всех 5 режимах --context).
5.3 Facts: извлечение/мердж/сохранение фактов, facts в запросе, facts_usage (заглушка).
5.4 Branching: создание/список/переключение веток, файлы в Branches/, приглашение с именем
ветки, независимость веток (заглушка). 5.5 /compare-strategies: сценарий «ТЗ» на трёх
стратегиях, таблица 4 критериев, «N» — без API (заглушка). 5.6 Устойчивость: битый JSON
снимка, битый файл ветки, снимок v3, CP1251, стрелки. 5.7 Тесты через .sh из /tmp + сводная
таблица в .mem_10. Все тестовые файлы — в /tmp, удалить после.

ШАГ 6. Финал

6.1 Итоговый вывод в .mem_10 (что получилось, что осталось спорным; закрытие Ч1–Ч4 и
требований пользователя к Branching).
6.2 Финальный отчёт в .agents_memory_10.
6.3 git add/commit — ТОЛЬКО по прямому запросу пользователя.

Критерии приёмки всего дня

1. Три стратегии работают и переключаются: --context sliding-window|facts|branching
   (+ сохранённые compression|leveling).
2. Sliding Window отбрасывает старые сообщения (удаление, Ч1); N настраивается.
3. Facts: блок ключ-значение обновляется после каждого сообщения пользователя (служебный
   запрос по шаблону, только новые факты — Ч3); в запросе facts + «сырое» окно (Ч2);
   токены обновления учитываются отдельно.
4. Branching: ветки в Branches/ (файл на ветку), checkpoint и независимое продолжение,
   активная ветка видна в терминале, /branch list и /branch switch работают.
5. /compare-strategies: сценарий «ТЗ 10–15 сообщений» на каждой стратегии, сравнение по 4
   критериям; живой API — только после y/N.
6. Комплект дня полный: README_d10.md, den_10_Kod.py, den_10_run.sh, den_10_run.desktop.
7. Код запускается в venv недели из любого каталога; устойчив к битым файлам и CP1251;
   снимок v3 грузится с апгрейдом до v4.
8. Справочные файлы не скопированы в день; чужие дни не изменены.
9. Журналы .mem_10 и .agents_memory_10 ведутся по каждому подшагу.
10. Тесты проходят без живого ключа; в коде дня нет упоминаний «День 9»/«den_9»/«Den_9».
